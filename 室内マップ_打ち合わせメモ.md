# 室内マップ 打ち合わせメモ

> 履歴資料です。現行仕様は[現行仕様.md](現行仕様.md)を参照してください。下記には試作時の判断と旧残骸の記録も残します。

## 背景・目的
- Tilemapで室内マップを試作中。最優先目標は「室内マップを正常に作り、プレイヤーが歩ける状態まで動作確認すること」。
- PKD全体をドット絵に統一すると決定したわけではない。今回は室内マップの試作としてドット素材を使う。
- Yarn、シナリオ、既存ゲーム進行には不要な変更を加えない。

## 素材候補（Assets/Sprites/Tiles/ 以下に展開済み、ZIPのまま置かれてはいない）
- `wall_tiles.png`（288×384, 32px基準）
- `floor_tiles.png`（192×288, 32px基準。通常床の他、絨毯の3×3部品・境界・斜め部品が混在）
- `Pixel_16_interiors_v2_free/` 内 `tiles and items.png`（200×200, 16px基準）
- `pixelinterior_LRK_v1.1/`（Bitglow製16×16素材）: `floorswalls_LRK.png`, `cabinets_LRK.png`, `decorations_LRK.png`, `doorswindowsstairs_LRK.png`, `kitchen_LRK.png`, `livingroom_LRK.png`

## 2026-08-28: 床の隙間バグ修正 + 歩行確認 実装ログ
- **原因**: `floor_tiles.png` は16pxグリッドで自動スライス済みだったが、Pixels Per Unit が32のままインポートされていた。Gridのセルサイズ1×1に対し1タイルが0.5×0.5にしかならず、これが床の隙間の直接原因だった。`wall_tiles.png` はグリッド分割すら未実施でPPUも既定値100のままだった。
- **対応**: `wall_tiles.png`/`floor_tiles.png` を32×32グリッドで再スライス（Point Filter / Compression None / PPU32）。`Assets/Editor/RoomSetupTool.cs`（Tools > PKD > Build Starter Room）で機械的に実施。
- **構成**: SampleScene.unityの既存Grid（Cell Size 1×1）配下に `Floor`（9×7マス、隙間なく充填）／`Rug_Decoration`（新規、今回は空レイヤー）／`Wall`（外周1マス厚、TilemapCollider2D＋Rigidbody2D(Static)＋CompositeCollider2Dで衝突をまとめる）を構成。SortingOrderはFloor(0) < Rug(1) < Wall(2)。
- **歩行**: 既存の未使用スクリプト `Assets/Scripts/PlayerMove.cs` を、`transform.position` 直接操作（壁を素通りしてしまう実装だった）からRigidbody2D.MovePosition方式に修正。`RoomPlayer`（仮のピンク色四角スプライト）を部屋中央に配置。
- 今回使用したのは `wall_tiles.png` + `floor_tiles.png` の1系統のみ。LRK / Pixel16 パックは未使用（「無理に複数パックを混ぜず、まず一つの素材系統で統一感のある部屋を作る」という指示に沿ったため）。
- 旧 `Assets/Sprites/Tiles/Room/Room.prefab`（Cell Size 0.5の別Grid、壊れた16pxスライスを参照）はSampleSceneから未参照の残骸と判断し、触らず放置。

## 2026-08-28: 新規リクエスト「タイルフォルダの素材を全部使えるように」
- ユーザー要望：`Assets/Sprites/Tiles/` 以下の素材（LRKパック一式、Pixel_16_interiors一式）を全部使える状態にしてほしい。
- **床壁系の方針（確定）**: `floorswalls_LRK.png`（16px、床・壁の塊2枚のみ）と `tiles and items.png`（Pixel_16_interiors、16px、200×200）は、wall_tiles/floor_tilesと同様に16×16グリッドで再スライスしてTile化する。
- **家具・ドア窓系の方針（確定・実装済み）**: `cabinets_LRK.png` / `decorations_LRK.png` / `kitchen_LRK.png` / `livingroom_LRK.png` / `doorswindowsstairs_LRK.png` は16×16に均等分割できない大きめのオブジェクトが多いため、機械的なグリッドタイル化ではなく、アルファ値の連結成分（4/8近傍のflood fill）でアイテムごとに自動バウンディングボックス切り出しする方式で対応（`Assets/Editor/TileMaterialImportTool.cs`）。
- **`tiles and items.png`（Pixel_16_interiors）の扱いを変更**: ユーザーは当初「16×16グリッドで再スライス」を指示していたが、画像は200×200で16では割り切れず（12.5マス）、実際の中身も暖炉・ベッド・ソファ・ラグ等の可変サイズ家具アイテムの集合で、グリッド分割すると絵が崩れる構成だった。家具系と同じ自動バウンディングボックス切り出し方式に変更して対応（本人へは実装後に報告）。

### 2026-08-28 実装結果（この節は途中で内容が変わったので、最新は下の「最終状態」を見ること）
- `cabinets_LRK.png`: 69アイテムを自動検出・切り出し。
- `decorations_LRK.png`: 16アイテム。
- `doorswindowsstairs_LRK.png`: 42アイテム。
- `kitchen_LRK.png`: 31アイテム。
- `livingroom_LRK.png`: 82アイテム。
- `tiles and items.png`: 18アイテム。
- Unityバッチモードでエラーなしを確認済み。6ファイル全て（cabinets_LRK / tiles and items / livingroom_LRK / decorations_LRK / doorswindowsstairs_LRK / kitchen_LRK）検出バウンディングボックスを元画像に重ねて目視確認し、アイテムの結合・分断ミスなしを確認済み。

### 2026-08-28: floorswalls_LRK.pngのやり直し（重要な訂正）
- 最初に16×16グリッドで224タイルにスライスしたが、ユーザーから「グリッドマスに対して細かすぎる」と指摘。
- ユーザーが提示したitch.io製品ページ（bitglow.itch.io/pixelinterior-livingroomkitchen）の"multiple floors & wallpapers"説明画像を参考に、実際の中身は壁紙3色・パネル3色・レンガ3色・くすみカラー6色＝**15種類の「大きい素材見本」**（各64×30〜48px、1マスの4〜3倍のサイズ）であることが判明。一度この15枚の見本画像をそのままスライスしたが、これも「1マス(16×16px)に収まらない」という問題があり、そのままシーンにドラッグすると巨大な浮いた画像になってしまうことが発覚（ユーザーがUnity上で実際に試して発覚）。
- 最終的に、各見本の中心から16×16pxの「継ぎ目なく繰り返せる最小単位」を切り出し、15枚の実用タイルとして再構成（`Assets/Editor/TileMaterialImportTool.cs`の`FloorswallsSwatches`配列、3回目の修正版）。4×4タイル敷き詰めプレビュー画像を自作して継ぎ目を目視確認済み（15枚全部）。
- **重要**: 「見本画像（大きいプレビュー用の1枚絵）」と「実際にTilemapの1マスに敷き詰められる小さいタイル」は別物、というのがこの一連のやり取りで得られた教訓。今後similar な素材パックを扱うときは、最初から実際にゲームのグリッドサイズに収まるかを確認してから「完了」と報告すること。

## 最終状態（2026-08-28時点、引き継ぎ用まとめ）

**目的**: Tilemapで室内マップを試作し、隙間なく歩ける部屋を作る。あわせて`Assets/Sprites/Tiles/`以下の素材を全部使える状態にする。

**完了していること**:
1. `floor_tiles.png`（Assets/Sprites/Tiles/）: インポート設定修正（PPU32・Point Filter・Compression None）＋32×32グリッドで再スライス。空白セル除外、見た目のわかる名前付き（50枚）。床の隙間バグ（PPU/グリッド不一致が原因）は解消済み。
2. `wall_tiles.png`: 同様に修正（83枚）。
3. `floorswalls_LRK.png`（Assets/Sprites/Tiles/pixelinterior_LRK_v1.1/）: 16×16の実用タイル15枚（壁紙3色・パネル3色・レンガ3色・くすみカラー6色）。上記の訂正済みバージョン。
4. `cabinets_LRK.png`/`decorations_LRK.png`/`doorswindowsstairs_LRK.png`/`kitchen_LRK.png`/`livingroom_LRK.png`/`tiles and items.png`: アルファ連結成分による自動個別アイテム切り出し（計258個、`{ファイル名}_item_{番号}`という名前）。
5. `Assets/Scenes/SampleScene.unity`のGrid配下に`Floor`(9×7マス充填)/`Rug_Decoration`(空レイヤー)/`Wall`(外周、TilemapCollider2D+Rigidbody2D(Static)+CompositeCollider2Dで衝突あり)のTilemap構成。
6. `RoomPlayer`（仮のピンク色スプライト）を部屋中央に配置。`Assets/Scripts/PlayerMove.cs`をRigidbody2D.MovePosition方式に修正済み（元はtransform直接操作で壁をすり抜ける実装だった）。
7. 使用ツール（再実行可・べき等）: `Assets/Editor/RoomSetupTool.cs`（Tools > PKD > Build Starter Room）、`Assets/Editor/TileMaterialImportTool.cs`（Tools > PKD > Import All Tile Materials）。

**未確認・要注意点**:
- **Unity実機画面での見た目は一度も確認できていない**。Claude側はUnityエディタの画面を直接見る手段を持たず、Unityバッチモード（-nographics、コンパイル・インポート確認のみ）と、画像ファイルを直接読んで自作した確認用プレビュー画像でしか検証していない。実際に部屋が正しく表示されるか、Play Modeで隙間なく歩けるか、カメラに収まるかは未確認。
- `floor_tiles.png`/`wall_tiles.png`の一部の名前（`floor_greyPlank_*`、`wall_windowSlit_*`など）は目視での類推で付けたもので、正確な用途は保証できない。
- 旧`Assets/Sprites/Tiles/Room/Room.prefab`、`New Tile Palette.prefab`、`floor_tiles_148.asset`、`floor_tiles_149.asset`は、初期の誤ったスライスの残骸。SampleSceneから未参照だが未削除のまま。

## Hierarchy整理（2026-08-31）

- 旧室内試作のルート`RoomPlayer`は非アクティブで、Scene内参照・コードからの名前参照がないことを確認し、`SampleScene`から削除した。
- 空Transformとして検出された弾発射点と悲しみマップ配置マーカーは、すべて現行コンポーネントから参照されているため残した。
- 各ミニゲームRoot、時計演出、Good Morning演出、弾テンプレートは非アクティブ運用が正常であり、未使用扱いで削除しない。
- ユーザーがUnity上で`brick_darkbrown`を2つ手動でシーンに配置済み（見本画像時代の巨大サイズのもの）。削除するかは未対応。
- Wall Tilemapが1マス厚のままなので、floorswalls_LRKの「壁紙+パネル」を縦2段に重ねた壁を作るには、Wall Tilemapを2マス厚にする追加対応が必要（今回は未着手）。
