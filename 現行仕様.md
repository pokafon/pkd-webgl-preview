# PKD 現行仕様

> 更新日: 2026-09-01
>
> このファイルはゲーム実装の現行仕様を確認するための正本です。物語・台詞の正本は
> [シナリオ全文_読みやすい版.md](シナリオ全文_読みやすい版.md)です。
> `*_打ち合わせメモ.md`と各READMEは経緯・旧仕様を含む履歴資料であり、現行仕様の根拠にはしません。

## 判断の優先順位

1. ユーザーの最新の明示指示
2. 物語・台詞は`シナリオ全文_読みやすい版.md`
3. ゲーム進行・演出・操作は本ファイル
4. UnityのScene・Prefab・C#は実装事実
5. 打ち合わせメモと旧READMEは履歴資料

文書と実装が食い違った場合、黙って片方を採用せず、食い違いを記録して同じ作業内で揃える。

## 全体進行

`Prologue → Anger → IkariBattle → Anxiety → FuanBattle → Escape → BedFlight → Sadness → MemoryRecall → SadnessBattle → Ending`

Yarnからのミニゲーム起動は`MinigameLauncher`の`start_minigame`コマンドへ集約する。

## 怒り戦

- 現行実装は縦型のHP制シューティング。
- 怒りは12HP、コンタックは4HP。
- スペースキーで否定弾を連射する。
- 怒りの赤い弾は、怒り本体の胸付近から出現する。
- 怒りのHPに応じて3段階の台詞・攻撃パターンへ移行する。
- コンタックのHPが0になると、スペースキーで同ラウンドを再挑戦する。
- 旧資料にある「横スクロールする文字を避け、最後に一発だけ撃つ」仕様は廃止済み。

## 不安戦

### 冒頭

- 石畳の上で、不安が上、コンタックが下の近い縦構図から始める。
- 不安だけが先に上へ移動し、完全に画面外へ出る。
- 少し間を置いてから、床を上へスクロールして質問画面へ入る。
- コンタックは世界上で停止し、スクロールによって画面下へ抜ける。
- 冒頭では上側の質問床とYES／NO入口を表示しない。
- Inspector調整項目:
  - `openingChaseDuration`: 不安が画面外へ出る時間
  - `openingAfterExitHoldSeconds`: 退場後の間
  - `openingScrollDuration`: 質問画面までのスクロール時間
  - `openingLayout`: 二人の位置、サイズ、スクロール量

### 質問

- 5問は固定順。
- 仮の正解順は`YES → NO → YES → NO → YES`。
- 各回答直後に正誤判定し、間違えた場合は暗転先から1問目へ戻る。
- 足跡は問題ごとのYES／NO直線経路として内部に保持する。
- 画面には現在の問題に対応する足跡だけを表示する。
- 同じ問題で同じ側を再び選んでも、足跡を重複生成しない。
- 正解そのものは画面へ表示しない。

### 締め

- 全問通過後はYES／NO入口を消し、普通の石畳だけを表示する。
- 不安を上、コンタックを下へ配置する。
- 不安の台詞、コンタックの「それは異常です。」の順に表示する。
- 一拍置いてコンタックが自動で一発撃ち、不安を倒す。

## ベッド飛行

- 旧・家から飛び出す演出は使用しない。参考画像から取り込んだ「ベッド＋寝ている主人公」の一体スプライトで開始し、見切れない範囲で画面全体を自由に飛ぶ。
- 逃避行中だけ、専用素材による街の前景・街の後景・雲を別速度で横スクロールする。各素材は実際の画面幅より外側まで先に生成しておき、画面内へ突然出現させない。
- ベビーメリーは画面上部の右寄りに常時見える位置でカメラへ追従する。UIのように完全固定せず、吊り紐と本体がごく小さく時間差で揺れる。
- メリーは雲より後ろ、かつプレイヤーより後ろへ描画する。位置・大きさ・描画順・色・揺れは`BabyMobileAmbient`のInspectorから調整できる。
- メリーと逃避行背景は`BedFlightRoot`内に限定し、ほかの章では表示しない。
- コンタックのチラ見せ演出は使わない。
- 終盤にプレイヤー操作を止め、コンタックが現れて弾を撃つ。
- 弾は必ずベッドへ命中し、暗転して悲しみ編へ進む。

## 記憶回想・悲しみ戦

- 家の中と屋外の2マップを共有する。
- 記憶回想では母と友達3人の記憶を歩く。
- 友達3人との会話後に17時チャイムを再生し、家の中へ入った瞬間に停止する。
- 悲しみ戦では友達3人を撃破後、帰宅する。
- 母の「おかえり。」、悲しみの「あ…あ…あ…」の後、母を撃つ。
- 最後にコンタックの「これで異常なし」を表示して終了する。

## 全体音量

- 全BGM・SE・チャイムへ`AudioListener.volume = 0.7`を適用し、従来より約3割小さくする。
- `MinigameLauncher.globalAudioVolume`からInspectorで調整できる。

## 現在必要な実機確認

- 不安戦冒頭の距離、退場速度、退場後の間、スクロール速度。
- 不安戦冒頭で質問床・入口が一切映り込まないこと。
- 不安戦で間違えた直後に1問目へ戻り、問題別の足跡が正しく切り替わること。
- 全章をPrologueからEndingまで通した際の画面・BGM・入力状態。

## シーンと素材の保守ルール

- `SampleScene`のルートは`_Core`、`_Presentation`、`_Minigames`、`_World`の4分類に固定する。
- 各ミニゲームは専用Rootを持ち、開始前は非アクティブにする。起動・終了は`MinigameLauncher`へ集約する。
- シーン配置と実行時で別の旧画像を持たせない。現行キャラクター画像をシーンとControllerの両方へ設定する。
- `Enemy`や`Player`だけの曖昧な名前を避け、`AngerActor`、`AnxietyActor`、`ContackPlayer`のように役割を名前へ含める。
- Editorビルダーを再実行しても上記構造と現行素材へ戻ることを必須とする。
- ユーザーが手動調整したドットマップのTilemap Layer、Sorting Layer、Order in Layer、Transform、配置は正とし、明示指示なしに変更・再構築しない。
- WebGLの日本語表示はStatic生成した`NotoSansJP SDF`へ統一し、Yarnと各ミニゲームのC#内にある文字を再生成時に収録する。
- リリース候補はスモークテスト3系統、シーン参照検証、WebGLビルド、ブラウザ起動時のConsoleエラー・警告0件を確認する。
