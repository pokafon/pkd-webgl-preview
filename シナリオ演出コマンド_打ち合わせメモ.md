# シナリオMarkdown演出コマンド 打ち合わせメモ

> 履歴資料です。現行仕様は[現行仕様.md](現行仕様.md)、物語・台詞は[シナリオ全文_読みやすい版.md](シナリオ全文_読みやすい版.md)を参照してください。

## 目的（ユーザー要望、2026-09-02）

シナリオMarkdownに背景・立ち絵・BGM・SE・暗転・待機などの演出指定を書き、Unity上（Yarn経由）に反映できる仕組み。

## 採用した方式

**Markdown → Yarn変換**。理由：既存の`DialogueRunner`(Yarn Spinner 3.2.8)が選択肢・ジャンプ・進行管理を持っており、Markdownをランタイムで直接解釈すると同じ仕組みを二重に作ることになるため。既存の`DialogueVisuals.cs`の`<<background>>`/`<<portrait>>`という`[YarnCommand]`方式（未指定なら現状維持）をそのまま拡張した。

## 実装内容（2026-09-02、実装済み）

- **[DialogueVisuals.cs](Assets/DialogueVisuals.cs)を拡張**：既存の`background`/`portrait`コマンドに加えて
  - `<<portrait_position "left|center|right">>` — 立ち絵の左右位置（既存の単一Imageのx座標を計算して移動。複数立ち絵は将来拡張）
  - `<<bgm "ID">>` / `<<se "ID">>` — NamedClip配列＋AudioSource（bgmはループ、seは単発）。`none`でBGM停止
  - `<<blackout "on|off">>` — 画面全体を覆う黒Imageの表示切替
  - `none`判定を大文字小文字区別なしに修正（Markdown側の小文字`none`とYarn側の`"None"`両方に対応）
  - `<<wait 秒数>>`はYarn Spinner標準の組み込みコマンドをそのまま使用（新規実装なし）
- **[DialogueVisualsUpgradeTool.cs](Assets/Editor/DialogueVisualsUpgradeTool.cs)**（`Tools > DialogueVisuals > Upgrade Scene`）：`SampleScene.unity`に暗転用Canvas+Image（sortingOrder 100）とBGM/SE用AudioSourceを追加・配線するバッチ実行可能なEditorツール。再実行しても安全（既に配線済みなら何もしない）
- **[ScenarioMarkdownToYarnConverter.cs](Assets/Editor/ScenarioMarkdownToYarnConverter.cs)**（`Tools > Scenario > Convert Markdown To Yarn`）：`## scene_id`見出し＋「ラベル: 値」の演出指定＋話者名/セリフ形式のMarkdownを、上記Yarnコマンドを使ったYarnノードへ変換する

### 対応ラベル

| Markdown | Yarn |
|---|---|
| `背景: ID` | `<<background "ID">>` |
| `立ち絵: ID` | `<<portrait "ID">>` |
| `位置: left/center/right` | `<<portrait_position "...">>` |
| `BGM: ID` | `<<bgm "ID">>` |
| `SE: ID` | `<<se "ID">>` |
| `暗転: on/off` | `<<blackout "...">>` |
| `待機: 秒数` | `<<wait 秒数>>` |

未指定の演出はYarn側にも出力しない（＝現状維持）。演出指定にも話者+セリフ形式にも当てはまらない行（選択肢`->`・コメント`//`など）はそのまま素通しする。

### 命名規則・アセット対応

IDはUnity側の`DialogueVisuals`コンポーネントのInspector上の`backgrounds`/`portraits`/`bgmClips`/`seClips`配列（名前→アセット参照）で管理する。Markdown側はIDのみを書き、ファイルパスは持たない。既存の命名（`Room`/`Office`/`Boss`等）と、今後の新しい命名規則（`bg_場所_状態`／`キャラクター名_表情`）は同じ辞書に共存できる（キーが文字列なだけなので、既存のyarnファイルを壊さず新規追加できる）。

## 検証結果（2026-09-02）

- Unity 6000.3.22f1のバッチモードで、`DialogueVisualsUpgradeTool.Upgrade()`実行・コンパイルともにエラーなし
- `ScenarioMarkdownToYarnConverter`のテスト変換（ユーザー提示のサンプルMarkdown）で、期待通りのYarn出力を確認
- Unity Editorを実際に起動し（Unity Pipeline経由）、Play Mode上でDialogueVisualsの各コマンドを直接呼び出して確認：
  - **実機で画面キャプチャして目視確認**：`<<background "Room">>` + `<<portrait "Boss">>` + `<<portrait_position "right">>` → 背景・立ち絵とも表示され、立ち絵が画面右側に配置された
  - **実機で画面キャプチャして目視確認**：`<<portrait "none">>` → 背景はそのまま、立ち絵だけ消えた
  - **コンポーネントの実行時state読み取りで確認（画面キャプチャは確認できず）**：`<<blackout "on">>`/`"off">>` → `blackoutImage.enabled`がtrue/falseに正しく切り替わることを確認。ただしGame Viewのスクリーンショット取得がこの時だけ古いフレームを返し続け、暗転の黒画面自体は画像では確認できなかった（Pipelineのcapture_game_viewツール側の制限の可能性。コード・設定は他の2ケースと同一パターンで、状態は正しく切り替わっているため、実装上の問題ではないと判断しているが、**ユーザー自身の実プレイでの目視確認を推奨**）

## 今後の拡張候補（未実装、指示があれば着手）

フェード・画面揺れ・ノイズ・画面フラッシュ・立ち絵のフェードイン/アウト・複数立ち絵。現状は過剰な汎用フレームワーク化を避け、最小構成で実装した。

## TRUE ENDの相談パート更新・暗転sortingOrder修正（2026-09-03、実装済み）

ユーザーが`シナリオ全文_読みやすい版.md`のTRUE END部分（会社→帰宅→就寝→相談パート）を大幅に書き直したのを受けて、[Escape.yarn](Assets/Yarn/Escape.yarn)の`TrueEnd_Start`〜`TrueEnd_End`を全面反映した。

- シーン1（会社）・シーン2（帰宅）に新しい選択肢を追加（グレーアウトなし、雰囲気演出のみで分岐なし・ユーザー確認済み）
- シーン2は本文が22:30（夜）と明記されていたため、背景を`RoomDay`→`Room`に変更
- 怒り・不安・悲しみへの相談内容を新しい短い掛け合いに全面差し替え、「○○を解放した。」の一文を追加
- **`<<blackout "on">>`/`<<blackout "off">>`を実際に組み込んだ**（各相談の「コンタックが事情を説明する」の前後）
- **既存の「時計＋ノイズ」演出（`ClockGlitchIntro`、怒り戦・不安戦の入退室演出として既に実装済みのもの）を流用するため、[MinigameLauncher.cs](Assets/AngerBattle/MinigameLauncher.cs)に`<<clock_glitch_intro>>`（就寝→精神世界、`clockGlitchIntro`フィールドを流用）と`<<wake_glitch_intro>>`（精神世界→朝、`wakeGlitchIntro`フィールドを流用）を新規追加**。ユーザー指示「時計が進むのまえのシーンの流用でいいでしょ」に対応（新規演出は作らず、既存コンポーネントをそのまま呼び出すだけ）
- **暗転Canvasのバグ修正**：前回実装したBlackoutCanvasの`sortingOrder`が100（対話テキストのCanvas＝0より手前）になっており、暗転中はセリフも見えなくなってしまう不具合があった。`-1`（背景・立ち絵のCanvas＝-10より手前、対話テキストのCanvas＝0より奥）に修正し、暗転中もセリフだけは読める状態にした（[DialogueVisualsUpgradeTool.cs](Assets/Editor/DialogueVisualsUpgradeTool.cs)のソースと、既存シーンの値の両方を修正）

### 検証結果（2026-09-03）

- Unity 6000.3.22f1のバッチモードで、コンパイル・Escape.yarn/yarnPのインポートともにエラーなし
- Unity Editorを実際に起動し、Play Mode上で`<<blackout "on">>`を呼び出し、画面キャプチャで**実機目視確認**：背景が隠れ、対話テキストは引き続き読める状態になっていることを確認（前回できなかった暗転の目視確認、今回は成功）
- `<<clock_glitch_intro>>`/`<<wake_glitch_intro>>`はコンパイル確認のみ（既存の`ClockGlitchIntro.Play()`をそのまま呼ぶだけなので、怒り戦・不安戦で既に動作実績のあるコードパスの再利用。単体での実機確認はまだ）
