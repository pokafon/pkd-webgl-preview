# 不安戦（FuanBattle）打ち合わせメモ

> 履歴資料です。現行仕様は[現行仕様.md](現行仕様.md)、物語・台詞は[シナリオ全文_読みやすい版.md](シナリオ全文_読みやすい版.md)を参照してください。下記には廃止済みの旧仕様も残します。

`怒り戦_打ち合わせメモ.md`と同じ進め方（仕様を決めてからまとめて実装）で進めます。大枠は怒り戦を踏襲する想定ですが、細部は要相談です。

## 呼称について
- 精神パート2。ミニゲーム名は`Anxiety.yarn`内の記述に合わせて`FuanBattle`（不安戦）とする

## Anxiety.yarn（現実パート2「穏やかな服従」ドラフト）の現在の構成

```
Anxiety（05:30 自宅、起床）
  → Anxiety_Train（06:15 満員電車、選択：少し場所を空ける／「当たっています」と伝える）
    → Anxiety_Train_MakeSpace / Anxiety_Train_TryToSpeak
      → Anxiety_Train_Avoid（共通合流）
        → Anxiety_Office（14:30 会社、選択：はい／明日は休みます）
          → Anxiety_Office_Accept / Anxiety_Office_Refuse
            → Anxiety_InnerVoice（？？？の声、4つの台詞）
              → Anxiety_Home（帰宅、選択：電話をかける／やめておく）
                → Anxiety_Phone_Call / Anxiety_Phone_Stop
                  → Anxiety_Phone_After（？？？の声、5つの台詞）
                    → Anxiety_Bed（？？？の声、4つの台詞）
                      → Anxiety_MedChoice（選択肢は「コンタックを飲む」の一択のみ）
                        → Anxiety_TakeMed（服薬、「？？？: どうしよう。ぐるぐる思考が止まらない。」）
                          → <<start_minigame "FuanBattle"> を呼び、不安戦へ接続（記述済み）
```

**作者（ユーザー）注記（Anxiety.yarn内コメントより）**：
- 元の企画テキストにあった「選択不可（言葉が出てこない／波風を立てる理由がない）」の演出は今回は見送り、すべて通常の選択肢として実装。選択肢を削る／選択不可にする対応は方針が固まった次回以降
- `Anxiety_Office_Accept`に「信頼＋1」のような評価演出コメントあり（マルチエンディング用、後で実装予定）
- ノードタイトルは`Anxiety_`系列で統一。ただし服薬ノードのみ、`Anger.yarn`と同名衝突を避けるため`Anxiety_MedChoice`/`Anxiety_TakeMed`にリネーム済み（中身は同一）

---

## 現実パートの対応表（確定）

| # | シーン | 元の台詞（Yarn上のノード） | 対応する弾幕台詞（順番） |
|---|---|---|---|
| 1 | 電車／会社の共通ルート | Anxiety_InnerVoice最後の台詞「でもでもでもでも。」 | 2番目「でもでもでもでも」 |
| 2 | 交際相手への電話 | Anxiety_Phone_Afterの台詞「失敗したな。ダメだな。恥ずかしいな。」「すべてリセットしたい。」 | 1番目「失敗したなダメだな恥ずかしいな」／5番目（最後）「すべてリセットしたい」 |
| 3 | 就寝 | Anxiety_Bed最後の台詞「考えない。考えない。考えない。」 | 4番目「考えない考えない考えない」 |
| 4 | 服薬直前 | Anxiety_TakeMedの台詞「どうしよう。ぐるぐる思考が止まらない。」 | 3番目「どうしようぐるぐる思考が止まらない」 |

※ 5台詞は上記シーンから抜粋したもので、Yarn上の登場順とは一致しない（弾幕演出用に「不安が積み重なる」順番へ再構成済み）

怒り戦は「3シーン→3台詞→弾幕回避×3→敵登場」という組み方だったが、不安戦はシーン数・台詞数がそもそも多い。

**台詞（確定・5つ、順番固定）**：怒り戦の3台詞より多く、畳み掛けるより「不安が積み重なっていく」持続的な流れにする。弾幕として流す文字列には句読点（丸）を付けない。最後は「すべてリセットしたい」で固定
1. 「失敗したなダメだな恥ずかしいな」（元：？？？「失敗したな。ダメだな。恥ずかしいな。」）
2. 「でもでもでもでも」（元：？？？「でもでもでもでも。」）
3. 「どうしようぐるぐる思考が止まらない」（元：？？？「どうしよう。ぐるぐる思考が止まらない。」）
4. 「考えない考えない考えない」（元：？？？「考えない。考えない。考えない。」）
5. 「すべてリセットしたい」（元：？？？「すべてリセットしたい。」／固定の最終台詞）

**台詞の見せ方の見直し（2026-08-19）**：実機で見たところ「でもでもでもでも」「考えない考えない考えない」の2つが単調で退屈だった。以下の方向で修正：

- 主軸3台詞（失敗したな〜／どうしよう〜／すべてリセットしたい）は今まで通り、この順番で1回ずつ流す（最後は必ず「すべてリセットしたい」）
- 合いの手の文字色は**紫**（`fillerColor`、不安本体と同じ色）、主軸は白（`mainPhraseColor`）で視覚的に区別

**表示方式の変更（実装済み）**：合いの手は主軸と順番待ちせず、主軸が流れている間に**ランダムなタイミングで重なって同時に**流れる（`RunAvoidPhases`で主軸用と合いの手用のコルーチンを並行実行）

**合いの手の文言（確定・要実装）**：長い版（「でもでもでもでも」「考えない考えない考えない」）は廃止し、**短い版のみ**を使う
- 「でも」（旧：でもでもでもでも）
- 「考えない」（旧：考えない考えない考えない）
- それぞれ`fillerRepeatCount`回（現状3回）ランダムなタイミングで出現するのは変更なし

---

## 精神パート2「不安戦」の仕様（未定・要相談）

怒り戦（横スクロール弾幕回避＋一撃攻撃）をベースに、以下を相談したい：

- [x] **ゲーム性（確定）**：怒り戦と同じ大枠（弾幕回避→ボス登場→一撃攻撃）を踏襲。差分は「弾幕の動き（蛇行・加速）」「台詞5つ」「敵のセリフが2ブロック」「敵の色（紫）」のみ
- [x] **弾幕で流す台詞**：上記5つに確定
- [x] **弾幕（流れてくる文字）の動き**：怒り戦は右から左への直線移動だったが、不安戦は**不規則に方向転換（ランダムに蛇行・加速）**する動きにする
  - 予測不能な動きで「不安」を煽るイメージ。具体的なパラメータ（蛇行の頻度・振れ幅、加速のタイミング等）は実装時に相談しながら調整
- [x] **敵（「不安」本体）のビジュアル・登場演出（確定）**
  - 見た目：紫色
  - 登場演出：怒り戦の`EnemyAnger`と同じ（画面右側からゆっくりスライドインして定位置へ）
- [x] **敵のセリフ・プレイヤーの返しセリフの中身（確定）**
  > 不安: わたしは不安。
  > 不安: 自分を傷つけるもの避けたい。穏やかな暮らしを送りたい。本当は悩みたくなんてない。
  >
  > 不安: 優しい世界が欲しいだけ。
  >
  > コンタック: それは異常です。（怒り戦と同じ返しセリフ）
  - **表示の分け方（確定）**：2ブロックに分けて、それぞれスペースキーで読み進める
    1. 1ブロック目（「わたしは不安。」＋「自分を傷つけるもの避けたい。穏やかな暮らしを送りたい。本当は悩みたくなんてない。」の2文をまとめて表示）→ スペースで送る
    2. 2ブロック目（「優しい世界が欲しいだけ。」）→ スペースで送る
    3. 続けて「コンタック: それは異常です。」を自動表示（怒り戦と同じく、プレイヤー操作なし・このままスペースで攻撃も可）
  - 実装イメージ：怒り戦の`enemyLine`（単一文字列）を配列化し、`ShowLineAndWaitForSpace`を配列の要素数ぶん順番に呼ぶ形になりそう（`AngerBattleController`と`FuanBattleController`（仮）で共通化できるか、実装時に検討）

- [x] **開始演出の一言（確定）**：怒り戦と文字列も完全に同じ「コンタック: 心の声を震めなくちゃ。」をそのまま使い回す（仕組み：コンタックの一言表示→スペースキーで開始、も同じ）

- [x] **決着方法（確定）**：怒り戦と同じ。「コンタック: それは異常です。」表示中にスペースキーを押すと弾を発射し、命中で一撃撃破・不安戦終了
- [x] **BGM（確定）**
  - 曲名：ワスレナグサ（`Assets/Audio/ワスレナグサ.mp3`に配置済み）
  - テンポ：95 BPM
  - ループ範囲：始点 0:40.421 ～ 終点 4:45.473

---

## 実装方針（怒り戦から踏襲する想定のもの）

- **アセット方針**：既製アセットは使わず自作
- **シーン構成方針**：同じシーン内でミニゲーム一式を非アクティブ⇔アクティブで切り替える方式（`FuanBattleRoot`のような親オブジェクト）
- **Yarn連携方式**：`<<start_minigame "FuanBattle">>` を`MinigameLauncher.StartMinigame()`のswitch文に`case "FuanBattle":`を追加して受ける
- **セリフ表示**：怒り戦で作った「背景パネル＋話者名＋本文」の独立UI一式（`AttackLineText`/`AttackLineCharacterName`/`AttackLineBackground`相当）と同じ仕組みを流用できそう
- **操作キー**：スペースキーに統一（怒り戦と合わせる）

---

## Anger.yarn → Anxiety.yarn への接続（未実装）

現状、`Anger.yarn`側から`Anxiety.yarn`へ`<<jump>>`する記述はまだ無い。怒り戦の後にこの話を続けるには、`Anger.yarn`側（怒り戦終了後の合流地点）から`<<jump Anxiety>>`のような接続が必要。

---

## 実装状況（2026-08-19）

仕様が固まったので、怒り戦と同様に一気に実装した。

**C#側：実装済み・バッチ確認OK**
- `Assets/FuanBattle/FuanBattleController.cs`（新規）：`AngerBattleController`と同じ構造で、5台詞・敵セリフ2ブロック（`enemyLines`配列）・蛇行移動対応
- `Assets/AngerBattle/FallingWord.cs`：`erraticMovement`（蛇行移動）オプションを追加。既存の怒り戦（直線移動）はそのまま動作、不安戦側でオプションを有効にして使う
- `Assets/AngerBattle/MinigameLauncher.cs`：`case "FuanBattle":`を追加。`fuanBattleRoot`/`fuanBattleController`フィールドを追加し、`RunBattle()`で怒り戦・不安戦共通の起動/終了処理にリファクタ
- `PlayerController.cs`/`DenialBullet.cs`/`BattleBGM.cs`は無改造のまま流用

**シーン構築：実施済み（ユーザーの指示によりClaude側でシーンファイルを直接編集）**
- 当初は`Assets/FuanBattle/README_セットアップ手順.md`の手順書に沿ってユーザー側でUnityエディタ上に組んでもらう想定だったが、「やって」との指示で方針転換し、`AngerBattleRoot`一式の構造をコピーする形で`SampleScene.unity`に直接`FuanBattleRoot`一式（Player/Enemy/BulletSpawnPoint/BattleUI（AttackLineText・AttackLineBackground・AttackLineCharacterName）/BGMPlayer/FuanBattleController）を新規追加した
- Enemyの見た目は同じスプライトのまま`SpriteRenderer.Color`を紫（{r: 0.576, g: 0.345, b: 0.827}）に変更
- BGMPlayerは`ワスレナグサ.mp3`（ループ 40.421〜285.473秒）を設定
- `DenialBulletPrefab`/`FallingCharacterPrefab`は怒り戦のプレハブをそのまま参照（複製なし）
- `MinigameLauncher`の`fuanBattleRoot`/`fuanBattleController`参照も設定済み
- バッチモードでのコンパイル＋シーン読み込みは確認済み（エラーなし）。ただし**GameObject一式を丸ごと新規追加する規模のシーン編集をYAML手書きで行っており、通常の小さな追記より確認漏れのリスクが高い**。実機でのPlay確認は必須

**デバッグ機能（実装済み）**
- F1キーでシーンセレクトメニュー（`MinigameLauncher.cs`）。「Anger」「Anxiety」ノードへのジャンプ、「IkariBattle」「FuanBattle」の単体起動に対応

## 修正（2026-08-19、実機フィードバックより）

- [x] **蛇行移動が画面外に出て文字が読めなくなる不具合を修正**
  - `FallingWord.cs`に`erraticVerticalBounds`（初期値 -4〜4）を追加。上下の画面端に達したら跳ね返る（Y方向の速度成分を反転）ようにして、画面外に出続けないようにした
- [x] **「でもでもでもでも」「考えない考えない考えない」の単調さを改善**（上記「台詞の見せ方の見直し」参照）
- [x] **攻撃タイミングの変更（怒り戦・不安戦共通）**
  - 変更前：「それは異常です」表示中にスペースを押すと、同じ押下で即座に弾も発射していた
  - 変更後：スペースでセリフを消す → **1拍（`beatsBeforeFire`）待つ** → プレイヤー操作なしで自動的に弾を発射
  - `AngerBattleController.cs`・`FuanBattleController.cs`両方に反映。バッチ確認OK
- [x] **合いの手を「同時に重なって出現」する方式に変更**
  - 主軸3台詞は順番通り流しつつ、合いの手はランダムなタイミングで重なって同時に流れる（`RunPhraseSequence`＋`RunScatteredFiller`を並行実行）
  - 表現意図：「考えない考えない」と打ち消そうとしても、いろんな考え（でも／すべてリセットしたい、等）が勝手に浮かんでくる感覚を、主軸を律儀に待たない割り込みで表現
- [x] **合いの手の文言を短縮**：「でもでもでもでも」→「でも」、「考えない考えない考えない」→「考えない」（各3回・ランダムなタイミングは変更なし）
- [x] **弾幕の移動速度を調整**：`erraticSpeedRange`を(3〜11)→(2〜7)に低下（リズムに乗る範囲でやや遅く）
- [ ] 文字の同時被り対策（画面上の同時表示数を制限する案）→ **保留**。速度を落とした後、様子を見てから必要か判断する

## 不具合修正：同じ台詞内で文字が追い越し合う（2026-08-20・実装済み）

- **症状**：蛇行移動は文字ごとに独立したランダム速度・方向で動くため、例えば「すべてリセットしたい」の中で後の文字が前の文字を追い越し、読む順番が崩れて見えることがあった
- **修正内容**：`Assets/AngerBattle/FallingWord.cs`に`leader`（同じ台詞内で直前に出た文字への参照）・`minLeaderGap`（保つ最小間隔）を追加。移動計算後、leaderのx座標＋間隔より先（画面の左方向）に出ようとしたらそこで足止めする
- `Assets/FuanBattle/FuanBattleController.cs`の`RunPhraseSequence`（主軸3台詞）・`RunScatteredFiller`（合いの手）両方に適用（1文字生成するたびに`leader`として直前の文字を渡す）
- **適用範囲の確認（2026-08-20・ユーザー確認済み）**：制約は「同じ台詞内」のみ。主軸台詞と合いの手をまたいだ追い越し（合いの手が主軸を追い越す等）は今まで通り自由（意図的にそのまま）。実装上も主軸用・合いの手用でそれぞれ別のleaderチェーンなので対応済み
- バッチモード確認OK（コンパイルエラーなし、2026-08-20）。実機でのPlay確認は必須

### 追い越し防止の動きの質感を改善（2026-08-20・実装済み）

- **フィードバック**：追い越し防止を入れる前の動きの方が綺麗だった。境界にぶつかった瞬間に硬く止まる（leaderのx座標に張り付いて縦だけ揺れる）動きが不自然に見える、とのこと
- **修正内容**：`FallingWord.cs`に`catchUpSoftZone`（既定1.5）を追加。leaderとの間隔がこの範囲まで縮まったら、即停止ではなく間隔に応じてx方向の前進を滑らかに減速させる（縦方向の揺れは今まで通り自由なまま）。`0`にすると従来の硬い即停止に戻せる
- `FuanBattleController.cs`に`leaderCatchUpSoftZone`を追加し、文字生成時に反映
- バッチモード確認OK（コンパイルエラーなし、2026-08-20）。実機でのPlay確認は必須（前より自然に見えるか、感触を見て`catchUpSoftZone`の値は要調整）

## 不具合修正：時計＋ノイズ演出で、ノイズが時計より先に止まる（2026-08-20・実装済み・バッチ確認待ち）

- **症状**：`ClockGlitchIntro`のノイズ矩形（`RandomizeNoiseBlocks`）が、`blackHoldDuration`の追加ティック区間（画面が完全に黒くなった後も時計を刻み続ける間）で再生成されておらず、そこだけ止まって見えていた
- **修正内容**：`Assets/FuanBattle/ClockGlitchIntro.cs`の`Play()`内、`blackHoldDuration`用の追加ティックループにも`RandomizeNoiseBlocks(noiseBlockCountEnd)`を追加。時計が完全に止まるまでノイズも生成し続けるようにした
- バッチモード確認OK（コンパイルエラーなし、2026-08-20）。実機でのPlay確認は必須

## 次にやること候補

- [x] シーン構築（`FuanBattleRoot`一式・`MinigameLauncher`の参照追加）→ 実施済み。実機確認OK（F1メニューから起動して動作確認済み・2026-08-19）
- [x] `Anger.yarn` → `Anxiety.yarn`の接続（`<<jump Anxiety>>`をどこに入れるか）→ 対応済み（下記「不具合報告：Good Morning演出後、次のシーンに戻らない」参照）
- [ ] 蛇行移動のパラメータ（`erraticChangeInterval`/`erraticAngleSpread`/`erraticSpeedRange`/`erraticVerticalBounds`）の微調整（動きは確認できたので、感触は追って調整）

## 服薬直後の「時計＋ノイズ」演出（実装済み・実機確認待ち、2026-08-19）

`Anxiety.yarn`の`Anxiety_TakeMed`ノード末尾コメント（`//時計が 1:42、3:16、4:03 と進む。画面のノイズが増え、精神世界パートに移る`）に対応する、`<<start_minigame "FuanBattle">>`直後・FuanBattle通常フロー（「コンタック: 心の声を震めなくちゃ。」表示）の前に挟む導入演出。

**注（2026-08-31・プロジェクト監査で追記）**：以下のチェックボックスは未チェックのまま残っているが、下記「実装状況まとめ（2026-08-19）」の通り実装済み。この直後の3段階フリッカー版は、さらにその後「修正依頼（大幅に方向転換）」で連続カチカチ版に置き換わっている。

- [x] **ノイズ表現**：静止画のグリッチ風UIオーバーレイ。自作方針に合わせ、ランダムなブロック/走査線をUI上にC#で描画する軽量表現（既製アセット・シェーダー不使用）
- [x] **時計表示**：デジタル時計風のUIテキストが表示される（3段階非連続版は後の修正依頼で連続カチカチ版に置き換え済み）
- [x] **組み込み方法**：`MinigameLauncher`に新しい演出ステップとして追加（`RunBattle()`等の起動フローに、FuanBattle通常フローの前段として挿入）
- [x] **進行方法**：自動再生（プレイヤー操作なし）。演出終了後は自動的に不安戦本編（コンタックの一言）へ繋がる
- [x] 具体的なパラメータは実装時に調整済み
- [x] **時刻の値について（2026-08-19追記）**：目安値のまま実装（正確に1:42/3:16/4:03である必要はない、という方針通り）
- [x] **切り替わり方（2026-08-19追記）**：後の修正依頼で「等速で連続的にカチカチ」に置き換え済み
- [x] **音（2026-08-19追記）**：時計の音（カチカチというSE）をこの演出中に流したい。BGMと別のSEとして再生する想定
  - **素材確保済み（2026-08-19追記）**：`Assets/Audio/ClockSound.mp3`としてユーザーが配置。これを時計の演出中に流すSEとして使う（`.meta`未生成＝Unity未インポートのため、実機で一度開いた際に自動生成される想定）

### 実装内容（2026-08-19）

- `Assets/FuanBattle/ClockGlitchIntro.cs`（新規）：時計＋ノイズ演出本体
  - `displayTimes`（既定：1:42／3:16／4:03、あくまで目安値・Inspectorで変更可）の各段階の前に、ランダムな時刻へ`flickerCountPerStage`回（既定3回）フリッカーしてから実際の目安時刻に着地する「カチカチカチッ」という切り替わり方
  - フリッカー・着地のたびに`tickAudioSource.PlayOneShot(tickClip)`で時計の音（`ClockSound.mp3`）を鳴らす
  - `noiseBlockCountPerStage`（既定：2→5→9）で段階が進むごとにグリッチ矩形（`noiseBlocks`、UI Imageのランダム矩形10個）の同時表示数を増やし、ノイズが増えていく見た目を表現
  - 自動再生（プレイヤー操作なし）。`Play()`はIEnumeratorで、終了まで呼び出し元をブロックする
- `Assets/AngerBattle/MinigameLauncher.cs`：`RunBattle`に任意の`preStartIntro`フック（`Func<Task>`）を追加し、`RunFuanBattle`からのみ`clockGlitchIntro`演出を挟むようにした（怒り戦側は影響なし）。`clockGlitchIntro`フィールドを追加（未設定なら演出をスキップ）
- シーン（`SampleScene.unity`）：`FuanBattleRoot`の子として`ClockGlitchIntro`一式（専用Canvas＋CanvasScaler＋AudioSource＋`ClockGlitchIntro`スクリプト、中央の`ClockText`（TMP、フォントサイズ140・太字・中央寄せ）、`NoiseBlock0`〜`NoiseBlock9`のグリッチ用矩形10個）を新規追加。`MinigameLauncher`の`clockGlitchIntro`参照も設定済み
- `Assets/Audio/ClockSound.mp3.meta`（新規、手書き・最小構成）：Unity未インポート状態でもシーンからguid参照できるように、スクリプトと同じ手法（guidのみ書いたmetaを先に用意）で対応。実機で開いた際にAudioImporterの詳細設定が自動生成される想定
- **注意**：今回もFuanBattleRoot配下にGameObject一式を新規追加する規模のシーンYAML手編集。バッチモード検証OK（コンパイル・シーンインポートともにエラーなし、2026-08-19）

### 修正依頼（2026-08-19、実機フィードバックより・大幅に方向転換・未実装）

前回の「3段階（1:42→3:16→4:03）でそれぞれフリッカーしてから着地」という作りそのものを見直す。「1:42／3:16／4:03」は3つの固定スナップショットではなく、あくまで目安・一例。時計なので本来もっと多くの数字を連続的に通過するはず、という指摘。

- [x] **時計は3つの固定値で止まらず、連続的にカチカチと時を刻み続ける**（0:00頃の就寝目安から、朝方（4時台ぐらい）まで、前にしか進まない時間経過として多数の時刻を次々と通過していく。1:42/3:16/4:03のような値はその通過点の一例に過ぎない）
- [x] **緩急（2026-08-19修正）**：ゆっくり始まって加速、ではなく**最初から一定の速いテンポ（パパパパパパ）で等速にカチカチ刻み続ける**
- [x] **デジタル時計の文字が、演出全体を通じて途切れずだんだん拡大され、手前に迫ってくる**（3段階でジャンプ的に大きくなるのではなく、連続的なスケールアップ）
- [x] **コンタックの姿・セリフは出さない**：この演出中はキャラクター表示なし、時計とノイズのみ
- [x] **尺**：現在の実装は短すぎる。全体の長さを今の実装の**約3倍**にする（下記「実装状況まとめ」で演出全体6秒として実装済み）
- [x] 時計の音（`ClockSound.mp3`）は、この連続的な刻みに合わせて鳴らし続ける想定（詳細は実装時に調整）

### 【重要】スコープ変更：不安戦専用ではなく「精神世界パート入場」共通演出（実装済み・2026-08-19）

この時計＋ノイズ演出は**不安戦（FuanBattle）専用ではない**。怒り戦（IkariBattle）も含め、**精神世界パートに入るたびに毎回使う共通の導入演出**という位置づけ。

- [x] `ClockGlitchIntro`を`FuanBattleRoot`配下から、シーン直下（怒り戦・不安戦どちらからも参照できる共通の場所）に置き直した
- [x] `MinigameLauncher.cs`の`RunAngerBattle`にも`PlayClockGlitchIntroIfPresent`フックを渡すように変更（怒り戦側も同じ演出を通る）
- [ ] 時刻の範囲（startTime/endTime）は不安戦の文脈（`Anxiety.yarn`のコメント由来、0:00→4:03）由来の値のまま、怒り戦側も共用。怒り戦用に別の値にするかは保留（据え置きで進めた）

### 背景・ブラックアウト演出（実装済み・2026-08-19）

- [x] **背景パネルを追加**：`BackgroundPanel`（黒・初期アルファ0）を`ClockGlitchIntro`の一番手前の子として新規追加
- [x] **段々ブラックアウトしながら時計が前面に迫る**：`backgroundStartAlpha`(0)→`backgroundEndAlpha`(1)を演出全体の進行に合わせてLerp。時計はズームインしながら手前に出てくる。演出終了時に画面は真っ暗になっており、その状態で`gameObject.SetActive(false)`となり本編へ一瞬で切り替わる
- [x] **色**：白・モノクロのまま（紫は使わず）

### 時計の文字の動き（実装済み・2026-08-19）

- [x] スケール拡大（1→3倍）に加えて、`jitterAmount`（既定6px）による**ガタツキ・ジッター**を毎ティック追加

### 実装状況まとめ（2026-08-19）

`ClockGlitchIntro.cs`を全面改訂し、以下の共通演出として実装：
- 0:00→4:03を一定テンポ（0.12秒間隔）で連続的に刻み続ける（3固定値ではない）
- 時計文字は演出全体（6秒）を通じてズームイン（1→3倍）＋ジッター
- 背景は同時に黒くブラックアウト（0→1）
- ノイズ矩形も1→10個へ段階的に増加
- コンタックの姿・セリフは出さない
- 怒り戦・不安戦どちらの精神世界パート入場でも共通で使う（`MinigameLauncher.RunAngerBattle`/`RunFuanBattle`両方から呼ばれる）
- バッチモード検証OK（コンパイル・シーンインポートともにエラーなし、2026-08-19）
- **実機でのPlay確認は必須**（F1メニューから「IkariBattle」「FuanBattle」それぞれ単体起動して、両方でこの演出が挟まるか確認してほしい）

### 実機フィードバック（2026-08-19）：演出開始直後、背後が透けて見える問題

「すごくいい」との評価をもらいつつ、演出開始直後（`BackgroundPanel`のアルファがまだ0に近い序盤）は背後のゲーム画面（カメラの濃い青の背景色、プレイヤーの丸スプライトなど）が透けて見えてしまう、との報告あり（実機スクリーンショットで確認：時刻表示「1:08」の時点で、背景は濃い青のまま・青い丸スプライトと白い矩形が見えている状態）

- [ ] **保留・後日実装**：単色の黒フェードではなく、**専用の背景イラスト**を用意して、演出開始と同時に最初から表示しておく方向で確定。ただし**背景イラスト素材がまだ用意できていない**ため、素材が揃うまでは対応を保留（ユーザー了承済み：「とりあえず黙っておく」「保留でいい」）
  - ※イメージ方向性の相談は下記「ミニゲーム背景」の話と統合（[怒り戦_打ち合わせメモ.md](怒り戦_打ち合わせメモ.md)「背景・立ち絵まわりの今後の予定」参照）。この演出専用の背景をどうするかは別途未定
- [ ] このF1デバッグ起動固有の問題かどうかは未確認（ユーザー：「まだテストしないので分からない、保留でいい」）。通常フロー（Yarnの会話からの遷移）での確認も後回し

### 改善案：完全ブラックアウト後、1〜2秒ほど黒いまま時計を刻ませ続ける（実装済み・2026-08-19）

- [x] `blackHoldDuration`（既定1.5秒）フィールドを追加。背景アルファが1（完全に黒）に達した後も、この秒数のあいだ`tickInterval`と同じテンポで時計を刻み続ける（数字はendTimeを超えてさらに前進、ジッターとカチカチ音も継続）。その後で本編（戦闘）へ切り替わる。バッチ検証OK

## 「Good Morning」演出（撃破後・目覚めのシーン、新規・未実装、2026-08-19）

精神世界パート（怒り戦・不安戦どちらも共通）で敵を撃破した後に、場面転換の合図として表示する演出。

**経緯の補足（紆余曲折あり、これが最終結論）**：
1. 最初「撃破後にGood Nightを表示（安らかに眠りにつく合図）」という話が出た
2. 「撃破後に目覚める描写」という話も一瞬出たが、「精神世界＝就寝中の話なので、撃破直後に目覚めるのはおかしい」と一度Good Nightに差し戻した
3. 最終的に「やはり目覚めるシーン」に戻り、暗転（Good Night）ではなく**ホワイトアウト**（まぶしい光で目覚める）の方が合う、という結論になった
4. 文言は時計演出の英語表記と対になる形で**"Good Morning"**に確定

- [x] **適用範囲**：怒り戦・不安戦どちらも共通（撃破後に必ず出す）
- [x] **内容**：撃破後、目覚めのシーンとして**ホワイトアウト**（白フェード）＋**"Good Morning"**の文字表示
- [x] **音**：`Assets/Audio/Bell_Accent03-1(Dry).mp3`（ユーザーが用意・インポート済み）
- [x] **雰囲気**：静か・穏やか（ジッターなし、ゆっくりフェードイン）
- [x] **文字色**：淺いグレー（{r:0.75, g:0.75, b:0.75}）
- [x] **終わり方**：白から徐々に透明になり、会話画面へ溶け込むように戻る（時計演出の「真っ暗のまま一瞬でカット」とは対照的）

### 実装内容（2026-08-19）

- `Assets/FuanBattle/GoodMorningOutro.cs`（新規）：`backgroundPanel`（白）と`label`（淺いグレー"Good Morning"）のアルファを0→1（フェードイン、`fadeInDuration`=1.2秒、開始と同時にベル音`chimeClip`を再生）→`holdDuration`（1.5秒）静止→`onReveal`コールバックを呼ぶ（この瞬間は画面が真っ白なので、背後のバトルルート非表示・会話UI復帰の切り替えが見えない）→1→0（フェードアウト、`fadeOutDuration`=1.5秒、会話画面へ溶け込む）
- `Assets/AngerBattle/MinigameLauncher.cs`：`RunBattle`に`postBattleOutro`（`Func<Action, Task>`）フックを追加。敵撃破時は`OnEnemyDefeated`が呼ばれ、`postBattleOutro`に「バトルルート非表示＋会話UI復帰」の`Reveal`ローカル関数を渡す。`Reveal`は演出の白フェード最中（`onReveal`呼び出しタイミング）に実行されるため、フェードアウトで自然に会話画面が見えてくる。怒り戦・不安戦どちらも同じ`goodMorningOutro`参照を使う
- シーン：`GoodMorningOutro`一式（専用Canvas・白`BackgroundPanel`・"Good Morning"の`Label`・ベル音用`AudioSource`）をシーン直下（`ClockGlitchIntro`と同じ階層）に新規追加。音は`Assets/Audio/Bell_Accent03-1(Dry).mp3`（ユーザー用意・インポート済み）
- `MinigameLauncher`の`goodMorningOutro`参照も設定済み
- バッチモード検証OK（コンパイル・シーンインポートともにエラーなし、2026-08-19）
- **実機でのPlay確認は必須**

### 実機フィードバック（2026-08-19、未実装）

- [x] **攻撃命中からGood Morning演出開始までに間を置く（実装済み）**：`MinigameLauncher.cs`に`postDefeatPauseSeconds`（既定1.0秒）フィールドを追加。`OnEnemyDefeated`で`postBattleOutro`を呼ぶ前に`Task.Delay`でこの秒数待つ。怒り戦・不安戦共通
- [ ] **`blackHoldDuration`を短く**：現状1.5秒→**1.2秒**に変更（完全に黒くなった後、時計を刻み続けながら黒いままにする時間を短縮）
  - **訂正（2026-08-19）**：やはり変更キャンセル。現状の実装値（1.5秒）のまま据え置きでよい

## ClockGlitchIntroとGood Morningを1つの機能に統合する案（2026-08-22・実装済み）

**注記**：この件は実は[怒り戦_打ち合わせメモ.md](怒り戦_打ち合わせメモ.md)「実装待ち：Good Morning演出の3つの不具合」に既に詳しく方針確定済みだった（見落として二度手間になった）。実装内容・詳細はそちらを参照。以下は不安戦側の会話ログとしてそのまま残す。

- **経緯**：現状は「入場時の時計＋ノイズ演出（`ClockGlitchIntro`）」と「撃破後のGood Morning演出（`GoodMorningOutro`：白フェード+"Good Morning"）」が別々の演出として繋がりなく存在している。これを1つに繋げたい、という提案
- **確定したイメージ**：撃破後、いきなり白フェード+"Good Morning"に入るのではなく、まず`ClockGlitchIntro`と同様の「時計がカチカチ刻む」演出を再度表示し、時刻が目標時刻（5:30想定、要確認）に到達した瞬間に、自動的にGood Morning（白フェード+"Good Morning"）へ切り替わる、という1本の流れにする
- **目標時刻（確定・2026-08-22）**：「5:30までの間に止めろ」＝5:30が上限。時計を刻ませ続け、**5:30に達する前に**止めてGood Morningへ切り替える（現状の`ClockGlitchIntro`は`blackHoldDuration`の余分な刻みで放っておくと6:30近くまで進んでしまうことがあり、これはNG）
- **音・ノイズ（訂正・確定・2026-08-22）**：「音とノイズはいらない」は**Good Morning本体（文字表示部分）**の話であり、撃破後に再度刻ませる**時計部分には適用しない**。時計部分は入場時の`ClockGlitchIntro`と同じく**ノイズ矩形・`ClockSound.mp3`のカチカチ音あり**でよい（訂正：「音、ノイズ有でいい」）。Good Morningの文字表示・保持中は今まで通り音・ノイズなしの静かなまま
- **背景色（確定・2026-08-22）**：`ClockGlitchIntro`は背景パネルを黒へフェード（`backgroundStartAlpha`0→`backgroundEndAlpha`1）していたが、撃破後バージョンは**黒ではなく白**へフェードする（目覚め＝ホワイトアウトのため）
- **GoodMorningOutro側の独自フェードインは廃止（確定・2026-08-22）**：時計が刻む間に背景パネルが白へ向けて進行するため、時計が止まった時点で画面は既に真っ白。`GoodMorningOutro.Play()`側の`fadeInDuration`によるフェードインは不要（これが「フェードインがいらない」の意図。現状はここでフェードインする間、裏の会話UI（コンタックの台詞ボックス）が透けて見えてしまう不具合があった）
- **時計演出とGood Morningの継ぎ目（確定・2026-08-22）**：「時計のあととGood Morningのあいだに後ろが見えてしまっている」との指摘。継ぎ目で一瞬でも背景（透明）に戻ると裏側が見切れるため、**同じ白背景パネルを共有し、透明に戻さず途切れなく白いまま繋げる**（時計演出終了時のパネルのアルファ値をそのままGood Morning側の開始アルファ値として引き継ぐ）
- **Good Morning終了時のフェードアウトは維持（確定・2026-08-22）**：「白→徐々に透明→会話画面へ溶け込む」という終わり方自体は変更なし。問題視されていたのはフェードイン側・継ぎ目側であり、フェードアウト（コンタックへ戻る際の溶け込み）はそのまま
- **未着手**：仕様は固まったが、実装の指示はまだ受けていない

## Good Morning演出のさらなる改善（2026-08-19・一部未実装）

- [x] **演出全体を長く**：撤回。`holdDuration`は1.5秒のまま変更なし
- [x] **文字色**：淺いグレーのままで問題なし（見えにくいという懸念は撤回）
- [x] **「天国っぽさ」の演出**：撤回。光の粒パーティクルは実装しないことになった（フォントの変更のみで対応）
- [x] **フォント**：既存のYuGothicUI SDFではなく、"Good Morning"部分に**天国っぽい専用フォント**を使う
  - 候補としてCinzel（荘厳・神々しいセリフ体）とItaliana（繊細で軽やかなセリフ体）を提示し、**Italiana**に決定
  - `Assets/Fonts/Italiana-Regular.ttf`をGoogle Fonts（SIL Open Font License）からダウンロード・配置済み（`Italiana-OFL.txt`同梱）
  - **完了（2026-08-22）**：ユーザーがUnityエディタの「Font Asset Creator」でSDF変換を実施済み。`SampleScene.unity`の「Good Morning」ラベル（`GoodMorningOutro`配下の`Label`）の`m_fontAsset`/`m_sharedMaterial`をItaliana SDF（guid `37a704f30b28ecc499e94cab4f77257e`）に差し替えた。バッチモード確認OK（コンパイル・シーンインポートともにエラーなし、2026-08-22）
  - **実機でのPlay確認は必須**：Good Morning演出でItalianaフォントが正しく表示されるか
- [ ] **時計の音が小さすぎて聞こえない（2026-08-19追記）**：現状`AudioSource.m_Volume`は0.6に設定済みだが、実機で聞こると全然聞こえないとのこと。元ファイル（`ClockSound.mp3`）自体の収録音量が小さい可能性がある
  - **対応方針（2026-08-19追記）**：ユーザー側で元ファイルの音量を上げてから`Assets/Audio/ClockSound.mp3`を上書き差し替え済み（2026-08-19 22:44更新確認）。実装時にAudioSourceの音量も改めて確認する

## 不具合報告：Good Morning演出後、次のシーン（会話）に戻らない（2026-08-20・未対応）

- **症状**：不安戦（おそらく怒り戦も共通コードのため対象）で敵を撃破→「Good Morning」演出が表示された後、現実パートの会話に戻らない
- **コードを追った限りの流れ（`Assets/AngerBattle/MinigameLauncher.cs`）**：
  1. `OnEnemyDefeated()` → `postDefeatPauseSeconds`待ち → `postBattleOutro(Reveal)`（＝`PlayGoodMorningOutroIfPresent`）を呼ぶ
  2. `RunGoodMorningOutroCoroutine` → `GoodMorningOutro.Play(onReveal)`（`Assets/FuanBattle/GoodMorningOutro.cs`）を実行
  3. `Play()`内：白へフェードイン→`holdDuration`静止→`onReveal?.Invoke()`（＝`Reveal()`：バトルルート非表示＋`SetDialogueVisible(true)`）→白から透明へフェードアウト→`gameObject.SetActive(false)`
  4. コルーチン終了で`outroFinishedSource.TrySetResult(true)` → `battleFinishedSource?.TrySetResult(true)` → `[YarnCommand("start_minigame")]`の`await`が完了し、Yarn側の会話が続くはず
  - 静的にコードを読んだ限りでは明確な原因は特定できず（ロジック上は最終的に`battleFinishedSource`が解決されるはずに見える）
- **原因の当たりをつけるために確認したいこと（実機で見てほしい）**：
  - Playモードのコンソールに例外（NullReferenceExceptionなど）が出ていないか（似た事例が以前怒り戦側であった：[怒り戦_打ち合わせメモ.md](怒り戦_打ち合わせメモ.md)の「怒り戦終了直後にNullReferenceExceptionが発生することがある」）
  - 「Good Morning」の白い画面が**完全に消えて透明に戻っているか**、それとも**白いまま止まっている**か（`fadeOutDuration`終了まで到達していない可能性）
  - `goodMorningOutro`・`chimeAudioSource`・`chimeClip`など、`MinigameLauncher`/`GoodMorningOutro`のInspector参照が抜けていないか
- まだ調査・修正には着手していません。上記のいずれか（特にコンソールのエラー有無）が分かると原因を絞り込みやすいです
- **2026-08-20追記**：発生箇所は**怒り戦**（不安戦ではない）と確認。白い画面は完全に透明に戻っており、コンソールにもエラーなし
- **原因判明（2026-08-20・コードのバグではない）**：`Assets/Yarn/Anger.yarn`の`Anger_TakeMed`ノードは`<<start_minigame "IkariBattle">>`が最後の行で、その後に続く台詞・`<<jump>>`が何も書かれていない。そのため怒り戦→Good Morning演出が終わった時点でYarnのノード自体が終了し、「次のシーン（現実パート）に戻る」ための続きの台詞が存在しない。`MinigameLauncher.cs`側のロジックは正常に動作しており、単に**続きの台本が未執筆**なのが原因
  - これは「次にやること候補」の「怒り戦の後に続く現実パート（現実パート2に相当）の内容を検討する」（下記、未着手）とそのまま対応する話

## 精神パート3「逃避（Escape）」との接続（2026-08-20・未実装）

- `Assets/Yarn/Escape.yarn`（`title: Escape`）を新規作成済み（内容はユーザー提供のドラフトそのまま）。まだどこからも`<<jump>>`されていない独立ノード
- **接続順が判明**：怒り（Anger）の後ではなく、**不安（Anxiety/FuanBattle）の後**に逃避（Escape）が続く
  - 現状`Anxiety.yarn`の`Anxiety_TakeMed`は`<<start_minigame "FuanBattle">>`で終わっており、その後の接続がない（`Anger.yarn`にすでに追加した`<<jump Anxiety>>`と同じパターンで、`Anxiety_TakeMed`の末尾に`<<jump Escape>>`を足せば良さそう）
  - **対応済み（2026-08-20）**：`Anxiety_TakeMed`の`<<start_minigame "FuanBattle">>`の後に`<<jump Escape>>`を追加した（バッチモードでのインポート確認OK）。実機（Play mode）での確認待ち

### Escape.yarn 台本の全面改訂（2026-08-22・実装済み・要相談点あり）

ユーザーから新しいEscape台本ドラフト（選択肢2つ・彼女との連絡・「ベッドが宇宙を飛んでいる」という錯覚の独白）を受け取り、ダメ出し編集校正 → 修正の順で対応。

- **誤字修正**：「選択し」→「選択肢」、「繰り替えし」→「繰り返し」
- **選択肢の実装方針（確定・実装済み）**：新ドラフトにあった「返さない＝非活性で選べない」という選択不可演出は、上記「元の企画テキストにあった『選択不可』の演出は今回は見送り」の経緯と重なるため採用せず、代わりに**どちらを選んでも同じ場所へ合流する**（フレーバーテキストだけ変えて、物語は分岐しない）方式に変更。「選んだつもり」だけが残る形で逃避を演出する狙い
- **旧`Escape.yarn`（「なんだか今日は心が軽い」「ちくたくちくたく」等）との統合**：冒頭の「なんだか今日は心が軽い」、内心の声「わかってはいるんだけど」、「ちくたくちくたく」は新ドラフトに残しつつ組み込んだ。旧版にあった「？？？: なんだか疲れた」は新ドラフトの地の文「なんだか疲れた」と重複するため削除
- **BedFlightへの接続（新規・実装済み）**：末尾の「ベッドが宇宙を飛んでいる」という独白から`<<start_minigame "BedFlight">>`を呼ぶようにした。`BedFlight`はすでにC#側実装済み（`MinigameLauncher.cs`の`case "BedFlight":`）だが、Yarn側からの接続はこれが初めて
- ノード構成：`Escape`（仕事選択）→`Escape_Contact`（彼女への連絡選択）→`Escape_Loop`（独白＋`<<start_minigame "BedFlight">>`）の3ノードに分割
- バッチモード確認OK（コンパイル・インポートともにエラーなし、2026-08-22）

### 再改訂：選択肢を「非活性（選べるが押せない）」方式に変更（2026-08-22・実装済み）

ユーザーが台本を再度手直しし、「合流」方式ではなくやはり**非活性ボタン**（不安戦で一度見送った方式）に戻す指示。以下対応：

- **Yarn側**：`<<if false>>`を各選択肢の非活性側（「やらない」「連絡を返さない」）に付与。Yarn Spinnerの標準機能で、条件がfalseの選択肢はグレーアウト表示・選択不可になる
- **表示に必要なシーン側設定（判明・対応済み）**：Dialogue System prefab（Options List View、`52571f68872914e24837210513edea1d`）の`showUnavailableOptions`は既定で`0`（false）＝`<<if false>>`の選択肢は**非表示**になる設定だった。非活性表示（グレーアウトして見えるが押せない）にするには`1`（true）が必要と判明したため、[Assets/Scenes/SampleScene.unity](Assets/Scenes/SampleScene.unity)にPrefabInstanceの上書き（`showUnavailableOptions: 1`）を追加した
- **本文の誤字・重複修正**：「これが現実何なのか」→「自分が見ている現実が何なのか」、「ぼやけて〜ぼやけても」の重複を解消
- **末尾の強調表現**：Markdownの`**太字**`はYarn/TMPでは効かないため、TMPのリッチテキストタグ`<b>`に変更。ただしYarn Spinnerのコンパイラは`<`/`>`単体を見るとコマンド構文と誤解して警告を出すため、`\<b\>`のようにエスケープして記述（Yarn Spinnerの標準エスケープ構文）。バッチインポートでは警告は出るがエラーにはならず、正常にインポートされることを確認済み
- バッチモード確認OK（コンパイル・シーンインポートともにエラーなし、2026-08-22）
- **実機でのPlay確認は必須**：
  - [ ] 非活性選択肢（「やらない」「連絡を返さない」）がグレーアウト表示され、実際に選べないか
  - [ ] `<b>`タグが太字として表示されるか（TMPのリッチテキスト設定に依存。今回のシーン調査ではrichText明示的に無効化している形跡はなし）
  - [ ] `BedFlight`側の開始演出・終了後の接続先（`<<jump>>`で次のどこに戻るか）が未定
  - [ ] 「彼女」という一人称・二人称表現でよいか（他パートでは「交際相手」という中立表現だった）

### 不具合修正：フォント再生成のたびに、既に移行済みのTMP_Textのマテリアル参照が壊れる（2026-08-22・実装済み）

- **症状**：`AttackLineText`（怒り戦・不安戦・ベッド飛行）、Dialogue Systemの「Last Line」等で、テキストボックスが真っ白のまま何も表示されなくなる。Consoleに「Please assign a Font Asset to this ○○ gameobject.」という警告
- **原因**：`JapaneseFontAssetGenerator.cs`のフォント再生成は、毎回`NotoSansJP SDF.asset`を削除→再作成している。GUIDはメタファイル書き換えで維持しているが、**アセット内部のMaterialサブアセットのfileIDは再生成のたびに変わる**。`ReplaceFontReferencesInOpenScenes()`は「まだ旧フォント（YuGothicUI SDF）のままのTMP_Text」だけを対象にしていたため、**既にNotoSansJP SDFへ移行済みのTMP_Text**（前回以前の実行で直したもの）はスキップされ、フォント自体の参照は生きているのに`m_sharedMaterial`だけ古いfileIDを指したまま壊れて残っていた
- **対応**：`ReplaceFontReferencesInOpenScenes()`の対象条件に「既に`newFont`（今まさに再生成したNotoSansJP SDF）を指しているTMP_Text」も追加。`tmp.font = newFont`を再代入させることで、TMPが現在の正しいMaterialを再解決するようにした（`Italiana`等、意図的に別フォントを使っているテキストは対象外のまま）
- バッチモード確認OK（コンパイルエラーなし、2026-08-22）
- **要対応**：この修正はコード側のみなので、**現在すでに壊れているシーン内の参照はまだ直っていない**。`Tools > Yarn Dialogue > Generate Japanese Font Asset`をもう一度実行してもらう必要がある（修正後のコードで実行すれば、今回は`AttackLineText`等も正しく再修復されるはず）

### 不具合修正：選択肢ボタンの文字が別フォント（LiberationSans SDF）で文字化け（2026-08-22・実装済み）

- **症状**：Escapeの選択肢「たまった家事をやる」の「家」が□（U+25A1）になる。Consoleには`[LiberationSans SDF] font asset`という、`NotoSansJP SDF`ではないフォント名で警告
- **原因**：選択肢ボタン（Option Item）はYarn Spinnerパッケージの`Option Item.prefab`（`dev.yarnspinner.unity`パッケージ内、レジストリ経由で取得しているため直接編集不可）をランタイムに複製して使っており、そのTMP_Textの`m_fontAsset`がTMPのデフォルトフォント（LiberationSans SDF、日本語グリフなし）にハードコードされていた。これは`JapaneseFontAssetGenerator.ReplaceFontReferencesInOpenScenes()`のスキャン対象（開いているシーンのGameObject）に含まれない（実行時にInstantiateされるプレハブのため）ので、フォント再生成では直らない別問題
- **対応**：[Assets/Fonts/Option Item JP.prefab](Assets/Fonts/Option Item JP.prefab)として、パッケージの`Option Item.prefab`の内容を複製した独立プレハブを新規作成し、`m_fontAsset`/`m_sharedMaterial`だけ`NotoSansJP SDF`（guid `793a73a9419767f43b46aa0690b35dc8`）に差し替え。`SampleScene.unity`の`Dialogue System`インスタンスの`optionViewPrefab`フィールドに、このPrefabInstance上書きで新プレハブを参照させた
- バッチモード確認OK（コンパイル・シーン＆プレハブインポートともにエラーなし、2026-08-22）
- **実機でのPlay確認は必須**：選択肢ボタンの文字が正しく表示されるか

### 不具合修正：F1メニューから「Escape」に飛べない（2026-08-22・実装済み）

- **原因**：[怒り戦_打ち合わせメモ.md](怒り戦_打ち合わせメモ.md)の「F1デバッグメニューの改善要望」で`MinigameLauncher.cs`のC#側デフォルト値に`"Escape"`を追加済みだったが、`SampleScene.unity`内の`MinigameLauncher`コンポーネントは非プレハブで、`debugStoryNodes`配列が`[Anger, Anxiety]`のままシーンに個別シリアライズされていた（C#のデフォルト値は新規作成時にしか反映されず、既存シーンの値は自動更新されない）
- **対応**：`SampleScene.unity`の`debugStoryNodes`配列に`Escape`を直接追記した。バッチモード確認OK（2026-08-22）

## 不具合報告：不安戦でも撃破後に敵が消える（2026-08-20・未対応）

- 怒り戦では「撃破後は敵を白くするだけで非表示にしない」に直したが（`Assets/AngerBattle/EnemyAnger.cs`＋`AngerBattleController.cs`）、不安戦側の`Assets/FuanBattle/FuanBattleController.cs`（202〜203行目付近）にはまだ`enemy.OnDefeated -= HandleEnemyDefeated; enemy.SetPresent(false);`が残っており、撃破後に敵が非表示になる
- **対応済み（2026-08-20）**：`FuanBattleController.cs`側も`AngerBattleController.cs`と同様に撃破後の`enemy.SetPresent(false)`を削除した。敵のColor/Sprite差し替えは`EnemyAnger`側で共通実装済みなので、不安戦の敵（`EnemySpriteWhite.png`＋紫ティント）も撃破時に白へ切り替わるはず（`defeatedSprite`は不安戦のEnemyには未設定だが、素材が既に白地なのでColorを白にするだけで正しく白くなる。怒り戦のように別スプライトへの差し替えは不要）。バッチモード確認はUnityエディタが開いていたため未実施（実機で閉じたら確認予定）

### 実機（Unityエディタ）での確認待ち

- [x] F1メニューから「FuanBattle」を単体起動し、シーンが正しく組めているか → OK
- [x] ~~Enemyの紫色が意図通りか~~ → **NG（2026-08-20実機フィードバック）**：「紫っぽくない」との指摘
  - **原因（調査済み・未修正）**：`Enemy`の`SpriteRenderer`は`AngerBattle`と共用の`EnemySprite.png`（赤色スプライト、[Assets/AngerBattle/Sprites/EnemySprite.png](Assets/AngerBattle/Sprites/EnemySprite.png)）をそのまま使い、`m_Color`だけ紫（R0.576, G0.345, B0.827）に変更している（シーン内`SampleScene.unity`のEnemyオブジェクト、`FuanBattleRoot`配下）
  - `SpriteRenderer.Color`はスプライトのピクセル色に**乗算**されるため、赤い下地（R高・G/B低）×紫のティント＝結果はR×R・G×G・B×Bで暗い赤〜こげ茶寄りになり、紫にならない（Bチャンネルも下地が低いため十分に持ち上がらない）
  - **実装済み（2026-08-20）**：`EnemySprite.png`（赤・怒り戦と共用）はそのまま残し、不安戦専用の白色スプライト`Assets/FuanBattle/Sprites/EnemySpriteWhite.png`（白い塗り＋グレーの縁取り）を新規作成。`SampleScene.unity`内のEnemy（`FuanBattleRoot`配下）の`SpriteRenderer.m_Sprite`をこの新スプライトに差し替え済み。白地なので乗算ティント（R0.576, G0.345, B0.827）がそのまま紫として見た目に出るはず
  - **実機確認OK（2026-08-21）**：意図通り紫に見えた
- [ ] BGM（ワスレナグサ）が正しく再生・ループするか
- [ ] 蛇行する弾幕が画面内に収まり、合いの手（紫）と主軸（白）が区別できているか（修正後の再確認）
- [ ] 敵のセリフ2ブロック→コンタックの返し→1拍後に自動発射、の新しい流れが正しく動くか（怒り戦・不安戦とも）
- [ ] 服薬直後の「時計＋ノイズ」演出：3段階の時刻切り替え（カチカチカチッ＋ClockSound.mp3）とグリッチノイズが正しく表示・再生され、演出後に不安戦本編（コンタックの一言）へ自動で繋がるか

## YES／NO質問UI 試作（2026-08-26・実装着手）

- ユーザー確認済みの方針：仮の質問文5問で試作する。どちらを選んでも正解・不正解は示さず、選択結果だけを履歴に残す
- 仮質問：
  1. 「断っても、嫌われない？」
  2. 「返事がないのは、怒っているから？」
  3. 「失敗しても、取り返せる？」
  4. 「このままで、本当に大丈夫？」
  5. 「今まで選んだ答えは、本当に正しかった？」
- 進行に応じて質問パネルの揺れを強くし、現実パートの不安台詞を順に重ねる
- 既存の開始台詞、BGM、敵登場、撃破、ベル音、「心が少し軽くなった。」までの共通フローは維持する
- 不安本体の台詞はシナリオ記載の「わたし、こわいよ。わからないかもしれないもん。できないかもしれないもん。あぶないことは、いやだもん。」を使用する
- シーンYAMLの既存未コミット変更を避けるため、質問UIは既存CanvasとTMPフォントを使って実行時に生成する

### 商業品質を狙う縦切りへの再設計（2026-08-27・実装済み）

- ユーザーから「商業レベル」「ヒット作を狙う最高のデザイン」への明確な実装指示を受け、単純な質問UI試作から看板シーンを狙う演出へ再設計した
- `AnxietyQuestionExperience.cs`を新設し、戦闘進行と質問UIの責務を分離した
- 最初は整然とした画面だが、回答するほど背景色、制御されたドリフト、文字エコー、履歴、不安台詞が段階的に侵食する
- 前問の回答を「さっきは『YES/NO』を選んだ。確信はある？」と正確に引用する。選択内容の改ざん、正誤判定、偽入力は行わない
- 最終回答後は全履歴を見せ、「答えは、返ってこない。」から不安本体登場時の静止へ繋ぐ
- マウス、左右キー、Y/Nキーに対応。GraphicRaycaster/EventSystemが無い場合は実行時に補完する
- `questionMotionScale`を0にすれば動きを止められ、演出時間もInspectorから調整できる
- `FuanBattleQuestionSmokeTest.cs`を追加し、5問・参照・TMPフォント・YES/NOボタン・入力基盤の生成を自動検証対象にした

### 石畳・楕円選択面・雨・選択先へのカメラ侵入（2026-08-28・実装済み／実機調整待ち）

- CLIP STUDIOから書き出した`Assets/Sprites/Floor.png`、`YesGate.png`、`NoGate.png`を質問画面に使用する。画像は現段階では仮素材として扱う
- 物語、5問の質問文、前回答参照、回答履歴、既存の質問ロジックは変更しない
- 既存CanvasがScreen Space Overlayで、標準Particle Systemを質問画面の手前に重ねる必要があるため、背景と左右の選択面はCanvas ImageではなくSpriteRendererで表示する
- YES／NOの文字はTextMeshProで表示し、長方形ボタンの見た目は透明化する。左右の楕円に合わせたクリック判定を使い、楕円の外側は反応させない
- 雨は外部素材を探さず、Unity標準Particle Systemだけで作る。細長い薄青灰〜白の雨粒を画面全体に低負荷で降らせる
- 1〜4問目は、回答した側の選択面を強調して質問UIを消し、Main Cameraをその楕円へ寄せながら拡大し、暗転後にカメラを戻して次の質問へ進む
- 5問目は現在の最終回答履歴を見せた後、回答した側へ同じカメラ侵入を行い、その暗転を既存の敵が撃つ戦闘画面への接続に使う
- `dotnet build Assembly-CSharp-Editor.csproj`は0エラー。Unity 6.3.22f1のバッチモードでSampleScene読込と質問UIスモークテストもPASS
- **実機でのPlay確認は必須**：楕円の端で誤クリックしないか、YES／NOそれぞれ正しい側へカメラが入るか、雨量・文字位置・拡大速度が仮画像に合うか、5問目の暗転から敵登場へ自然に繋がるかを確認して微調整する

#### 心理スリラー方向へのUI洗練（2026-08-28・実装済み／実機調整待ち）

- ユーザー確認：作品の方向性は洗練された心理スリラー。1〜4問目の履歴表示方式は任意、突入時の小さな画面揺れや将来的な音演出も使用可
- 質問文・前回答・進行表示を上部の半透明パネルへ集約し、左右の入口から文字を視覚的に分離した。YES／NO表示も暗い楕円の中央から石畳側の上へ移した
- 1〜4問目の履歴は最新回答だけを細く表示し、5問目で全履歴を展開する。質問と入口が同時に主張しすぎない情報設計にした
- カメラ侵入は一瞬だけ引いてから後半加速する動きへ変更。Orthographic Sizeを`0.9`まで縮め、暗部が画面外まで広がった終盤だけUI暗幕を重ねて完全な黒へ接続する。左右に応じたごく小さな傾きと揺れも追加
- 雨Particle Systemは元実装に存在していたが見えにくい可能性があったため、外部素材を追加せず、発生量`85/秒`・最大`260`、ストレッチ長、斜め落下速度、出入りのアルファフェードを調整した
- C#ビルドは0エラー。Unity 6.3.22f1のバッチモードで、上部質問パネル、深度`0.9`の設定、楕円クリック、雨のストレッチ描画・粒子数・発生量・速度を含むスモークテストがPASS

#### CLIP STUDIO素材を活かした最終UI整理（2026-08-28・実装中）

- 上部の半透明ボックス内は**質問文だけ**を表示する。`QUESTION`、`02/05`、「さっきはYESを選んだ。確信はある？」、白い×印、文字エコーは表示しない。白い×印はユーザーが不要文言を示すため画像上へ描いただけで、演出要素ではない
- YES／NOは水面へ文字を重ねず、左右の水面直下に置く同寸の細い選択プレートで選ぶ。手書き風にはせず、暗い半透明面・1px相当の輪郭・細めの字間で心理スリラー寄りに統一する
- 進行表示は一般的な数字やゲージではなく、`Assets/Sprites/leftfoot.png`と`rightfoot.png`を使った**5歩分の足跡**にする。回答済みは濡れた青灰色、現在位置は淡い青紫で脈動、未到達は低透明度。回答時には現在の一歩を短く強調する
- 選択操作はプレートの矩形領域へ変更する。マウスホバー／キーボード選択ではごく小さく浮き、押下時だけ沈む。水面そのものはカメラ侵入先として残す
- `YesGate.png`／`NoGate.png`は水面外にも低アルファの紫色が残っており、2枚を全画面合成するとFloorが白紫に濁る。専用のアルファ再マップマテリアルで低アルファ背景だけを除去し、水面と柔らかい縁は残す。Floor・両Gate・足跡は無圧縮インポートにする
- カメラが一瞬引く際に黒縁が見える原因は、背景が初期画角ぴったりだったこと。メイン画像の構図とユーザー調整値を変えず、背後へ14%大きい安全用Floorを1枚置いて隠す
- ユーザーが実機で調整・保存した`questionDiveDuration = 2.0`、`questionDiveOrthographicSize = 0.7`、雨量`85`は変更しない
- ユーザー導入済みの`Cinematic Weather VFX Bundle`から雨筋用URPマテリアルだけを使用し、巨大なサンプルPrefabは使用しない。既存の画面サイズParticle Systemへ適用し、Unity 6の`Particle Velocity curves must all be in the same mode`をx/y/zのCurve Mode統一で修正する
- `Assets/Audio/雨で濡れた道路を走る.mp3`（約5.4秒）を質問中だけ低音量・2D・ループで再生し、質問終了／中断時に停止する
- 物語、5問の質問文、前回答の内部記録、Q1〜4の突入、Q5の全履歴と戦闘接続は変更しない
- **自動検証済み**：`dotnet build Assembly-CSharp-Editor.csproj --no-restore`は0エラー。Unity 6.3.22f1バッチでSampleScene読込・新規素材参照・質問だけの上部パネル・選択プレート・5歩・安全背景・入口透過補正・Cinematic Weather雨・Velocity Curve Mode・濡れた道路のループ音を検証し、`FUANBATTLE_QUESTION_SMOKETEST_RESULT: PASS`
- **実機Play確認待ち**：プレートと足跡の視認性、雨の見え方と音量、2.0秒のカメラ侵入中に黒縁が消えたか、最終暗転のつながりを確認する

#### 実機フィードバック後のUI再修正（2026-08-28・実装済み／実機確認待ち）

- 直前版は実機で、NO側の水面が見えない、選択プレートが大きすぎる、足跡が縦に重なって「！」のように見える、左下の履歴と右下の思考文が画面を散らかす、雨がほとんど見えない、全体が白っぽい、という問題を確認した
- NO側が消えた原因は、YES／NOのSpriteRendererへ同じ透過補正マテリアルを共有し、その`_MainTex`がYES画像を参照していたため。入口ごとに専用ランタイムマテリアルを作り、それぞれのSpriteテクスチャを明示的に割り当てるよう修正した
- 文字による回答履歴と右下の不安思考文は表示しない。5問目の後にも全履歴画面を追加せず、その質問への回答から直接カメラ侵入へ進む。回答内容はゲーム進行用として内部には保持する
- 進行表示は「未回答の5歩を先に並べる」方式を廃止。画面下中央の小さな両足を出発点にし、回答した時だけ1歩ずつ追加する。YESなら次の足跡が左、NOなら右へ曲がり、5回答分の経路そのものが進行と履歴を兼ねる
- 選択時の順序は**水面の波紋→選んだ方向へ足跡が着地→短い間→カメラ侵入**。プレートのホバーでも水面に弱い波紋を出し、決定時は広がって消える波紋にする
- 水面と、その下の細いYES／NOプレートの両方をクリック可能にした。水面側は透明な楕円判定、プレート側は従来どおり矩形判定。キーボードのY／N・左右キーも維持する
- YES／NOプレートは水面直下で幅・高さを縮小し、半透明度と輪郭を弱め、上端の装飾線を削除した。質問パネルも横幅と高さを少し詰め、質問文を40ptへ上げた
- 全体へ暗い青紫のカラーグレーディングを重ね、元画像の夜らしいコントラストを戻した。雨はユーザー設定の発生量`85`を維持したまま、粒の太さ・アルファ・ストレッチ長だけを上げて視認性を改善した
- ユーザーが保存した`questionDiveDuration = 2.0`、`questionDiveOrthographicSize = 0.7`、雨量`85`は変更していない
- C#コンパイル確認は0エラー。Unity 6.3.22f1バッチで、4入力領域、履歴・思考文の非表示、初期両足＋5歩、YES／NO別マテリアルと別テクスチャ、2本の波紋、雨、音を含む質問UIスモークテストが`PASS`。実機Play確認は必要

#### 足跡経路・足音・雨コントラスト・水面反応の再設計（2026-08-28・実装済み／実機確認待ち）

- 足跡を「1回答＝1歩」「5歩で5問を数える」進行ゲージとして扱う設計を廃止した。足跡は回答方向と進行位置を示す一本の歩行経路として残すが、表示された歩数そのものは質問数・進行度を表さない
- 未回答時は画面下中央に、前版より大きい左右2歩を角度と前後差を付けて表示する。回答時は現在位置からYESなら左、NOなら右へ、距離に応じて2〜8歩を自動生成して斜めに歩く。回答が左右へ変われば、経路も画面内を自然に蛇行する
- 追加の足跡は回答直後に一斉表示せず、短い回答確定時間の中で順に着地させる。最大40歩分を確保しているが、質問数との対応はプレイヤーへ見せない
- `雨で濡れた道路を走る.mp3`は質問中の常時低音量ループをやめ、回答を選んだ瞬間からカメラ侵入が終わるまで再生する。シーン保存値`0.22`は変更せず、歩行中だけ実効音量を最低`0.46`へ上げて聞き取りやすくした
- 水面ホバー時に表示していた丸いLineRenderer輪郭を完全に削除した。プレートと同様に、水面画像全体の明るさが滑らかに変わる反応へ変更し、決定時も円ではなく水面全体の短い明滅と非選択側の減光で応答する
- 雨量`85`を大量に増やす方法は採らず、Cinematic Weatherの明るい雨筋を約72%と、標準アルファブレンドの暗青灰色雨筋を約34%の二層構成にした。明るい雨は暗い水面、暗い雨は明るい石畳で見えるため、総量をほぼ維持したまま背景への同化を抑える
- ユーザー調整値`questionDiveDuration = 2.0`、`questionDiveOrthographicSize = 0.7`、雨量`85`、音量保存値`0.22`、質問文と物語進行は変更していない
- C#ソリューション全体のビルドは0エラー。Unity 6.3.22f1バッチで、初期2歩＋最大40歩の連続経路、丸いLineRendererが0個、明暗2層のParticle System、選択時足音の音量と再生設定を含むスモークテストが`PASS`。実機Play確認は必要

#### 足跡の視認性・再選択・足音の修正（2026-08-28・実装済み／実機確認待ち）

- 実機確認で、足跡の間隔が短く全履歴が同じ濃さだったため、左側で団子状に重なり、移動角度も横倒しに見える問題を確認した
- 足跡を回答ごとに高さの異なる帯へ進ませ、YES／NO間は滑らかなS字軌道でつなぐ。間隔を広げ、角度を前進方向の±48度以内に制限し、手前は大きく奥は小さくする
- 左右へ迷った履歴は削除せず一本の経路として残す。直近の回答経路を最も明るく、一つ前を中間、それ以前を薄くして、現在地と右往左往した履歴を同時に読めるようにする
- 2問目以降もYESの選択反応を確実に再発火させるため、各質問開始時にEventSystemの選択を一度解除してからYESへ戻す。水面ホバー状態は質問間で維持し、質問終了時だけ消去する
- 選択時の走行音を`雨で濡れた道路を走る.mp3`から`アスファルトの上を走る1.mp3`へ差し替える。カメラ時間、侵入サイズ、雨量、保存音量、質問文、物語ロジックは変更しない
- C#ソリューション全体のビルドは0エラー。Unity 6.3.22f1バッチで、`YES→NO→YES→NO→YES`の蛇行経路、回答ごとの奥行き前進、足跡角度±48度以内、足跡の描画順、指定音源の実参照を含むスモークテストが`PASS`。実際の画面で濃さ・間隔・音量の最終確認は必要

## 冒頭の追跡演出と締めの縦構図への変更（2026-08-31・実装済み／実機確認待ち）

- 不安戦開始直後に質問／穴へ入るのではなく、現在の石畳の床を背景に、不安とコンタックが同じ画面内にいる状態から始める
- キャラクターには参考画像の実物を使用する
  - 不安：`参考画像/ChatGPT Image 2026年8月30日 20_39_50.png`（`Assets/Resources/AngerBattle/AnxietyVertical.png`へ取り込み済み）
  - コンタック：`参考画像/contack_3quarter_back_idle_32x64.png`（同一画像が`Assets/Resources/AngerBattle/ContackVertical.png`として導入済み）
- 冒頭は不安を画面上側、コンタックをその少し下へ、互いが近く見える距離で配置する
- 不安が上方向へ逃げ、コンタックが自動で追う。プレイヤー操作ではなく短い自動演出とする
- 二人の上移動に合わせてカメラも縦スクロールし、現在の石畳＋YES／NOの穴がある質問画面を見せ、そのまま既存の質問進行へ接続する
- 背景は新規作成せず、現在の`Floor.png`、`YesGate.png`、`NoGate.png`を使う
- 全ての穴／質問が終了したら、不安とコンタックが再び向き合う。締めは冒頭より近い距離で、不安を上側、コンタックを下側に置く縦構図とする
- 既存の締めにある「コンタックが横方向から不安の前へ移動し、右方向へ撃つ」構図は不要。今回の縦構図に置き換える
- 締めの既存台詞は維持する
  - 不安：「わたし、こわいよ。わからないかもしれないもん。できないかもしれないもん。あぶないことは、いやだもん。」
  - コンタック：「それは異常です。」
- 台詞を読み終えた後、コンタックが上方向へ自動で撃つ
- シーンYAMLの既存未コミット差分を保護するため、立ち絵・追加床・追跡演出は`AnxietyQuestionExperience`が実行時生成する。シーンへの手書き追加は行わない
- 締めでは既存Player／Enemyへ実画像を適用し、画面下／上へ近く配置する。旧来の右からの登場は使わず、弾は二人の位置差から上方向を計算して発射する
- C#全体のビルドは0エラー。Unity 6.3.22f1バッチで実画像のSprite読み込み、追跡用床と二人、質問背景、縦構図用リソースを検証し、`FUANBATTLE_QUESTION_SMOKETEST_RESULT: PASS`
- 実機Playでは、冒頭の二人の距離・2.4秒の追跡速度・スクロールの継ぎ目・締めの距離と弾の命中を確認する

## YES／NOの足跡を道標にする再挑戦ループ（2026-08-31・実装済み／実機確認待ち）

- 現在の、回答に応じて足跡が画面内を右往左往・蛇行する演出は廃止する
- YES側／NO側へ向かう足跡は、それぞれ分かりやすい一直線の経路にする
- 5問を順番に進み、途中で正しくない側を選んだ場合は1問目へ戻る再挑戦形式に変更する
- 足跡は各問でどちらを選んだかを示す道標として、1問目へ戻っても消さずに残す
- 以前と同じ選択肢を再び選んだ場合は新しい足跡を重ねず、既にある同じ経路の上を歩く
- 5問を正しい順路で最後まで抜けたときだけ、締めの不安との再会へ進む
- 仮の正解順は、質問内容に合わせて`YES → NO → YES → NO → YES`とする
  1. YES「断っても、嫌われない？」
  2. NO「返事がないのは、怒っているから？」
  3. YES「失敗しても、取り返せる？」
  4. NO「このままで、本当に大丈夫？」
  5. YES「今まで選んだ答えは、本当に正しかった？」
- 各問×YES／NOを固有の直線経路として記録する。既に通った同じ経路は再生成せず、反対側を試した場合だけ別の4歩を追加する。最大40歩で5問×両側を保持できる
- 正しくない側へ入った場合も通常どおり入口へのカメラ侵入と暗転を行い、その暗転先から1問目へ戻す。足跡の経路データと表示は消さない
- C#全体のビルドは0エラー。Unity 6.3.22f1バッチで、5問分の直線経路、同一路の重複防止、反対側の履歴追加、仮の正解順を検証し、`FUANBATTLE_QUESTION_SMOKETEST_RESULT: PASS`
- 実機Playでは、間違えた時に1問目へ戻ること、足跡が残ること、同じ選択肢で重複しないこと、正解順でのみ締めへ進むことを確認する

## 冒頭・足跡・締めの実機フィードバック反映（2026-08-31・実装済み／実機確認待ち）

- 冒頭と締めの不安／コンタックの位置・表示サイズをInspectorへ外出しし、コードの固定値を直接触らず調整できるようにする
- 冒頭の初期距離は直前版より離す。不安だけが上へ逃げ、コンタックはその場で停止する。カメラが不安を追うため、停止したコンタックは画面下へ抜ける
- 足跡履歴は全問題を同時表示しない。各問題のYES／NO経路は内部に残し、現在表示中の問題に対応する足跡だけを表示する
- 間違った側へ歩いた足跡も、その問題の履歴として残す。同じ経路を再び選んでも足跡は重複生成しない
- 各回答直後に正誤判定し、間違えた瞬間の暗転先から1問目へ戻す。残りの問題を最後まで進めてから戻す方式にはしない
- 最終問題まで正解した場合だけ締めへ進む。質問順はランダム化せず、Inspectorの`Questions`配列順を維持する
- 締めではYES／NOの穴を非表示にし、石畳だけの普通の床で不安とコンタックを向き合わせる
- 既存セリフを読み終えた後、コンタックが不安へ仮の弾を撃ち、命中を仮締めとする
- C#全体のビルドは0エラー。Unity 6.3.22f1バッチで、Inspector調整値、問題別の足跡表示、同一路の重複防止、締めの穴非表示を検証し、`FUANBATTLE_QUESTION_SMOKETEST_RESULT: PASS`
- 実機Playでは、冒頭の距離とスクロール速度、停止したコンタックの抜け方、問題を跨いだ足跡の切り替え、締めの二人の位置と弾の命中を確認する

## 不安だけを先に退場させる冒頭へ修正（2026-08-31・実装済み／実機確認待ち）

- 以前は不安の上移動と床スクロールが同時に始まり、不安をカメラが追っているように見えていた。
- 現行は「不安だけが上へ移動して完全に画面外へ出る → 0.45秒の間 → 床を上へスクロール」の順に分離した。
- コンタックは不安の退場中はその場に止まり、床スクロールが始まってから画面下へ抜ける。
- 冒頭準備時は質問床とYES／NO入口を非表示にし、スクロール開始時に初めて上から入ってくるようにした。
- 不安の画像下端が画面内に残らないよう、立ち絵の表示サイズから完全退場位置を自動計算する。
- Inspectorで`openingChaseDuration`、`openingAfterExitHoldSeconds`、`openingScrollDuration`を個別調整できる。
- 初期値は不安退場1.15秒、間0.45秒、スクロール1.35秒。

## 回答方法を「地上2穴クリック選択」から「落下ステージ最下部のYES/NO」へ変更（2026-09-03・打ち合わせのみ／未実装）

### ユーザー依頼の要旨
- 現在のYES/NO選択構造（正誤判定・問題進行・問いUI・問いデータ）は維持したまま、「穴に落ちる短い落下ゲーム」を挟む形に変更する。
- 地上のYES/NO 2つの穴を、意味を持たない1つの穴に変更する。
- コンタックのクリック移動を廃止し、WASDでコンタックを移動できるようにする。プレイヤーが自分で穴まで移動し、そのまま落ちると次へ進む。
- 穴に落ちると縦長の落下ステージへ遷移。コンタックは自動で下方向へ落下し、プレイヤーは左右移動で障害物を回避する。仮素材（単色・四角形）で可、落下区間は短めでよい。今回は操作感確認が優先。
- 落下ステージ最下部をYES/NOの2方向に分岐させ（左=YES、右=NOなど視認しやすく）、どちらかの出口に入ることでその問いへの回答とする。
- 正解なら既存の正解判定ロジックを使い次の問いへ進み、地上画面へ戻る（中央付近に1つの穴がある状態で再開）。不正解なら既存ルールどおり1問目へ戻し、地上画面から再開する。
- 今回は入れない：ヒント収集、Spaceキーのスロー能力、波動エフェクト、複雑な障害物、完成版アート、新しい文章や問い。
- 既存の問いデータ・問いUI・正解／不正解判定・問題進行管理は可能な限り流用する。

### 調査で判明した、依頼文の前提と現状実装の食い違い
- 依頼文は「現在はコンタックをクリックして穴に入り、入った穴で回答判定する」という前提だが、実際の現行実装（`Assets/FuanBattle/AnxietyQuestionExperience.cs`）はそうではない。現状は、床に置かれたYES/NOの矩形ボタン＋水たまり状のゲート画像（`YesGate`/`NoGate`、楕円クリック領域）を**直接クリック**（またはキーボードY/N・左右キー）して回答を確定し、選んだ側のゲートへ**カメラがズームイン**する演出。プレイヤーキャラ`ContackPlayer`は質問シーケンス中`player.enabled = false`で完全に操作停止しており、クリックでキャラを穴まで歩かせる処理も、穴への物理的な接触判定も存在しない。
- 「穴」＝YesGate/NoGateは見た目だけのSpriteRenderer画像で、コライダーもトリガーも無い。
- 上記を踏まえ、今回の変更は「既存のクリック確定方式を、WASD移動＋穴への接触（トリガー）判定方式に置き換える」実質的な新規実装が必要（クリック方式の"廃止"というより新設）。

### 実装方針の検討ポイント（要ユーザー確認・未確定）
1. 落下ステージの置き場所：プロジェクトは単一シーン方式（`Assets/Scenes/SampleScene.unity`のみ）が現行仕様。落下ステージも新規`.unity`シーンを作らず、`FuanBattleRoot`配下に落下用のコンテナを追加し、カメラ切り替え＋世界の表示/非表示で遷移させる想定。
2. 地上の穴への到達判定：`ContackPlayer`にWASD移動用の`PlayerController`（`Assets/AngerBattle/PlayerController.cs`、矢印/WASD軸移動は既に実装済み・流用可）を有効化し、穴オブジェクトへ2Dトリガーコライダーを追加、`OnTriggerEnter2D`で落下ステージ開始をトリガーする想定。
3. 落下ステージの実装：新規スクリプト（例：`FallStageController`）で自動下降・左右移動・障害物（仮：単色矩形）・最下部YES/NO分岐（トリガー2つ）を実装し、`Choose(answer)`など既存の回答確定APIへ合流させ、正誤判定・進行管理（`AnxietyQuestionExperience.IsCorrectAnswer`・`Play()`コルーチンの`i`カウンタ等）はそのまま使う想定。
4. 既存のYES/NOゲート画像・水たまり演出・足跡演出は今回の「地上の穴」表示からは外れる（穴が1つになり意味を持たなくなるため）。これらの資産・演出コードをどう扱うか（削除／流用停止のみ／将来の別演出に転用）は未確定。

### 現時点のステータス
- 上記4点をユーザーに確認し、実装方針が固まってから着手する（CLAUDE.mdルール1に基づき、方針確認と実装可否の確認は別に行う）。
- ユーザー回答：落下ステージは既存シーン内（`FuanBattleRoot`配下）にコンテナ追加、地上の穴はYesGate画像を転用。この方針で実装開始の許可を取得済み。

### スコープ修正（2026-09-03・実装着手直前）
- 「今回まだ入れないもの」から「Spaceキーのスロー能力」を外し、今回の試作に含める。
- Spaceキーで約1.5秒スローになる仮機能。クールタイムは仮で3秒程度。スロー中も左右移動可能。
- 他のスコープ外（ヒント収集、波動の本格エフェクト、複雑障害物、完成アート、新規文章）は変更なし。

### 実装完了（2026-09-03・Unityバッチモード検証PASS／実機Play未確認）

- 地上の穴を1つに統合し、YesGate画像を転用（NoGate側は将来転用のため残置、常時非表示）。`AnxietyQuestionExperience.cs`内`AnxietyQuestionWorldVisuals.SetGateObjectsActive`で制御。
- 地上のクリック選択（YES／NOボタン・水面クリック領域）を廃止。`ContackPlayer`の既存`PlayerController`（WASD／矢印キー対応済み）を、地上の穴待機中だけ有効化する方式にした（`WaitForHoleEntry`）。移動範囲は質問カメラのビューポートから毎回計算し直す一時的な範囲に上書きし、退出時に元へ戻す。
- 穴への到達判定は物理コライダーではなく、プレイヤー座標と穴のワールド座標との距離判定（半径0.55）。
- 穴に到達すると、既存の暗転演出（`PlayHoleDive`、YES／NOどちらかへの寄りではなく穴そのものへの寄りに変更）を経て、新規`FallStageController`（`Assets/FuanBattle/FallStageController.cs`）による落下ステージへ遷移する。
- 落下ステージは仮素材（単色矩形、SpriteRendererを動的生成）。自動下降＋左右移動、簡易障害物3個（当たっても視覚フラッシュのみでペナルティなし＝今回は複雑な障害物処理を避けるための判断）、最下部でYES（左・青系）／NO（右・紫系）へ視覚的に分岐し、到達時のプレイヤーX位置で回答を確定する。
- Spaceキーで1.5秒間、自動下降速度のみ0.35倍に減速（左右移動速度は変えない）。クールタイムはスロー終了後から3秒。
- 正誤判定（`IsCorrectAnswer`）・問題進行（`i`カウンタ）・不正解時に1問目へ戻す処理は既存ロジックをそのまま流用、変更なし。
- Inspectorの「落下ステージ（試作）」セクション（`FuanBattleController`）で速度・距離・幅・スロー秒数／クールタイム／倍率を調整可能。
- 廃止した旧クリック方式のコード（`CreatePuddleButton`、`EllipseRaycastFilter`、`CreateFootstepProgress`等）は完全に削除。足跡の経路計算ロジック（`AppendFootstepSegment`等）はUIが無くなり実質休眠状態だが、スモークテストが引き続き検証しているため残置。
- `Assets/Editor/FuanBattleQuestionSmokeTest.cs`を新構造に合わせて更新し、Unity 6.3.22f1バッチモードで`FUANBATTLE_QUESTION_SMOKETEST_RESULT: PASS`（コンパイルエラーなし、旧ボタン0個、FallStageController構成済み、Inspector新項目の妥当性を検証）。
- 未確認：実機Play Modeでの操作感（WASD移動、穴への落下、落下中の左右移動、スロー、最下部YES／NO判定、正解／不正解時の遷移）。Unity Editorは今回の作業中は閉じたまま（バッチモードのみ使用）だったため、ユーザーによる実機確認が必要。
