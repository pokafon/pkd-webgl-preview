# 改善点 打ち合わせメモ

> 2026-09-03深夜、「改善点洗い出して」の指示を受けて全Yarnファイル(Prologue/Anger/Anxiety/Escape/Sadness)の
> `<<background>>`/`<<portrait>>`state・使用アセットを一通り監査した結果。
> プロジェクトルール1により、ここに挙げた項目は**まだ実装していない**（明確な実装指示待ち）。

## 確実なバグ・修正済み

- [x] `TrueEnd_Start`シーン1で上司の立ち絵が表示されない → 対応済み（`<<portrait "Boss">>`追加、2026-09-03）

## 要判断（勝手に直さず、確認してから対応）

### 1. `Prologue`/`Ending`ノードに`<<background>>`が一度も無い

[Prologue.yarn](Assets/Yarn/Prologue.yarn)・[Escape.yarn](Assets/Yarn/Escape.yarn)の`Ending`ノードは、上司の昇進報告→彼女のセリフという全く同じ構成のセリフを使っているが、どちらも`<<background>>`を一度も呼んでいない。
- `Prologue`はゲーム開始直後（最初のノード）なので、背景は非表示のまま（登場時のデフォルト状態）
- `Ending`は`Sadness_AfterBattle`（背景`RoomDay`）から遷移してくるため、上司の昇進報告シーンなのに部屋の背景のまま表示される

2つが同じセリフの対構造（オープニング/エンディングの鏡合わせ）なので、**背景なしが意図的な演出（台詞だけを浮かび上がらせる狙い）である可能性もある**。一方、新しく書いた`TrueEnd_Start`のシーン1（同じ上司のセリフパターン）は`<<background "Office">>`を明示している。統一するかどうかは要確認。

### 2. `<<portrait_position>>`が実装済みだが、Yarn側で一度も使われていない

`left`/`center`/`right`を切り替えられるが、現状すべての立ち絵表示はデフォルト位置（left）のまま。2人同時に会話しているように見せたい場面（例：TrueEnd相談パートで隔離した感情との対話）などで使う想定があれば、指示があれば組み込む。

### 3. `<<bgm>>`/`<<se>>`用の音源登録 → 対応済み（2026-09-03）

既存ミニゲームで使っている音源をそのまま`DialogueVisuals`の`bgmClips`/`seClips`に登録した（新規音源は作らず流用、`DialogueVisualsUpgradeTool.cs`に登録テーブルを追加）。

| 種別 | 登録名 | 音源 | 元の用途 |
|---|---|---|---|
| BGM | TrickStyle | Trick_style.mp3 | 怒り戦バトルBGM |
| BGM | Wasurenagusa | ワスレナグサ.mp3 | 不安戦バトルBGM |
| BGM | LakeWaltz | 湖面のワルツ.mp3 | ベッド飛行BGM |
| SE | ClockTick | ClockSound.mp3 | 時計演出 |
| SE | Bell | Bell_Accent03-1(Dry).mp3 | 目覚まし演出 |
| SE | EveningChime | 夕焼け小焼け…チャイム.mp3 | 記憶回想の帰宅チャイム |
| SE | WhipCrack | 鞭を振り回す1.mp3 | 戦闘SE |
| SE | RunWetRoad | 雨で濡れた道路を走る.mp3 | 戦闘SE |
| SE | RunAsphalt | アスファルトの上を走る1.mp3 | 戦闘SE |

登録のみで、**Yarn側でどのシーンにどれを鳴らすかはまだ決めていない**（別途相談）。Play Modeで`<<bgm "TrickStyle">>`が実際に鳴ることは確認済み。

### 4. 記憶回想（子供時代）のプレイヤーに専用ドット絵が無い

[MemoryRecallSceneBuilder.cs:99](Assets/Editor/MemoryRecallSceneBuilder.cs:99)で、悲しみの絵(`Sadness.png`)を子供時代プレイヤーの見た目として仮流用している（前回の会話で既出）。

### 5. 記憶回想の友達3人が全員同じ絵

`Cute_Fantasy_Free`パックの`Player.png`を3人とも使い回している（前回の会話で既出）。3人を描き分けるかは要確認。

### 6. 未使用の残骸ファイル

`Assets/SadnessBattle/Sprites/Friend.png`・`SadnessPlaceholder.png`はシーン内のどこからも参照されていない（前回の会話で既出、削除の許可待ち）。
