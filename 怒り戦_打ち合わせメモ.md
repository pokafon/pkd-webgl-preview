# 怒り戦（旧：憤怒戦）・プロローグ拡張 打ち合わせメモ

> 履歴資料です。現行仕様は[現行仕様.md](現行仕様.md)、物語・台詞は[シナリオ全文_読みやすい版.md](シナリオ全文_読みやすい版.md)を参照してください。下記には廃止済みの旧仕様も残します。

## 呼称について
- 「憤怒」は以後「怒り」と呼ぶ

## 【現状】怒り戦の実装（2026-09-02・[AngerBattleController.cs](Assets/AngerBattle/AngerBattleController.cs)を正として記載）

下記「精神パート1『怒り戦』の仕様（最新版）」節（横スクロール弾幕回避＋一撃攻撃）は**もう実態と一致していない**。以下が実コードの現行仕様。

**形式**：縦型レイアウトのHP制シューティング（プレイヤー＝コンタックが画面下、怒りが画面上）

- **HP**：怒り12／プレイヤー（コンタック）4
- **開始**：コンタックの一言「心の声を鎮めなくちゃ。」→スペースキーでBGM再生・戦闘開始
- **プレイヤー操作**：画面内（カメラ表示範囲に収まるよう自動計算した矩形）を自由移動。スペース**長押し**で青弾を連射（`playerShotCooldown`=0.16秒間隔）
- **怒りの弾幕（フェーズ制、被弾ダメージに応じて自動昇格）**
  - フェーズ0（HP12〜10）：狙い撃ち単発
  - フェーズ1（HP9〜7）：扇状弾（3〜5発、交互）
  - フェーズ2（HP6〜4）：安全地帯が左右に動く弾のカーテン
  - フェーズ3（HP3〜0、最終）：7方向弾と狙い撃ちを繰り返し、怒り本体も左右に動く
- **節目演出**：3ダメージごと（HP9/6/3到達時）にヒットストップ→画面上の弾を消去→`thresholdLines`の台詞（「人から奪うだけのくせに。」「消えてしまえばいいのに。」「すべてリセットしたい。」）を`thresholdLineDuration`秒（1.8秒）自動表示（スペース待ちではない）→次フェーズへ
- **被弾**：プレイヤーは怒りの弾に触れるとHP-1、無敵時間（点滅）あり。HP0で撃破され、リトライ演出（`RunPlayerDefeatAndRetry`：死亡演出→スペースで即座に同じ戦闘をやり直し、開始の一言は2回目以降スキップ）が入る
- **撃破**：怒りのHPが0になると戦闘終了。以後は`MinigameLauncher`側でレベルアップ一言→目覚めの時計演出→Good Morning演出（本メモ下部で詳述）に続く

旧仕様との主な違い：「被弾してもペナルティなし」ではなくHP制になった、弾幕は台詞が画面上を流れるのではなく敵が発射する当たり判定のある弾になった、`enemyLine`/`attackLine`（敵の自己紹介セリフ）は廃止され`thresholdLines`（節目の3台詞）に統合された。台詞3つの文言自体は旧仕様から変わっていない。

## 精神パート1「怒り戦」の仕様（最新版・2026-08-19時点、以後の再設計で置き換え済み）

**形式**：横スクロール弾幕回避＋一撃攻撃（プレイヤーは画面内固定範囲で移動、キャラは横に進まない）

**全体の流れ**
0. 精神世界パートに切り替わった直後、コンタックの一言「心の声を震めなくちゃ」を**現実パートと同じ見た目（背景パネル＋テキスト）**で表示（プレイヤー操作待ち）
   - プレイヤーが**スペースキー**を押すと、そこでBGM再生＋弾幕回避フェーズが始まる（現実パートの会話送りと同じキー。**実装済み**）
1. 台詞1「人から奪うだけのくせに。」が1文字ずつバラバラの高さ・タイミングで右から左へ流れる（弾幕ゲーのイメージ）→ プレイヤー（コンタック）は上下左右に移動して避ける
2. 台詞2「消えてしまえばいいのに」が同様に流れる → 避ける
3. 台詞3「全部気に入らない」が同様に流れる → 避ける
   - 被弾してもペナルティなし・ゲームオーバーなし（避けるのは演出目的）
4. 3つの台詞をすべて避け終えたら、**最後に1回だけ**「怒り」本体が登場
   - 怒りは画面右側からゆっくりスライドして定位置まで登場する（パッと瞬間表示ではない。**実装済み・実機確認待ち**）
     - `EnemyAnger.cs`の`appearFromOffsetX`（登場開始位置の距離）・`appearDuration`（スライドにかける秒数）で調整可能
   - 敵の登場と同時に音楽（BGM）を止める
   - **敵の登場と同時に、コンタック（プレイヤー）が怒りの正面・画面中央へ自動移動する（実装済み）**
   - 登場後、即セリフを出さず**一拍分だけ間を置く**
5. 一拍の間を置いたあと、**怒り自身のセリフ**が**現実パートと同じ見た目**で表示される。スペースキーで読み進める（**実装済み**）
   > 怒り: わたしは怒り。
   > 自分を不当に扱うもの、自分を軽視するもの、自分を脅かすものを拒絶したい。
   - 続けて自動で**コンタックの返し**「それは異常です。」が表示される（プレイヤー操作なし。**実装済み**）
   - （保留）敵が登場していきなり喋るのではなく、もう少し溜めを作るか等は今後調整の余地あり
6. プレイヤーが**スペースキー**を押すと、見た目はシンプルな弾を発射。**プレイヤーの実位置から怒りに向かって弾が飛ぶ（実装済み）**
   - **キー統一（確定・実装済み）**：戦闘中の操作キーはすべてスペースキーに統一（従来の弾発射キー`Enter`も含む）。会話送りと攻撃で操作キーが変わらないようにした
7. 一発ヒットで怒りを撃破し、怒り戦終了

**台詞（3つ、順番固定）**
1. 「人から奪うだけのくせに」（弾幕として流す文字列には句読点を付けない方針のため、余っていた「。」を除去済み・2026-08-19）
2. 「消えてしまえばいいのに」
3. 「全部気に入らない」

**実装環境**：Unity + Yarn Spinner for Unity
**アセット方針**：既製アセットは使わず自作
**シーン構成方針**：別Unity Sceneには分けず、**同じシーン内でミニゲーム一式（AngerBattleのような親オブジェクト）を非アクティブ⇔アクティブで切り替える方式**
**Yarn連携方式**：`<<start_minigame "IkariBattle">>` をasync/awaitで実装し、ミニゲーム終了まで自動待機。終了時にC#側から完了通知を出してYarnの続きへ進む（`<<wait_for_minigame_result>>`のような専用コマンドは不要）
**実装着手方針**：プロトタイプ経由ではなく、最初からある程度の完成形を目指して実装する。実行は高性能なモデルを使って一気に行う予定（現時点ではまだコードを書かない）

**BGMイメージ**
- 曲名：Trick_style
- テンポ：145 BPM
- ループ範囲：始点 0:12.000 ～ 終点 3:43.862
- このテンポ感を基準に、文字の出現タイミングを拍に合わせて設計する想定
  - 1拍 ≒ 0.414秒
  - 半拍（0.207秒）ごとに1文字：かなり忙しい弾幕
  - 1拍（0.414秒）ごとに1文字：適度な密度
  - 2拍（0.828秒）ごとに1文字：ゆったりめ、避けやすい
  - どの間隔を採用するかは実装時に要調整
- **実装指示**：怒り戦（避けフェーズ）が始まったら`Trick_style.mp3`をBGMとして再生する（ループ範囲：0:12.000～3:43.862）。怒り本体が登場したタイミングでBGMを停止する。ファイル自体はUnityプロジェクトの適切な場所（例：`Assets/Audio`など）に配置してAudioClipとして読み込む想定

---

## Anger.yarn（旧Prologue.yarn）の現在の構成

ファイル名・ノード名は共に `Anger` 系列に統一済み。

```
Anger（オフィス、上司との会話、選択：はい直します／今日は帰ります）
  → Anger_Accept / Anger_Refuse
    → Anger_InnerVoice（共通ルート：？？？の声、4つの台詞）
      → Anger_Train（電車シーン、選択：咳払いをする／黙って聞いている）
        → Anger_Train_Cough / Anger_Train_Silent
          → Anger_Train_Voice（？？？の声、続き）
            → Anger_Home（21時08分、帰宅）
              → Anger_MedChoice（選択肢は「薬を飲む」の一択のみ。「今夜は飲まない」ルートは意図的に削除済み）
                → Anger_TakeMed（服薬、「全部気に入らない」の台詞、精神世界への導入コメントあり）
                  → <<start_minigame "IkariBattle">> を呼び、怒り戦へ接続（記述済み）

Anger_IkariBattle_Script（<<jump>>されない、読み物用ドキュメントノード。怒り戦の全セリフの台本）
```

**Yarn化について（確定・実装済み）**：怒り戦中のセリフ（`startLine`/`phrases`/`enemyLine`/`attackLine`）は、実行時は引き続き`AngerBattleController.cs`側の文字列がそのまま使われる（Yarnプロジェクトの文字列テーブルからの動的読み込みはしない）。ただし読み物・台本として`Anger.yarn`内に`Anger_IkariBattle_Script`ノードを追加し、全体を一つのスクリプトとして読めるようにした。**セリフを変更する場合は、C#側とこのノードの両方を直す必要がある**（自動同期はされない）。

---

## 現実パートの対応表

| # | シーン | 内容 | 対応する言葉 | 状態 |
|---|---|---|---|---|
| 1 | オフィス | 上司「今日中に直してもらえる？」とのやり取り | 「人から奪うだけのくせに…。」 | 完成（Anger.yarnに反映済み） |
| 2 | 帰り道の電車 | 混雑した車内、近くで学生グループが楽しそうに笑い声を上げている。他人の悪意ではなく「何気ない幸せそうな様子」にイラつく、という点がポイント | 「消えてしまえばいいのに」 | 完成（Anger.yarnに反映済み） |
| 3 | 帰宅・服薬 | 机の上の薬を飲む。精神世界へ | 「全部気に入らない」 | 完成（Anger.yarnに反映済み。ミニゲーム接続コマンドも追記済み） |

---

## Unity実装まわりの補足メモ（宿題・保留事項）

- Dialogue Runnerの`Start Node`はノード名変更（Prologue→Anger）に合わせて修正済み
- 選択肢が出た際に自動で一番上のボタンにフォーカスを当てる`AutoSelectFirstOption.cs`は**修正済み・解消**
  - 旧版はボタンの「数」で変化を判定していたため、選択肢が1個（Anger_MedChoiceの「薬を飲む」のみ）の場合にフォーカスが当たらない不具合があった
  - 現行版は「一番上のボタンのインスタンス」が前回と違うかで判定するよう修正されており（コード内コメントにも明記）、選択肢が1個でも動作するはず

---

## 要修正（不具合）→ 2026-08-19 実装済み

- [x] **AttackLineText（セリフ表示）がYarnのセリフ表示の見た目になっていない**
  - 本編の会話で使っているYarn Spinner標準の`Dialogue System`内`Line Presenter`は、`Background`（背景パネル）＋`Text`という構成
  - 一方`AttackLineText`は背景パネルなしの白文字`TMP_Text`のみで、見た目が揃っていなかった
  - **実装内容**：`Line Presenter`の`Background`パネル（黒、不透明度83.5%、`AttackLineText`と同じ位置・サイズ）を複製した`AttackLineBackground`をシーンに追加し、テキストの背後に配置。`AngerBattleController`に`lineBackground`フィールドを追加して連動表示するようにした
    - ※ 当初案は「Yarnの`Dialogue System`本体をそのまま流用」だったが、実行中の現実パートの会話（`<<start_minigame>>`を呼んだ張本人の会話）と同じDialogueRunnerを戦闘中に再利用すると内部状態が競合する懸念があったため、**見た目だけを複製した独立UI**に変更（動作の安全性を優先）
  - `BattleUI`の`RectTransform`の`m_LocalScale`が`{0,0,0}`だった問題も`{1,1,1}`に修正済み
  - 開始演出「心の声を震めなくちゃ」も同じ`attackLineText`／`lineBackground`を使い回して表示

- [x] **話者名の出し方・文字揃えが現実パートと違う（2026-08-19 追加修正）**
  - 実機で見比べたところ、現実パートは話者名（例：「上司」）が本文と別の行・太字で表示され、本文は左上揃え。一方、戦闘中の表示は「コンタック: 本文」が1行にまとまり、しかも中央揃えになっていた
  - **実装内容**：
    1. `AttackLineText`の文字揃えを中央/中央(`m_HorizontalAlignment: 2`/`m_VerticalAlignment: 512`)から左/上(`1`/`256`)に変更（現実パートの`Line Presenter`本文と同じ設定）
    2. 話者名専用の`AttackLineCharacterName`（TMP_Text、フォントサイズ32・太字・左上揃え）をボックス上段に追加し、`AngerBattleController`に`characterNameText`フィールドを追加して連動
    3. `ShowLine()`側で、文字列中の最初の「`: `」を境に話者名と本文を自動分割するようにした（`startLine`/`enemyLine`/`attackLine`の書式「名前: 本文」はそのまま変更不要）
  - ボックス内の上下配分は現状「話者名44px（上）／本文96px（下）」で仮置き。実機で見て気になれば`AttackLineCharacterName`・`AttackLineText`それぞれのRectTransformの高さ・Y位置を微調整してほしい

- [x] **攻撃弾（DenialBullet）がプレイヤーの位置・敵の方向と連動していない**
  - 怒り本体の登場と同時に、コンタック（プレイヤー）が怒りの正面・画面中央（敵とyを揃えた位置）へ自動移動するようにした（`AngerBattleController.MovePlayerToCenter()`、移動中〜移動後はプレイヤー操作を一時停止）
  - 弾の発射位置を、固定の`bulletSpawnPoint`ではなくプレイヤーの実位置から出すように変更（`FireDenialBullet()`）

- [x] **戦闘中の操作キーがEnterとスペースで混在する**
  - `FireDenialBullet`のトリガーを`KeyCode.Return`から`KeyCode.Space`に変更。開始演出・現実パートの会話送りと同じキーに統一した

- [x] **開発用：ミニゲームへのデバッグ直行キーがない**
  - `MinigameLauncher.cs`に`#if UNITY_EDITOR`で囲んだデバッグ機能を追加。**F1キー**（`debugStartBattleKey`で変更可）を押すと、Yarnの会話を進めなくても怒り戦だけを単体で起動できる（戦闘中でない時のみ有効。ビルドには含まれない）

- [x] **怒り戦終了直後にNullReferenceExceptionが発生することがある（2026-08-19 実機で報告・修正）**
  - 実機Playで、怒り戦を撃破して現実パートの会話に戻った直後に以下の例外が発生することがあった：
    ```
    NullReferenceException: Object reference not set to an instance of an object
    TMPro.TextMeshProUGUI.GenerateTextMesh ()
    ...Yarn.Unity.LinePresenter.RunLineAsync...
    ```
  - **原因（推定）**：`MinigameLauncher.OnBattleFinished()`で`dialogueUIRoot`（本編の会話UI）を再アクティブ化した直後・同じフレームでYarnの会話が次のセリフを表示しようとすると、TextMeshProのCanvas参照がまだ再初期化できておらずヌルポになる
  - このバグはコンパイル・シーン読み込みチェック（バッチモード）では検出できない、Play中のタイミング依存の不具合
  - **修正内容**：`OnBattleFinished()`を、`dialogueUIRoot`再アクティブ化後にコルーチンで1フレーム待ってから会話を再開させる形に変更（`ResumeDialogueNextFrame()`）
  - 実機での再現確認待ち

- [x] **攻撃タイミングの変更（2026-08-19、不安戦の実装と合わせて怒り戦にも反映）**
  - 変更前：「それは異常です」表示中にスペースを押すと、同じ押下で即座に弾も発射していて早すぎた
  - 変更後：スペースでセリフを消す → **1拍（`beatsBeforeFire`）待つ** → プレイヤー操作なしで自動的に弾を発射
  - `AngerBattleController.cs`に反映（`不安戦_打ち合わせメモ.md`にも同内容を記載。FuanBattleControllerと共通の変更）

- [x] **弾幕の文字サイズをランダムに大きくする（2026-08-19）**
  - `fontSizeScaleRange`（1.0〜1.6倍）を追加し、`SpawnFallingCharacter`で文字ごとにランダムなサイズ倍率をかけるようにした
  - 狙い：文字サイズがバラバラだと迫力が出て、怒りの荒々しさに合う。大きい文字ほど避ける意識も強まる

---

## 要修正・追加要望 → 2026-08-20 実装済み（実機確認待ち）

- [x] **敵撃破時の演出を「白く色が変わるだけ」にしたい**
  - 要望確定：白くなった後も**非表示にしない**（そのまま残す）
  - 実装内容：`EnemyAnger.TakeDamage()`で撃破時に`SpriteRenderer.color`を`Color.white`にするだけにし、`AngerBattleController.RunBattleSequence()`側で撃破後に呼んでいた`enemy.SetPresent(false)`（非表示化）を削除。次回`SetPresent(true)`で登場する際は元の色（`originalColor`）に戻す
  - 実機確認待ち：敵を倒した後、白いまま画面に残るか

- [x] **うつ玉（DenialBullet）を白い球にしたい**
  - 要望確定：縁取りはグレーくらいで
  - 実装内容：`Assets/AngerBattle/Sprites/BulletSprite.png`を白い塗り＋グレーの縁取りの円に描き直し（`DenialBulletPrefab`は同スプライトを参照しているため差し替え不要）
  - 実機確認待ち：うつ玉が白い球に見えるか

## F1デバッグメニューの改善要望 → 2026-08-20 実装済み

- [x] **「ストーリー開始ノード」リストにEscapeがない**：`debugStoryNodes`に`"Escape"`を追加した
- [x] **「ミニゲーム単体起動」ボタンが戦闘後に何も続かない**：`debugMinigameNextNodes`（`{"Anxiety", "Escape"}`、`debugMinigames`とインデックス対応）を追加し、`RunDebugMinigameThenContinue()`で`StartMinigame()`完了後に対応する次ノードへ`JumpToStoryNode()`するようにした（`IkariBattle`→`Anxiety`、`FuanBattle`→`Escape`）
  - 通常のYarn経由（実プレイ時の`<<start_minigame>>`）は今回すでに`<<jump Anxiety>>`/`<<jump Escape>>`をyarn側に追加済みなので対象外。あくまでデバッグ用の単体起動ボタンだけの話
  - バッチモード確認OK。実機（F1メニューでの動作）確認待ち

## 背景・立ち絵まわりの今後の予定（2026-08-20メモ）

- 現実パート（会話中）には`DialogueVisuals`で背景（Office/Room）・立ち絵（Boss）を追加済み
- [x] ミニゲーム（怒り戦・不安戦の戦闘シーン）側にも背景を追加したい、とのこと → 対応済み。全ミニゲーム共通の`BattleBackground`として`SampleScene.unity`に実装されている
  - **未使用素材あり**：`Assets/Sprites/AngerBattleBackground.jpg`（赤い渦巻く雲）・`Assets/Sprites/FuanBattleBackground.jpg`（紫の渦巻く雲）を試作済みだが、まだシーンには未組み込み
  - **方向転換（2026-08-20相談）**：怒り戦・不安戦（・将来の逃避戦も含め）で色を変えた個別背景ではなく、**全ミニゲーム共通の1枚背景**にする方向で検討中。イメージは「**夜**・**月は見えない**・**雲の上に浮かんでいるような、きれいな感じ**」
  - **再方向転換（2026-08-20追記）**：作り込んだイラストより、**Unityデフォルトのカメラ背景色（青っぽい黒）**が「主張が強くなくて割といい」との評価。ただし完全にデフォルトのままではなく**少し手を加えたい**とのこと
  - **手の加え方（2026-08-20追記・確定）**：**星**を薄くまばらに散らす程度。全ミニゲーム共通（怒り戦・不安戦・将来の逃避戦）
  - **密度・演出（2026-08-20追記・確定）**：星の数は**少なめ**。**瞬き（明滅）はさせず静止したまま**でよい。理由：弾幕として台詞（文字）が画面上を流れてくるゲームなので、星が明滅すると視認性を邪魔する（ユーザー弁）
  - **画像素材（2026-08-20追記）**：星の画像はユーザーが**ナノバナナ（Google Gemini系の画像生成）で用意する**とのこと。Claude側でのプレースホルダー生成（丸ドット画像を書くだけの簡易案）は不要になった。素材が届き次第、単体スプライトを複数ランダム配置するか、背景に散りばめ済みの1枚絵をそのまま使うかを決めて実装
  - 実装方法（パーティクル／ランダム配置したスプライト等の実装手段）は未定・実装時に検討

## キャラクタービジュアルの方向性（2026-08-21相談・未着手）

3体分のビジュアルコンセプトを整理：

- **コンタック（プレイヤー）**：毎回変わらず「作用する」側。感情に対して淡々・機械的に「それは異常です」と否定する立場なので、無機質・薬っぽい幾何学的な形（カプセル・結晶のようなイメージ）
- **怒り・不安（敵、方向性確定済み）**：怒り＝赤くとげとげ、不安＝紫で不安定に歪む
- **逃避（敵、逃避戦の新設計に合わせた方向性）**：弾を撃たず動きも希薄な仕組みに合わせ、輪郭が薄い・色が薄い・輪郭が崩れかけている、という「実体感の薄さ」を表現

素材制作の方針は未定（これまでの背景素材と同様、画像自体はユーザー側でナノバナナ等を使って用意する想定。Blenderでの試作も相談中）。Claude側での対応はUnity組み込み（スプライト差し替え・SpriteRenderer調整等）が中心になる見込み。

### 怒りの敵スプライトをBlender試作品に差し替え（2026-08-21・実装済み）

- Geometry Nodes（棘のインスタンス配置、赤マテリアル）でBlenderで試作 → 正面から平行投影カメラで透過PNGレンダリング → `Assets/Sprites/ikari.png`として保存
- `Assets/AngerBattle/Sprites/EnemySprite.png`（既存の赤い敵スプライト、`SampleScene.unity`内`Enemy`オブジェクトが参照）の中身をこの新しい画像に差し替え（同じファイル・同じGUIDなのでシーン側の参照変更は不要）
- 実機確認待ち：Unity側で見た目・サイズ感が意図通りか

## 実装途中・要実装続行の確認待ち：撃破後の「レベルアップ」演出（2026-08-22）

ユーザー要望：各戦い（怒り戦・不安戦）の撃破後、Good Morning演出の前に、レベルアップみたいな音＋「頭が少し軽くなった」系の文言を入れたい。

**方針確認済み（AskUserQuestionで回答済み）**：
- 音：新規SEは作らず、Good Morningで使っている既存のベル音（`Bell_Accent03-1(Dry).mp3`）を流用
- 文言：Yarn側の既存文言（`Anger.yarn`の「頭が少しすっきりした。」、`Anxiety.yarn`の「心が少し軽くなった。」）を削除し、戦闘内（Good Morning演出の直前）のものに一本化

**実装状況（途中、ここまでコード変更済み）**：
- [x] `GoodMorningOutro.cs`：`PlayChime()`をpublicに変更（ベル音だけ単独で鳴らせるように）
- [x] `AngerBattleController.cs`：`levelUpLine`フィールド（既定「頭が少しすっきりした。」）と`ShowLevelUpLineAndWait()`（public）を追加
- [x] `FuanBattleController.cs`：`levelUpLine`フィールド（既定「心が少し軽くなった。」）と`ShowLevelUpLineAndWait()`（public）を追加
- [ ] **未着手**：`MinigameLauncher.cs`側の配線（`OnEnemyDefeated`に新しいbeatを挟む、`RunAngerBattle`/`RunFuanBattle`から新しいdelegateを渡す）
- [ ] **未着手**：`Anger.yarn`の`Anger_TakeMed`から「頭が少しすっきりした。」を削除
- [ ] **未着手**：`Anxiety.yarn`の`Anxiety_TakeMed`から「心が少し軽くなった。」＋関連コメントを削除
- [ ] **未着手**：バッチモード確認
- [ ] **未着手**：`シナリオ全文_読みやすい版.md`・HTML版の該当箇所更新（Yarn側の文言削除・戦闘内演出に移動したことの反映）

**ここで中断した理由**：設計方針（どの音・どこに文言）は確認したが、「実装を始めていいか」の最終確認をしないまま実装を始めてしまい、ユーザーから指摘があったため中断中。**次回、実装再開の指示があってから続きに着手すること。**

**追加で決まった課題（2026-08-22・未実装・要相談）**：ベルの音をレベルアップ演出側に移すと、`GoodMorningOutro.Play()`が開始時に自前で`PlayChime()`を呼ぶ作りになっているため、**レベルアップのベル→直後にGood Morningのベルで、ベルが2回連続で鳴ってしまう**。Good Morning演出側の作りをどう変えるか要相談：
- 案A：`GoodMorningOutro.Play()`内の`PlayChime()`呼び出しを削除し、ベルはレベルアップ演出の1回だけにする
- 案B：他の案（相談して決める）

### 壁打ちの結果：Good Morning演出を「目覚めの時計」演出に変更（2026-08-22・方針決定・未実装）

ユーザーとの壁打ちで方針決定：

- **就寝時の`ClockGlitchIntro`（0:00→4:03、実際の最終表示は5:01）**：`totalDuration`/`tickInterval`/`blackHoldDuration`等のテンポ・音・ノイズ感は一切変更しない。**`endTime`だけ`4:03`→`2:25`に変更**（計算式は既存のまま、最終表示が正確に`3:00`になるよう逆算した値。過去の実装ログにある通り、実際の最終表示は`endTime`より後ろにずれる仕様のため、この値になる）
- **Good Morningの直前に、同じ「カチカチ」音・ノイズの質感を保ったまま、`3:00`→`5:30`まで進む「目覚めの時計」演出を新規追加**。就寝時の`ClockGlitchIntro`と対になる演出（`3:00`は上記の就寝時演出の最終表示と一致、`5:30`は`Anxiety.yarn`の現実パート開始時刻「05:30 クラリオン自宅」と一致させる意図と思われる）
- **フェード方向（確定）**：A案＝画面がだんだん明るくなる（黒→透明、就寝時の逆方向）に確定
- **ズームの向き（確定）**：就寝時と同じく拡大（数字がだんだん大きくなる）
- **Good Morningの文字表示（確定）**：残す。目覚めの時計演出の後、これまで通り「Good Morning」を表示する
- **ベルの重複問題（確定）**：ベルはレベルアップ演出のときの1回のみ。`GoodMorningOutro.Play()`内の`PlayChime()`呼び出しは削除する（旧・案A採用）

**最終シーケンス（確定）**：
1. 敵撃破 → 一拍置く
2. **レベルアップ演出**：ベル音（`Bell_Accent03-1(Dry).mp3`）＋一言（怒り戦「頭が少しすっきりした。」／不安戦「心が少し軽くなった。」）を戦闘UIで表示、スペースで消す
3. **目覚めの時計演出（新規）**：`3:00`→`5:30`、就寝時と同じテンポ・音・ノイズ・ズーム方向（拡大）。画面は黒→透明へだんだん明るくなる
4. **Good Morning演出（既存、ベル削除）**：白フェードイン→静止→「Good Morning」表示→白から透明へ、会話画面へ溶け込む

**実装タスク一覧（まとめて実装待ち）**：
- [ ] `Anger.yarn`から「頭が少しすっきりした。」を削除、`Anxiety.yarn`から「心が少し軽くなった。」＋関連コメントを削除
- [ ] `MinigameLauncher.cs`：`OnEnemyDefeated`にレベルアップ演出のbeatを挟む配線（`RunAngerBattle`/`RunFuanBattle`から新しいdelegateを渡す。`AngerBattleController.ShowLevelUpLineAndWait()`/`FuanBattleController.ShowLevelUpLineAndWait()`は実装済み）
- [ ] `GoodMorningOutro.Play()`内の`PlayChime()`呼び出しを削除
- [ ] 目覚めの時計演出を新規実装（`ClockGlitchIntro`を流用・複製するか、既存クラスにパラメータを足して両対応にするかは実装時に判断。就寝時のフェード方向とは逆＝黒→透明にする必要がある点に注意）
- [ ] `ClockGlitchIntro`（就寝時）の`endTime`を`4:03`→`2:25`に変更
- [ ] シーンに目覚めの時計演出一式（Canvas・時計テキスト・ノイズ矩形等）を新規追加、`MinigameLauncher`から呼ばれるよう配線
- [ ] バッチモード確認
- [ ] `シナリオ全文_読みやすい版.md`・HTML版の該当箇所を更新
- [ ] 実機でのPlay確認は必須（音の重複がないか、フェードの向き、時計の最終表示が3:00になっているか等）

**実装完了（2026-08-22）：**
- [x] `Anger.yarn`から「頭が少しすっきりした。」を削除、`Anxiety.yarn`から「心が少し軽くなった。」＋関連コメントを削除
- [x] `AngerBattleController.cs`/`FuanBattleController.cs`：`levelUpLine`フィールド＋`ShowLevelUpLineAndWait()`（前回実装済み分と合わせて確認）
- [x] `MinigameLauncher.cs`：`OnEnemyDefeated`にレベルアップ演出→目覚めの時計演出→Good Morning演出の順で挟む配線。`PlayAngerLevelUpBeat()`/`PlayFuanLevelUpBeat()`/`PlayWakeGlitchIntroIfPresent()`を新規追加
- [x] `GoodMorningOutro.Play()`内の`PlayChime()`呼び出しを削除（ベルの重複を解消）
- [x] 目覚めの時計演出：既存`ClockGlitchIntro`をそのまま流用（新規クラスは作らず、`backgroundStartAlpha`/`backgroundEndAlpha`が既にLerpで両方向のフェードに対応済みだったため）。シーンに`WakeGlitchIntro`（`ClockGlitchIntro`と全く同じ構成、fileID`6420000xxx`帯）を新規複製し、`startTime: 3:00`/`endTime: 5:30`/`backgroundStartAlpha: 1`/`backgroundEndAlpha: 0`（就寝時と逆方向のフェード）に設定。テンポ・音・ノイズ・ズームは就寝時と完全に同じ値のまま
- [x] 就寝時の`ClockGlitchIntro`の`endTime`を`4:03`→`2:25`に変更（最終表示が正確に`3:00`になるよう逆算した値）
- [x] `MinigameLauncher`に`wakeGlitchIntro`フィールドを新規追加、シーン側で新しい`WakeGlitchIntro`を参照するよう配線
- [x] バッチモード確認OK（コンパイル・シーンインポート・Yarnインポート、いずれもエラーなし）
- [x] `シナリオ全文_読みやすい版.md`・HTML版を更新（戦闘内演出として統合したことを反映）

**実機でのPlay確認は必須**：
- [ ] 撃破後の流れ（レベルアップ一言→目覚めの時計→Good Morning）が違和感なく繋がるか
- [x] ~~ベルが1回しか鳴らないか~~ → **不具合発見・修正済み（2026-08-22）**：レベルアップ演出のベルが全く鳴っていなかった。原因は`GoodMorningOutro`のGameObjectが普段非アクティブにしてあり、非アクティブな状態で`AudioSource.PlayOneShot()`を呼んでも音が鳴らない（エラーも出ない）仕様だったため。`GoodMorningOutro.PlayChime()`内で先に`gameObject.SetActive(true)`するよう修正。バッチ確認OK。改めて実機確認必要
- [x] 誤字修正（2026-08-22）：開始演出の一言「心の声を震めなくちゃ」→「心の声を鎮めなくちゃ」（震める→鎮める）。`Anger.yarn`・`AngerBattleController.cs`・`FuanBattleController.cs`・`README_セットアップ手順.md`の4箇所を修正。Unityエディタが開いていたためバッチ未検証、文字列リテラルのみの変更で低リスク
- [x] ~~目覚めの時計の最終表示が3:00になっているか、画面がだんだん明るくなる向きになっているか~~ → **実機フィードバックで発見・修正済み（2026-08-22）**：背景パネルの「色」は黒のままで透明度だけ0→1→0（逆方向）にしていたため、透明になった先に見える暗い戦闘背景がそのまま見えるだけで、実際には白くなっていなかった。修正：`WakeGlitchIntro`の背景パネルの色を黒→白に変更し、透明度は標準の向き（0→1、透明→不透明）に戻した（＝だんだん白く塗りつぶされていく）。白背景でも視認できるよう、時計の文字・ノイズ矩形の色も白→黒に変更。バッチ確認OK、改めて実機確認必要
- [ ] 怒り戦・不安戦の両方で同じように動くか（横展開の確認）

## 実装待ち：Good Morning演出の3つの不具合（2026-08-22・実機フィードバックあり・未実装・方針確定・実装許可待ち）

実機で確認したところ、3つの問題が見つかった。方針は確定したが、まだコードは1文字も変更していない。次回セッションで「Good Morningの続きをお願い」と言われたら、以下の方針でまず`AskUserQuestion`等で実装許可を確認してから着手すること（CLAUDE.mdルール1参照）。

1. **目覚めの時計の最終表示が6:00になってしまっている**：狙いは5:30ちょうど。`WakeGlitchIntro`の`endTime`を`5:30`→`5:01`に変更する（前回3:00に合わせたときと同じ「`blackHoldDuration`の追加ティックでendTimeより先に進む」計算式の再逆算。`startTime=3:00`・`totalDuration=6`・`tickInterval=0.12`・`blackHoldDuration=1.5`は変更しない前提で算出）
2. **時計演出終了→Good Morning演出の間に、一瞬暗い戦闘背景が見えてしまう**：`WakeGlitchIntro`（時計演出）が自分でGameObjectを非表示にした直後、`GoodMorningOutro`が透明からフェードインを始めるため、その間に「何も覆っていない瞬間」ができてしまっている。
   - **対応方針**：
     - `ClockGlitchIntro.Play()`に`deactivateOnFinish`引数（既定`true`）を追加し、`false`の場合は最後の`gameObject.SetActive(false)`をスキップする（白いまま残す）
     - `GoodMorningOutro.Play()`に`skipFadeIn`引数（既定`false`）を追加し、`true`の場合はフェードインをせず最初から不透明（白）で開始する
     - `MinigameLauncher`に、時計演出とGood Morning演出を1本のコルーチンにまとめた新メソッド（例：`PlayWakeThenGoodMorningOutro`）を新設し、`postBattleOutro`としてこれを渡す形に変更する：①`wakeGlitchIntro.Play(false)`を待つ→②`goodMorningOutro.Play(onReveal, true)`を待つ→③ここで初めて`wakeGlitchIntro.gameObject.SetActive(false)`（Good Morning側が既に不透明白で覆っているので瞬間的な隙間が生まれない）
     - 現状の`PlayWakeGlitchIntroIfPresent`/`RunWakeGlitchIntroCoroutine`、および`RunBattle`/`OnEnemyDefeated`の`wakeIntro`引数は、この変更で不要になる（`postBattleOutro`側に統合されるため）ので削除する
3. **Good Morningの表示時間を延ばす**：`GoodMorningOutro.holdDuration`を現在の`1.5`から伸ばす（ユーザーからは具体的秒数の指定なし、3秒程度を提案し実装時に確認する想定）

**実装完了（2026-08-22）**：
- [x] `WakeGlitchIntro`の`endTime`を`5:30`→`5:01`に変更（就寝時と同じ計算式で逆算。最終表示が正確に`5:30`になる想定）
- [x] `ClockGlitchIntro.Play()`に`deactivateOnFinish`引数（既定`true`）を追加。`false`なら終了時の`gameObject.SetActive(false)`を省略し、覆った状態のまま残す
- [x] `GoodMorningOutro.Play()`に`skipFadeIn`引数（既定`false`）を追加。`true`なら最初から不透明（白）で開始し、フェードインをスキップ
- [x] `MinigameLauncher.cs`：`PlayWakeGlitchIntroIfPresent`/`RunWakeGlitchIntroCoroutine`と、`RunBattle`/`OnEnemyDefeated`の`wakeIntro`引数を削除。代わりに`PlayWakeThenGoodMorningOutro`（＋`RunWakeThenGoodMorningOutroCoroutine`）を新設し、①`wakeGlitchIntro.Play(deactivateOnFinish: false)`→②`goodMorningOutro.Play(onReveal, skipFadeIn: true)`→③`wakeGlitchIntro.gameObject.SetActive(false)`の順で1本に繋げた（`wakeGlitchIntro`未設定時はGood Morning単体・通常フェードインにフォールバック）。`RunAngerBattle`/`RunFuanBattle`はこれを`postBattleOutro`として渡すよう変更
- [x] `GoodMorningOutro.holdDuration`を`1.5`→`3`に変更（表示時間を延長。具体的な秒数の指定はなかったため、メモにあった「3秒程度」の提案値を採用。実機で見て要調整なら気軽に伝えてください）
- [x] バッチモード確認OK（コンパイルエラーなし、2026-08-22）
- **実機でのPlay確認は必須**：撃破後「レベルアップ一言→目覚めの時計（音・ノイズあり、5:30手前で止まる）→継ぎ目なくGood Morning（白のまま繋がりフェードインなし）→保持3秒→フェードアウトで会話画面（コンタック）へ」の流れが違和感なく繋がるか

### 実機フィードバック（2026-08-22）：音が鳴りっぱなし／5:30の数字が消えない／Good Morningが裏に隠れる（修正済み）

- **症状1**：時計のカチカチ音が最後まで鳴りっぱなしになる
  - **原因**：時計演出終了時、`tickAudioSource`を明示的に止めていなかった
  - **対応**：`ClockGlitchIntro.Play()`終了時に`tickAudioSource.Stop()`を追加
- **症状2**：「5:30」の数字表示がそのまま止まって残り続ける
  - **原因**：`deactivateOnFinish: false`で背景パネルを白いまま残すようにした際、数字テキスト（`clockText`）自体を隠す処理がなかった
  - **対応**：`ClockGlitchIntro.Play()`終了時に`clockText.gameObject.SetActive(false)`で数字だけ隠す（背景パネルは白いまま維持）。次回の再生に備え、`Play()`開始時に`clockText.gameObject.SetActive(true)`で再度表示状態に戻す処理も追加（`clockGlitchIntro`/`wakeGlitchIntro`はどちらも怒り戦・不安戦で使い回す共有インスタンスのため、前回の非表示状態が残ると2回目以降に数字が出なくなる不具合を予防）
- **症状3**：「Good Morning」の文字が表面に見えない（時計の白背景の裏に隠れている）
  - **原因**：`WakeGlitchIntro`と`GoodMorningOutro`、両方のCanvasの`m_SortingOrder`が同じ`50`になっており、描画順が意図通りにならず時計側が手前になっていた
  - **対応**：`GoodMorningOutro`のCanvasの`m_SortingOrder`を`50`→`51`に変更し、常にGood Morningが手前に描画されるようにした
- バッチモード確認OK（コンパイルエラーなし、2026-08-22）
- **実機でのPlay確認は必須**：音が時計フェーズ終了と同時に止まるか、数字が消えて白背景だけ残るか、Good Morningの文字がちゃんと手前に見えるか

### 簡略化：時計演出を居残らせず即非表示に戻す（2026-08-22・実装済み）

ユーザー指摘：「継ぎ目対策として時計演出（`WakeGlitchIntro`）を白いまま居残らせておく」仕組みは、そもそも要らないのでは？という指摘。

- **判断**：妥当。継ぎ目バグの本質的な原因は、Good Morning側が`alpha=0`から`fadeInDuration`（1.2秒）かけてフェードインしていたこと（その間、覆うものがなく背景が見えていた）。`skipFadeIn`で「最初から不透明（白）で開始」にしたことで解消済み。現在の実装は時計とGood Morningを同じコルーチン内で直接繋いでいるため、時計が終わった瞬間・同じフレーム内でGood Morningが不透明で始まるはずで、時計側を居残らせておく必要はない
- **対応**：
  - `ClockGlitchIntro.cs`：`deactivateOnFinish`引数・`clockText`の明示的な表示/非表示制御・`tickAudioSource.Stop()`をすべて削除し、元のシンプルな`Play()`（終了時は常に`gameObject.SetActive(false)`）に戻した
  - `MinigameLauncher.cs`：`RunWakeThenGoodMorningOutroCoroutine`を簡略化。`wakeGlitchIntro.Play()`（引数なし）→`goodMorningOutro.Play(onReveal, skipFadeIn: true)`の順に待つだけにし、手動での`wakeGlitchIntro.gameObject.SetActive(false)`呼び出しを削除（`Play()`内で自動的に非表示になるため）
  - `GoodMorningOutro.cs`の`skipFadeIn`引数はそのまま維持（継ぎ目バグの本質的な対策のため必須）
  - Good MorningのCanvasの`m_SortingOrder`（51、時計側は50）はそのまま維持（実害はなく、念のための保険として残す）
- バッチモード確認OK（コンパイルエラーなし、2026-08-22）
- **実機でのPlay確認は必須**：時計終了→Good Morning開始の間に一瞬でも背景が見えないか（理屈上は同一フレームで切り替わるはずだが、実機で要確認。もし隙間が復活したら、また継ぎ目の作り方を見直す必要がある）
- **実機確認OK（2026-08-22）**：継ぎ目・音・数字残留、いずれも問題なし。「Good Morning」が正しく手前に白背景＋文字で表示されることをスクリーンショットで確認

### 微調整：Good Morningの文字色・サイズ（2026-08-22・実装済み）

実機フィードバック：文字色をもう少し濃く、サイズをもう少し大きくして読みやすくしたい。

- **文字色**：`{r:0.75,g:0.75,b:0.75}`→`{r:0.5,g:0.5,b:0.5}`（濃いめのグレーに変更）
- **フォントサイズ**：`90`→`115`
- バッチモード確認OK（コンパイルエラーなし、2026-08-22）
- **実機でのPlay確認は必須**：色の濃さ・サイズ感がちょうど良いか

### 音の入れ替え：レベルアップ音を新規SEに、Good Morningのベルを復活（2026-08-23・実装済み）

ユーザー要望：新しい効果音`鞭を振り回す1.mp3`をレベルアップ演出（「頭が少しすっきりした」等）の音にし、Good Morning演出の音は元の仕様（開始と同時にベル`Bell_Accent03-1(Dry).mp3`を鳴らす）に戻す。

- **対応**：
  - `Assets/Audio/鞭を振り回す1.mp3`は配置済み・`.meta`未生成だったため、`ClockSound.mp3.meta`と同じ手法（guidのみの最小メタファイルを先に用意）で対応（`.meta`のguid: `bdc5d05188554816bfb2506321934e75`）
  - `GoodMorningOutro.cs`：`Play()`冒頭で`PlayChime()`を呼ぶよう復活（ベルが鳴る元の仕様に戻した）。任意のクリップを鳴らせる汎用メソッド`PlaySound(AudioClip clip)`を新設し、`PlayChime()`はその薄いラッパーにした
  - `MinigameLauncher.cs`：新規`levelUpClip`フィールド（`AudioClip`）を追加し、シーン側で`鞭を振り回す1.mp3`を参照するよう配線。`PlayAngerLevelUpBeat`/`PlayFuanLevelUpBeat`内の`goodMorningOutro.PlayChime()`呼び出しを`goodMorningOutro.PlaySound(levelUpClip)`に変更（AudioSourceは`GoodMorningOutro`のものを共用、鳴らすクリップだけ差し替え）
- バッチモード確認OK（コンパイル・鞭を振り回す1.mp3のインポート、いずれもエラーなし、2026-08-23）
- **実機でのPlay確認は必須**：レベルアップ演出で新しい音（鞭）が鳴るか、Good Morning演出開始時にベルが鳴るか（ベルが2回重複しないかも要確認）

### プロローグの新規反映（2026-08-23・実装済み）

ユーザー要望：[シナリオ全文_読みやすい版.md](シナリオ全文_読みやすい版.md)の「プロローグ」セクション（未着手・どの`.yarn`にもまだ存在しない新規本文）をYarn側に反映。

- **調査**：`Assets/Yarn/Escape.yarn`のEndingノード（78〜82行目）に、プロローグとほぼ同じ台詞（上司の昇進の話・彼女の同棲の話）が既にあり、意図的な円環構造（エンディングがプロローグのシーンをなぞり返す）と判断。ここで使われている表記慣習（`<<portrait "Boss">>`＝上司の立ち絵あり、`<<portrait "None">>`＝彼女は立ち絵なし。彼女の名前「ルイジ」は台詞の話者ラベルとしては使わず「彼女:」のまま）をそのまま踏襲した
- **対応**：
  - `Assets/Yarn/Prologue.yarn`（新規）：`title: Prologue`ノードを作成し、末尾で`<<jump Anger>>`してAngerに接続
  - `SampleScene.unity`：Dialogue Systemの`startNode`を`Anger`→`Prologue`に変更（ゲーム開始時に最初に呼ばれるノード）
  - `SampleScene.unity`／`MinigameLauncher.cs`：F1デバッグメニューの`debugStoryNodes`に`Prologue`を先頭に追加（シーン側の配列・コード側のデフォルト値、両方）
- バッチモード確認OK（コンパイル・Prologue.yarnのインポート、いずれもエラーなし、2026-08-23）
- **実機でのPlay確認は必須**：ゲーム開始時にプロローグから始まり、最後に`Anger`（会社のシーン）へ自然に繋がるか。彼女の立ち絵は`None`（表示なし）扱いなので、警告等が出ないかも確認

### 不具合修正：`DialogueException: No node named Prologue has been loaded.`（2026-08-23・実装済み）

- **症状**：実機Playで起動直後にエラー。`Prologue`ノードが見つからない
- **原因**：`Assets/Yarn/yarnP.yarnproject`（Yarn Projectのコンパイル済みアセット、`sourceFiles: ["**/*.yarn"]`というワイルドカード指定）が、新規追加された`Prologue.yarn`を検知して再コンパイルされていなかった。Unityのアセット依存関係は「既存ファイルの中身の変更」は検知するが、「ワイルドカードに一致する新規ファイルの追加」だけでは自動的に依存元（`.yarnproject`）を再インポート対象にしないことがある
- **対応**：`yarnP.yarnproject`自体に軽微な変更（末尾に改行を追加）を加えて強制的に再インポートさせた。バッチモードで`yarnP.yarnproject`が再インポートされたことを確認（コンパイルエラーなし。`Escape.yarn`68行目の既知の軽微な警告のみ）
- **教訓**：今後、新規`.yarn`ファイルを追加するたびに、`yarnP.yarnproject`側も明示的に触って再インポートを強制する必要がある（バッチ確認で「エラーなし」だけでは、新規ノードが実際にコンパイル済みプログラムに含まれているかまでは保証されないので注意）
- **実機でのPlay確認は必須**：`Prologue`ノードから正常に開始できるか

- [x] **開始演出の追加**：精神世界パート切り替え直後、コンタックの一言「心の声を震めなくちゃ」を表示 → スペースキーで弾幕回避フェーズ開始（`AngerBattleController.RunBattleSequence()`冒頭に実装済み）
- [x] 電車シーン（シーン2）の台詞をYarn Spinner形式で書く
- [x] オフィス→共通ルート（？？？の声）→電車→帰宅の順で通しの台本にする（Anger.yarnに反映済み）
- [x] 怒り戦のゲームデザイン（横スクロール弾幕回避＋一撃攻撃）を固める
- [x] `Anger_TakeMed`から`<<start_minigame "IkariBattle">>`を呼び出す形でYarn側の記述を追加する（Anger.yarnに反映済み）
- [x] 怒り戦の本実装（`Assets/AngerBattle/`配下にスクリプト一式・プレハブ・README作成済み）
- [x] `AutoSelectFirstOption.cs`の「選択肢1個の時にフォーカスが当たらない」不具合の調査・修正（コード上は解消済みと確認。実機での最終確認は未実施）
- [ ] ~~怒り戦の後に続く現実パート（拡張稿でいう「現実パート2」に相当する部分）の内容を検討する~~ → **誤解だった（2026-08-20判明）**：新規に書く内容ではなく、「現実パート2」＝**既存の`Assets/Yarn/Anxiety.yarn`そのもの**（精神パート2「不安戦」につながる現実パート）を指していた
  - **現状の不具合**：`Anger.yarn`の`Anger_TakeMed`ノードが`<<start_minigame "IkariBattle">>`で終わっており、`Anxiety.yarn`側（`title: Anxiety`ノード）へのジャンプが書かれていないため、怒り戦のGood Morning演出後に会話が止まって見える（バグ報告の原因。詳細は`不安戦_打ち合わせメモ.md`の「不具合報告：Good Morning演出後、次のシーンに戻らない」参照）
  - **対応済み（2026-08-20）**：`Anger_TakeMed`の`<<start_minigame "IkariBattle">>`の後に`<<jump Anxiety>>`を追加し、Anxiety.yarnへ接続した（バッチモードでのインポート確認OK）。実機（Play mode）での確認待ち
- [x] 敵登場後、即「それは異常です」を出すのではなく、敵が一言喋ってから表示する演出（`enemyLine`→スペースで送る→`attackLine`、実装済み・実機確認待ち）
- [x] **（本格版デバッグメニュー）** F1単発ショートカットに代えて、画面上にボタン一覧を出す「シーンセレクト的」なデバッグメニューを作る → **対応済み（実装時期不明・2026-08-22にメモの記載漏れを発見）**：`MinigameLauncher.cs`の`OnGUI()`が既にこの形（F1でボタン一覧の表示/非表示を切り替え、ストーリーノード・ミニゲームそれぞれのボタンが並ぶ）で実装されている。チェックの付け忘れだった
  - 理由：今後シーン／ミニゲームが増えていく予定のため、単発キーではなく一覧から選べる形にしておきたい
  - 現状の`MinigameLauncher`の`#if UNITY_EDITOR`のF1ショートカットはそのまま動作している（実機確認OK）ので、本格版に置き換えるまではこのまま使う

### 実機（Unityエディタ）での確認待ち

コード・シーンの修正はUnityのバッチモードでのコンパイル＋シーン読み込みが正常に通ることまでは確認済み。ただし実際にPlayして見た目・触り心地を見る作業は未実施。特に以下を確認してほしい：
- [ ] `AttackLineBackground`の位置・大きさ・不透明度が、現実パートのセリフ欄と違和感なく揃っているか
- [ ] 怒りのセリフ`enemyLine`が2行（「わたしは怒り。」＋改行＋長めの一文）になったため、`AttackLineText`のボックス（高さ140）に収まって見切れていないか
- [ ] 開始演出「心の声を震めなくちゃ」→スペース→戦闘開始、の流れが一回のスペース押下で誤発動しないか
- [ ] 怒り登場時のプレイヤー中央移動が不自然でないか（`moveToCenterDuration`＝0.3秒で調整可）
- [ ] スペースキーで弾がプレイヤー位置から怒りへ正しく飛ぶか
- [x] F1キー（`MinigameLauncher.debugStartBattleKey`）で怒り戦が単体起動できるか（実機確認OK）
- [ ] 怒りの登場スライドイン（`EnemyAnger.appearFromOffsetX`/`appearDuration`）が意図した速さ・距離になっているか（バッチモード未確認。Unityエディタ起動中のため競合しコンパイルチェック未実施）
- [ ] 話者名（`AttackLineCharacterName`）と本文の分割表示・文字揃え（左上）が期待通りか（バッチモード未確認。Unityエディタ起動中のため競合しコンパイルチェック未実施。コードレビューのみで実装）

## 文字接触時の非致死演出（2026-08-26・実装着手）

- ユーザー依頼により、文字へ接触した際の短いプレイヤー点滅と戦闘ルート揺れを追加する
- ダメージ、ペナルティ、ゲームオーバーは追加しない
- 文字1個につき接触通知は1回、戦闘側にもクールダウンを設けて連続発火を防ぐ
- 既存の弾幕、回避、敵登場、自動攻撃、撃破、ベル音、「頭が少しすっきりした。」の流れは維持する

### 商業品質を狙う接触フィードバックへの更新（2026-08-27・実装済み）

- 完全ランダムな画面揺れではなく、Perlinノイズ＋減衰カーブによる短い衝撃へ変更した
- プレイヤーの赤点滅に小さなスケールパンチを組み合わせ、非致死でも接触したことが明確に伝わる
- Collider2Dが双方にある場合はCollider境界、無い場合はRenderer境界を使う。参照は文字生成時にキャッシュする
- 演出中に戦闘終了・再発火しても、戦闘ルート座標、プレイヤー色、拡大率を必ず元へ戻す
- 揺れの強さ・周波数、点滅回数、色、パンチ倍率、クールダウンをInspectorから調整可能にした

## 【重要】ゲーム性が別物へ再設計されている（2026-08-31・プロジェクト監査で判明 → 2026-09-02・解消）

- 上記「精神パート1『怒り戦』の仕様（最新版・2026-08-19時点）」節、および「文字接触時の非致死演出」節は**もう実態と一致していなかった**。[AngerBattleController.cs](Assets/AngerBattle/AngerBattleController.cs)がコミット`c537590`（2026-08-31）で全面書き換えされ、別ゲームになっていた
  - 横スクロール弾幕回避＋一撃攻撃 → **縦型レイアウトのHP制シューティング**（敵12HP／プレイヤー4HP）
  - 台詞3つを避けるだけ → **フェーズ制弾幕**（単発／扇状／カーテン弾）＋3発ごとの節目セリフ（`thresholdLines`）
  - 「被弾してもペナルティなし」 → **被弾でプレイヤーHPが減り、無敵時間あり。撃破されるとリトライ演出（`RunPlayerDefeatAndRetry`）が入る**
  - `enemyLine`/`attackLine`フィールドはコード側で削除済み
- `FallingWord.cs`（蛇行移動・追い越し防止ロジック）は、現行の`AngerBattleController.cs`からは使われなくなっている（旧`AngerBattleSceneBuilder.cs`が構築するプレハブのみが参照。現状動作には無関係）
- **対応済み（2026-09-02）**：ユーザー確認の上、コード側を正としてメモを書き直した。本ファイル冒頭の「【現状】怒り戦の実装」節が現行仕様。ゲーム・コードは変更していない（ドキュメントのみの修正）

## 怒り弾の発射位置調整（2026-09-01、実装）

- 怒りの赤い弾が本体中心より下から出ていたため、怒りスプライトの表示範囲を基準に胸付近から出るよう変更した。
- 発射高さは`enemyBulletOriginHeight`で調整でき、既定値`0.25`は見た目の中心から半身の高さの25%上に置く。
- 単発・扇状弾・カーテン弾の全パターンで同じ高さを使用する。

## Inspector調整項目の拡張（2026-09-02、実装）

- 現行値を維持したまま、`AngerBattleController`へ本体・青弾・赤弾の描画順、青弾の発射オフセット、カメラ取得失敗時の移動範囲を公開した。
- 怒り本体が青弾を受けた時の演出時間、揺れ幅、拡大量、点滅の明るさをInspectorから調整可能にした。
- `AngerBattleHUD`へHPゲージ／ラベルの位置と大きさ、文字サイズ、Canvas描画順、死亡・点滅・脈動演出の時間を公開した。
- `AngerBattleHUD`は値変更時に既存のシーン内HUDへ自動反映し、コンテキストメニューからも再適用できる。

## 撃破直後の台詞追加（2026-09-02、実装）

- シナリオ文書とYarnの`Anger_IkariBattle_Script`参考ノードには「怒り：ぼく、もうイヤなんだ…もう、がまんなんてしたくない。」「コンタック：それは異常です。」の2行があったが、実際のバトル本体（`AngerBattleController.cs`）には未実装だった。
- ユーザー判断：この2行は必要。撃破直後・隔離／消去の選択に入る前のタイミングで実際に表示するよう実装。
- `AngerBattleController.cs`に`defeatExchangeLines`（string配列、Inspectorで編集可）を追加し、`RunBattleSequence`内で`HideLine()`の後・`EmotionResolutionFlow.Choose`の前に、配列の各行を`ShowLineAndWaitForSpace`で順番に表示するようにした。
- Unity Pipeline経由でrecompile確認済み（エラー・警告なし）。実機Play modeでの見た目確認は未実施。
