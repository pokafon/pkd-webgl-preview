# 怒り戦（旧：憤怒戦）・プロローグ拡張 打ち合わせメモ

## 呼称について
- 「憤怒」は以後「怒り」と呼ぶ

## 精神パート1「怒り戦」の仕様（最新版）

**形式**：横スクロール弾幕回避＋一撃攻撃（プレイヤーは画面内固定範囲で移動、キャラは横に進まない）

**全体の流れ**
0. 精神世界パートに切り替わった直後、コンタックの一言「心の声を震めなくちゃ」を**現実パートと同じ見た目（背景パネル＋テキスト）**で表示（プレイヤー操作待ち）
   - プレイヤーが**スペースキー**を押すと、そこでBGM再生＋弾幕回避フェーズが始まる（現実パートの会話送りと同じキー。**実装済み**）
1. 台詞1「人から奪うだけのくせに。」が1文字ずつバラバラの高さ・タイミングで右から左へ流れる（弾幕ゲーのイメージ）→ プレイヤー（コンタック）は上下左右に移動して避ける
2. 台詞2「消えてしまえばいいのに」が同様に流れる → 避ける
3. 台詞3「全部気に入らない」が同様に流れる → 避ける
   - 被弾してもペナルティなし・ゲームオーバーなし（避けるのは演出目的）
4. 3つの台詞をすべて避け終えたら、**最後に1回だけ**「怒り」本体が登場
   - 怒りは画面右側からゆっくりスライドして定位置まで登場する（パッと瞬間表示ではない。**実装済み・実機確認待ち**）
     - `EnemyAnger.cs`の`appearFromOffsetX`（登場開始位置の距離）・`appearDuration`（スライドにかける秒数）で調整可能
   - 敵の登場と同時に音楽（BGM）を止める
   - **敵の登場と同時に、コンタック（プレイヤー）が怒りの正面・画面中央へ自動移動する（実装済み）**
   - 登場後、即セリフを出さず**一拍分だけ間を置く**
5. 一拍の間を置いたあと、**怒り自身のセリフ**が**現実パートと同じ見た目**で表示される。スペースキーで読み進める（**実装済み**）
   > 怒り: わたしは怒り。
   > 自分を不当に扱うもの、自分を軽視するもの、自分を脅かすものを拒絶したい。
   - 続けて自動で**コンタックの返し**「それは異常です。」が表示される（プレイヤー操作なし。**実装済み**）
   - （保留）敵が登場していきなり喋るのではなく、もう少し溜めを作るか等は今後調整の余地あり
6. プレイヤーが**スペースキー**を押すと、見た目はシンプルな弾を発射。**プレイヤーの実位置から怒りに向かって弾が飛ぶ（実装済み）**
   - **キー統一（確定・実装済み）**：戦闘中の操作キーはすべてスペースキーに統一（従来の弾発射キー`Enter`も含む）。会話送りと攻撃で操作キーが変わらないようにした
7. 一発ヒットで怒りを撃破し、怒り戦終了

**台詞（3つ、順番固定）**
1. 「人から奪うだけのくせに」（弾幕として流す文字列には句読点を付けない方針のため、余っていた「。」を除去済み・2026-08-19）
2. 「消えてしまえばいいのに」
3. 「全部気に入らない」

**実装環境**：Unity + Yarn Spinner for Unity
**アセット方針**：既製アセットは使わず自作
**シーン構成方針**：別Unity Sceneには分けず、**同じシーン内でミニゲーム一式（AngerBattleのような親オブジェクト）を非アクティブ⇔アクティブで切り替える方式**
**Yarn連携方式**：`<<start_minigame "IkariBattle">>` をasync/awaitで実装し、ミニゲーム終了まで自動待機。終了時にC#側から完了通知を出してYarnの続きへ進む（`<<wait_for_minigame_result>>`のような専用コマンドは不要）
**実装着手方針**：プロトタイプ経由ではなく、最初からある程度の完成形を目指して実装する。実行は高性能なモデルを使って一気に行う予定（現時点ではまだコードを書かない）

**BGMイメージ**
- 曲名：Trick_style
- テンポ：145 BPM
- ループ範囲：始点 0:12.000 ～ 終点 3:43.862
- このテンポ感を基準に、文字の出現タイミングを拍に合わせて設計する想定
  - 1拍 ≒ 0.414秒
  - 半拍（0.207秒）ごとに1文字：かなり忙しい弾幕
  - 1拍（0.414秒）ごとに1文字：適度な密度
  - 2拍（0.828秒）ごとに1文字：ゆったりめ、避けやすい
  - どの間隔を採用するかは実装時に要調整
- **実装指示**：怒り戦（避けフェーズ）が始まったら`Trick_style.mp3`をBGMとして再生する（ループ範囲：0:12.000～3:43.862）。怒り本体が登場したタイミングでBGMを停止する。ファイル自体はUnityプロジェクトの適切な場所（例：`Assets/Audio`など）に配置してAudioClipとして読み込む想定

---

## Anger.yarn（旧Prologue.yarn）の現在の構成

ファイル名・ノード名は共に `Anger` 系列に統一済み。

```
Anger（オフィス、上司との会話、選択：はい直します／今日は帰ります）
  → Anger_Accept / Anger_Refuse
    → Anger_InnerVoice（共通ルート：？？？の声、4つの台詞）
      → Anger_Train（電車シーン、選択：咳払いをする／黙って聞いている）
        → Anger_Train_Cough / Anger_Train_Silent
          → Anger_Train_Voice（？？？の声、続き）
            → Anger_Home（21時08分、帰宅）
              → Anger_MedChoice（選択肢は「薬を飲む」の一択のみ。「今夜は飲まない」ルートは意図的に削除済み）
                → Anger_TakeMed（服薬、「全部気に入らない」の台詞、精神世界への導入コメントあり）
                  → <<start_minigame "IkariBattle">> を呼び、怒り戦へ接続（記述済み）

Anger_IkariBattle_Script（<<jump>>されない、読み物用ドキュメントノード。怒り戦の全セリフの台本）
```

**Yarn化について（確定・実装済み）**：怒り戦中のセリフ（`startLine`/`phrases`/`enemyLine`/`attackLine`）は、実行時は引き続き`AngerBattleController.cs`側の文字列がそのまま使われる（Yarnプロジェクトの文字列テーブルからの動的読み込みはしない）。ただし読み物・台本として`Anger.yarn`内に`Anger_IkariBattle_Script`ノードを追加し、全体を一つのスクリプトとして読めるようにした。**セリフを変更する場合は、C#側とこのノードの両方を直す必要がある**（自動同期はされない）。

---

## 現実パートの対応表

| # | シーン | 内容 | 対応する言葉 | 状態 |
|---|---|---|---|---|
| 1 | オフィス | 上司「今日中に直してもらえる？」とのやり取り | 「人から奪うだけのくせに…。」 | 完成（Anger.yarnに反映済み） |
| 2 | 帰り道の電車 | 混雑した車内、近くで学生グループが楽しそうに笑い声を上げている。他人の悪意ではなく「何気ない幸せそうな様子」にイラつく、という点がポイント | 「消えてしまえばいいのに」 | 完成（Anger.yarnに反映済み） |
| 3 | 帰宅・服薬 | 机の上の薬を飲む。精神世界へ | 「全部気に入らない」 | 完成（Anger.yarnに反映済み。ミニゲーム接続コマンドも追記済み） |

---

## Unity実装まわりの補足メモ（宿題・保留事項）

- Dialogue Runnerの`Start Node`はノード名変更（Prologue→Anger）に合わせて修正済み
- 選択肢が出た際に自動で一番上のボタンにフォーカスを当てる`AutoSelectFirstOption.cs`は**修正済み・解消**
  - 旧版はボタンの「数」で変化を判定していたため、選択肢が1個（Anger_MedChoiceの「薬を飲む」のみ）の場合にフォーカスが当たらない不具合があった
  - 現行版は「一番上のボタンのインスタンス」が前回と違うかで判定するよう修正されており（コード内コメントにも明記）、選択肢が1個でも動作するはず

---

## 要修正（不具合）→ 2026-08-19 実装済み

- [x] **AttackLineText（セリフ表示）がYarnのセリフ表示の見た目になっていない**
  - 本編の会話で使っているYarn Spinner標準の`Dialogue System`内`Line Presenter`は、`Background`（背景パネル）＋`Text`という構成
  - 一方`AttackLineText`は背景パネルなしの白文字`TMP_Text`のみで、見た目が揃っていなかった
  - **実装内容**：`Line Presenter`の`Background`パネル（黒、不透明度83.5%、`AttackLineText`と同じ位置・サイズ）を複製した`AttackLineBackground`をシーンに追加し、テキストの背後に配置。`AngerBattleController`に`lineBackground`フィールドを追加して連動表示するようにした
    - ※ 当初案は「Yarnの`Dialogue System`本体をそのまま流用」だったが、実行中の現実パートの会話（`<<start_minigame>>`を呼んだ張本人の会話）と同じDialogueRunnerを戦闘中に再利用すると内部状態が競合する懸念があったため、**見た目だけを複製した独立UI**に変更（動作の安全性を優先）
  - `BattleUI`の`RectTransform`の`m_LocalScale`が`{0,0,0}`だった問題も`{1,1,1}`に修正済み
  - 開始演出「心の声を震めなくちゃ」も同じ`attackLineText`／`lineBackground`を使い回して表示

- [x] **話者名の出し方・文字揃えが現実パートと違う（2026-08-19 追加修正）**
  - 実機で見比べたところ、現実パートは話者名（例：「上司」）が本文と別の行・太字で表示され、本文は左上揃え。一方、戦闘中の表示は「コンタック: 本文」が1行にまとまり、しかも中央揃えになっていた
  - **実装内容**：
    1. `AttackLineText`の文字揃えを中央/中央(`m_HorizontalAlignment: 2`/`m_VerticalAlignment: 512`)から左/上(`1`/`256`)に変更（現実パートの`Line Presenter`本文と同じ設定）
    2. 話者名専用の`AttackLineCharacterName`（TMP_Text、フォントサイズ32・太字・左上揃え）をボックス上段に追加し、`AngerBattleController`に`characterNameText`フィールドを追加して連動
    3. `ShowLine()`側で、文字列中の最初の「`: `」を境に話者名と本文を自動分割するようにした（`startLine`/`enemyLine`/`attackLine`の書式「名前: 本文」はそのまま変更不要）
  - ボックス内の上下配分は現状「話者名44px（上）／本文96px（下）」で仮置き。実機で見て気になれば`AttackLineCharacterName`・`AttackLineText`それぞれのRectTransformの高さ・Y位置を微調整してほしい

- [x] **攻撃弾（DenialBullet）がプレイヤーの位置・敵の方向と連動していない**
  - 怒り本体の登場と同時に、コンタック（プレイヤー）が怒りの正面・画面中央（敵とyを揃えた位置）へ自動移動するようにした（`AngerBattleController.MovePlayerToCenter()`、移動中〜移動後はプレイヤー操作を一時停止）
  - 弾の発射位置を、固定の`bulletSpawnPoint`ではなくプレイヤーの実位置から出すように変更（`FireDenialBullet()`）

- [x] **戦闘中の操作キーがEnterとスペースで混在する**
  - `FireDenialBullet`のトリガーを`KeyCode.Return`から`KeyCode.Space`に変更。開始演出・現実パートの会話送りと同じキーに統一した

- [x] **開発用：ミニゲームへのデバッグ直行キーがない**
  - `MinigameLauncher.cs`に`#if UNITY_EDITOR`で囲んだデバッグ機能を追加。**F1キー**（`debugStartBattleKey`で変更可）を押すと、Yarnの会話を進めなくても怒り戦だけを単体で起動できる（戦闘中でない時のみ有効。ビルドには含まれない）

- [x] **怒り戦終了直後にNullReferenceExceptionが発生することがある（2026-08-19 実機で報告・修正）**
  - 実機Playで、怒り戦を撃破して現実パートの会話に戻った直後に以下の例外が発生することがあった：
    ```
    NullReferenceException: Object reference not set to an instance of an object
    TMPro.TextMeshProUGUI.GenerateTextMesh ()
    ...Yarn.Unity.LinePresenter.RunLineAsync...
    ```
  - **原因（推定）**：`MinigameLauncher.OnBattleFinished()`で`dialogueUIRoot`（本編の会話UI）を再アクティブ化した直後・同じフレームでYarnの会話が次のセリフを表示しようとすると、TextMeshProのCanvas参照がまだ再初期化できておらずヌルポになる
  - このバグはコンパイル・シーン読み込みチェック（バッチモード）では検出できない、Play中のタイミング依存の不具合
  - **修正内容**：`OnBattleFinished()`を、`dialogueUIRoot`再アクティブ化後にコルーチンで1フレーム待ってから会話を再開させる形に変更（`ResumeDialogueNextFrame()`）
  - 実機での再現確認待ち

- [x] **攻撃タイミングの変更（2026-08-19、不安戦の実装と合わせて怒り戦にも反映）**
  - 変更前：「それは異常です」表示中にスペースを押すと、同じ押下で即座に弾も発射していて早すぎた
  - 変更後：スペースでセリフを消す → **1拍（`beatsBeforeFire`）待つ** → プレイヤー操作なしで自動的に弾を発射
  - `AngerBattleController.cs`に反映（`不安戦_打ち合わせメモ.md`にも同内容を記載。FuanBattleControllerと共通の変更）

- [x] **弾幕の文字サイズをランダムに大きくする（2026-08-19）**
  - `fontSizeScaleRange`（1.0〜1.6倍）を追加し、`SpawnFallingCharacter`で文字ごとにランダムなサイズ倍率をかけるようにした
  - 狙い：文字サイズがバラバラだと迫力が出て、怒りの荒々しさに合う。大きい文字ほど避ける意識も強まる

---

## 次にやること候補

- [x] **開始演出の追加**：精神世界パート切り替え直後、コンタックの一言「心の声を震めなくちゃ」を表示 → スペースキーで弾幕回避フェーズ開始（`AngerBattleController.RunBattleSequence()`冒頭に実装済み）
- [x] 電車シーン（シーン2）の台詞をYarn Spinner形式で書く
- [x] オフィス→共通ルート（？？？の声）→電車→帰宅の順で通しの台本にする（Anger.yarnに反映済み）
- [x] 怒り戦のゲームデザイン（横スクロール弾幕回避＋一撃攻撃）を固める
- [x] `Anger_TakeMed`から`<<start_minigame "IkariBattle">>`を呼び出す形でYarn側の記述を追加する（Anger.yarnに反映済み）
- [x] 怒り戦の本実装（`Assets/AngerBattle/`配下にスクリプト一式・プレハブ・README作成済み）
- [x] `AutoSelectFirstOption.cs`の「選択肢1個の時にフォーカスが当たらない」不具合の調査・修正（コード上は解消済みと確認。実機での最終確認は未実施）
- [ ] 怒り戦の後に続く現実パート（拡張稿でいう「現実パート2」に相当する部分）の内容を検討する
- [x] 敵登場後、即「それは異常です」を出すのではなく、敵が一言喋ってから表示する演出（`enemyLine`→スペースで送る→`attackLine`、実装済み・実機確認待ち）
- [ ] **（本格版デバッグメニュー・あとで実装）** F1単発ショートカットに代えて、画面上にボタン一覧を出す「シーンセレクト的」なデバッグメニューを作る
  - 理由：今後シーン／ミニゲームが増えていく予定のため、単発キーではなく一覧から選べる形にしておきたい
  - 現状の`MinigameLauncher`の`#if UNITY_EDITOR`のF1ショートカットはそのまま動作している（実機確認OK）ので、本格版に置き換えるまではこのまま使う

### 実機（Unityエディタ）での確認待ち

コード・シーンの修正はUnityのバッチモードでのコンパイル＋シーン読み込みが正常に通ることまでは確認済み。ただし実際にPlayして見た目・触り心地を見る作業は未実施。特に以下を確認してほしい：
- [ ] `AttackLineBackground`の位置・大きさ・不透明度が、現実パートのセリフ欄と違和感なく揃っているか
- [ ] 怒りのセリフ`enemyLine`が2行（「わたしは怒り。」＋改行＋長めの一文）になったため、`AttackLineText`のボックス（高さ140）に収まって見切れていないか
- [ ] 開始演出「心の声を震めなくちゃ」→スペース→戦闘開始、の流れが一回のスペース押下で誤発動しないか
- [ ] 怒り登場時のプレイヤー中央移動が不自然でないか（`moveToCenterDuration`＝0.3秒で調整可）
- [ ] スペースキーで弾がプレイヤー位置から怒りへ正しく飛ぶか
- [x] F1キー（`MinigameLauncher.debugStartBattleKey`）で怒り戦が単体起動できるか（実機確認OK）
- [ ] 怒りの登場スライドイン（`EnemyAnger.appearFromOffsetX`/`appearDuration`）が意図した速さ・距離になっているか（バッチモード未確認。Unityエディタ起動中のため競合しコンパイルチェック未実施）
- [ ] 話者名（`AttackLineCharacterName`）と本文の分割表示・文字揃え（左上）が期待通りか（バッチモード未確認。Unityエディタ起動中のため競合しコンパイルチェック未実施。コードレビューのみで実装）
