# PKD プロジェクト・作業ルール

このファイルは毎回のセッションで必ず読み込まれる。ユーザーが同じ指示を何度も繰り返さずに済むよう、
繰り返し指摘されたルールをここに書く。

## 0. 現行仕様の正本

- 物語・台詞は`シナリオ全文_読みやすい版.md`を正本とする。
- ゲーム進行・演出・操作は`現行仕様.md`を正本とする。
- `*_打ち合わせメモ.md`と各READMEは旧仕様を含む履歴資料。現在の動作判断にはそのまま使わない。
- 文書とScene・C#が食い違った場合は、ユーザーの最新指示を優先し、実装と正本文書を同じ作業内で揃える。

## 1. 実装は「実装して」と明確に言われてから

- 仕様やバグの話が出ても、それだけでは実装を始めない。まず打ち合わせメモ（`*_打ち合わせメモ.md`）に記録し、
  仕様を確認してから、**明確な実装の指示**（「実装して」「やって」「直しといて」等の直接的な命令）を待つ。
- 「わかった」「そう」「うん」等の**理解の確認への返事は実装許可ではない**。実装していいか自体を聞いた質問への
  「はい」だけが許可になる。
- 「自走」「まかせる」のような曖昧な言葉だけでは、勝手に判断して実装を進めてよいという意味にはならない。
  具体的に何を指すか不明な場合は、実装せずに確認する。
- 過去に4回以上、この確認を怠って実装を始めてしまい、ユーザーを怒らせている。同じ間違いを繰り返さないこと。
- **実装を始める前には、必ず一声かけて確認する。** 内容が明確な指示であっても、いきなりファイルを編集し
  始めるのではなく、「これから〜を実装します」と一言伝えてから着手する。理由：ユーザーは直したい点を
  まとめて一度に伝えたい（自走で次々進められるのではなく、都度チェックポイントが欲しい）。実装してから
  直しが発生すると「手直しの手直しの手直し」になって負担が増える。
- **「どの方法にするか」を聞く質問と、「実装していいか」を聞く質問は別物。両方必ず聞く。**
  設計の選択肢（音をどうするか、文言をどこに入れるか等）を確認しただけでは、実装の許可を得たことには
  ならない。選択肢への回答をもらった後、続けて「この方針で実装を始めていいですか」と改めて聞き、
  その返事を待ってから初めてファイルを編集する。1つの質問にまとめようとせず、必ず2段階で確認する。

## 2. 変更はまとめてから、Unityバッチモード確認は最後に1回

- 一個ずつ小さい変更をして、そのたびにUnityバッチモード検証をするのは非効率。関連する変更はまとめて適用してから、
  最後に1回だけ検証する。
- 例外：変更がそれ単体で大きい・リスクが高い場合（シーンYAMLへの大規模な手書き追加等）は、その変更だけ先に
  検証してから次に進んでよい。

## 3. 「何が残っているか」は必ずソースを見てから答える

- `*_打ち合わせメモ.md`のチェックリストは古くなりがちで、実装済みでもチェックが付いていないことがある
  （逆に、削除・変更された仕様がそのまま残っていることもある）。
- 状況報告・タスク一覧を聞かれたら、メモを鵜呑みにせず、実際のシーン・コードをgrep/readして確認してから答える。

## 4. Unityエディタが開いているか、ファイルを触る前に必ず確認する

- `tasklist //FI "IMAGENAME eq Unity.exe"` で確認してから、シーン・コードファイルを編集する。
- 開いている場合、ファイルを直接編集すると、ユーザー側でCtrl+Sした瞬間にエディタのメモリ上の内容で
  上書きされ、変更が消える危険がある。開いていたら閉じてもらってから作業する。
- 閉じているかどうか先に聞かず、まず自分で確認する（それでも開いていたら、その時点で伝える）。

## 5. 仕様が決まったら、その場でメモに書いて貯める。実装はまとめて後から

- 基本の進め方：話し合いで仕様・方針が決まるたびに、実装するかどうかに関わらず、その場で
  `*_打ち合わせメモ.md`に書く。実装は、そうやって貯まった項目を後でまとめて一気にやる
  （ルール1「実装前に必ず確認」と合わせて運用する）。
- 「言われないと忘れる」と何度も指摘されている。口約束で覚えておこうとしない。忘れる前提で、
  決まったことは決まった瞬間にメモへ書く。

## 6. その他

- ユーザーは音声入力（voice-to-text）でメッセージを送っていることが多く、誤変換・言い淀みが多い。
  文脈から意図を汲み取ること（例：「もじ」→「文字」、「しま」→「シマ」、「おこって」→「怒って」）。
- プロトタイプとしての採点（数値評価）はしない。
- 作業が一区切りついたら、Notion「PKD開発ログ」ページにも進捗を追記する
  （https://app.notion.com/p/3c338511e08a81d89286f54008c29476）。
