# ベッド飛行（BedFlight）打ち合わせメモ

> 履歴資料です。現行仕様は[現行仕様.md](現行仕様.md)、物語・台詞は[シナリオ全文_読みやすい版.md](シナリオ全文_読みやすい版.md)を参照してください。下記には廃止済みの旧仕様も残します。

`怒り戦_打ち合わせメモ.md`・`不安戦_打ち合わせメモ.md`と同じ趣旨で、仕様・実装経緯・トラブル対応を記録する。

## 呼称・位置づけ

- ミニゲーム名：`BedFlight`（ベッド飛行）
- 本編の想定文脈（ユーザー説明より）：コンタックを飲んで怒り・不安を倒し、主人公が社会になじむ → だんだん疲れてしまう → 「どこか遠くに行きたい」と思ったらベッドが宙に浮かび、街の上を自由に飛び回れる → 終盤、コンタックが現れて主人公を一撃で撃ち落とし、現実に連れ戻す
- ~~本編（Yarn）との接続はまだ無い。今回はF1デバッグメニューからの単体起動のみに対応~~ → **接続済み（2026-08-31時点）**：`Assets/Yarn/Escape.yarn`から`<<start_minigame "BedFlight">>`→`<<jump Sadness>>`で接続されている

## ゲーム性（確定）

怒り戦・不安戦とは逆に、**プレイヤー＝主人公、追ってくる敵＝コンタック**という関係が入れ替わっている点が特徴。

1. **開始演出**：一言（`主人公: 少しだけ、遠くに行きたい。`）を表示、スペースキー待ち
2. **開放フェーズ**（`freedomDurationSeconds`、既定14秒）：プレイヤーは画面内を自由に移動できる。追跡演出は一切出さず、空の色が徐々に明るく開けていく（「疲れた现実逃避」→「気持ちよく飛べている」への変化）
3. **チラ見せフェーズ**（`chaseDurationSeconds`、既定9秒）：開放感を大きくは壊さないまま、コンタックが画面端に一瞬だけ姿を見せては引っ込む（Peek）。時間とともに頻度が増え、出現位置が少しずつ内側へ近づく
4. **クライマックス**：プレイヤー操作を止め、コンタックが本登場（画面右からスライドイン）。セリフ（`コンタック: 見つけた。現実に戻ろう。`）を表示してスペースキーで読み進めた後、一拍置いて必ず命中する一撃を放つ（避けさせる仕様ではなく、企画上「必ず見つかって連れ戻される」結末を作るため）
5. 命中後、画面を暗転させてから終了を通知する

### 検討過程の要点（ユーザーとの相談より）

- 「追跡ゲージ」のような抽象的な数値表現は、何に追われているか分からず伝わりにくいという指摘があり不採用
- 代わりに、**怒り戦・不安戦で既に見せているコンタックの見た目をそのまま流用**し、「見覚えのある姿」がチラ見えする頻度・近さで緊張を積み上げる方式に変更（Web調査：チェイス演出は「脅威の正体を事前に理解できているか」が伝わりやすさの鍵、という知見を踏まえた判断）
- 「開放感」がこのシーンの核なので、チラ見せは全体の後半（チラ見せフェーズ）に限定し、前半（開放フェーズ）には一切出さない設計にした
- 今回のスコープは「チラ見せ＋コンタック登場・一撃で落とされるところまで全て」で確定（ユーザー確認済み）。よけて生き残る要素は無い

## 実装内容

**アセット方針**：新規の画像・プレハブアセットは作らず、既存のAngerBattle用スプライトを色替えして再利用（既製アセット不使用の方針を維持しつつ、Unity側でのインポート待ちを避けるため）

- コンタック：`PlayerSprite.png`（怒り戦・不安戦のプレイヤー、青い丸）をそのまま流用 → 「見覚えのある姿」
- ベッド（プレイヤー）：`EnemySprite.png`（四角）を茶色に色替え
- 同乗者・コンタックの弾：`BulletSprite.png`（丸）を色替えして流用
- 空・雲・ビルは全て実行時にコードで生成した単色スプライト（`CityBackgroundScroller.cs`）

**新規スクリプト（`Assets/BedFlight/`）**
- `CityBackgroundScroller.cs`：空の色（開放感でLerp）、ビル・雲の2層パララックス
- `ContacChaser.cs`：チラ見せ（Peek）・本登場（AppearFully）
- `ContacBullet.cs`：クライマックスで必ず命中する一撃（プレイヤー操作停止中に発射するため、狙いは外れない）
- `BedFlightController.cs`：全体の進行管理

**シーン構築**：`Assets/Editor/BedFlightSceneBuilder.cs`（Unityエディタの`Tools > BedFlight > Build Scene`から実行するツール）を用意したが、作業環境にUnityが無く実行できなかったため、**最終的にはFuanBattleRootと同じ方式で`SampleScene.unity`に直接`BedFlightRoot`一式を書き込んで対応**した（Player/Rider/Contac/ContacBulletTemplate/CityBackground+Sky/BattleUI（背景パネル・本文・話者名・終了暗転パネル）/BedFlightController）。既存の`MinigameLauncher`に`bedFlightRoot`/`bedFlightController`参照とデバッグメニュー用の`"BedFlight"`エントリを配線済み。

**安全策として追加した修正**
- `MinigameLauncher.Awake()`で、シーン保存時の状態に関わらず`battleRoot`/`fuanBattleRoot`/`bedFlightRoot`を必ず`SetActive(false)`する処理を追加（誤ってアクティブなまま保存された場合の重さ対策）
- `BedFlightController.FireAndWaitForHit()`で、`Instantiate`した弾を明示的に`SetActive(true)`する処理を追加（`contacBulletPrefab`が正式なPrefabアセットではなく非アクティブなシーン上テンプレートを指しているため）

## 実機確認でのトラブルと対応（2026-08-22）

- 実機Playで重くなる・固まる不具合を報告→調査の結果、**原因はBedFlightのコードとは無関係**。Unity 6の「AI」機能（Muse/AIジェネレーター）が`https://generators.ai.unity.com`にサブスクリプション未加入のままアクセスし続けてConsoleにエラーを連投していたのが主因と判明。AIパネル/Assistantを閉じたところ改善を確認
- F1メニューに「BedFlight」が出ない問題→`Tools > BedFlight > Build Scene`未実行が原因と判明。前述の通り、Unity非搭載環境のため実行できず、シーンファイル直接編集に切り替えた
- **Git同期のトラブル**：ユーザーのローカルPCでは`main`ブランチを直接使っており、しかもそのローカル`main`が`origin`とのブランチ追跡設定がされていない状態だった（リモートURL自体は正しく設定されていた）。この状態はローカル環境を直接見られない私からは事前に把握できず、後手の対応になった。最終的にはユーザー側で新しいフォルダにクローンし直してもらう方向で調整中（本メモ作成時点では反映確認は未完了）

## 実機（Unityエディタ）での確認待ち

- [ ] 新しくクローンしたフォルダでUnityプロジェクトを開き、F1メニューに「BedFlight」が表示されるか
- [ ] 「BedFlight」単体起動→開放フェーズ（空が明るくなる）→チラ見せフェーズ（コンタックが画面端に見え隠れ）→本登場→セリフ→一撃→暗転、の流れが正しく動くか
- [ ] ベッド・同乗者・コンタックの見た目（色替えしたスプライト）が意図通りか
- [ ] 各フェーズの時間・チラ見せの頻度感（`freedomDurationSeconds`/`chaseDurationSeconds`/`peekIntervalStart`/`peekIntervalEnd`等）の微調整

## 「コンタックが全然見えない」報告の原因究明（2026-08-22）

- ユーザー報告：チラ見せも本登場も何も出ない
- 静的なコード・シーン参照の確認（`contac`/`background`/`player`の配線、重複オブジェクトの有無）では異常なし
- Unity Test FrameworkでPlay modeを実際に自動実行するスモークテストを作成して検証 → **PASS**（チラ見せ・本登場とも正常に発生、エラーなし）。コード自体にバグは無いことを確認
- 副産物として発見：`Assets/AngerBattle/Sprites/EnemySprite.png`が、後から怒り戦用に「トゲトゲの爆発」形状へ描き直されており、それをそのまま流用していたベッドの見た目が壊れていた → `BedFlightController.cs`でベッドの絵をこのファイルに依存させず、実行時生成の単色矩形（茶色）に変更して修正（コミット待ち）
- **最終的な根本原因**：Unity EditorでHierarchy/Inspectorをクリックしてフォーカスが移った状態でスペースキーを押しても、Game view側の`Input.GetKeyDown`には届かない。そのため冒頭の一言（「少しだけ、遠くに行きたい。」）を一度も進められておらず、開放フェーズにすら入れていなかった。Game viewをクリックしてフォーカスを当ててからスペースを押すことで解決を確認
- この過程で見つかったUX上の問題：冒頭の一言表示中でも`player.enabled = true`になっているため、プレイヤー（ベッド）が動かせてしまい、「もう進行している」と誤解する原因になっていた（後述の改善点3）

## 追加の改善要望（2026-08-22、実装済み）

- [x] **文字化け対応**：`NotoSansJP SDF`フォントアセットに無い文字（「遠」「戻」等）が表示されずスペースに置き換わっていた問題を修正。
  - 根本原因は2つ重なっていた。(1) `JapaneseFontAssetGenerator.cs`がフォントアセットに焼き込む文字集合をYarnスクリプト（`.yarn`）からしか収集しておらず、BedFlightはまだYarn接続前でセリフがC#の文字列リテラルとしてハードコードされているため対象外だった → `Assets/BedFlight/*.cs`も走査対象に追加。(2) 生成元のソースフォント`Assets/Fonts/NotoSansJP-Regular.ttf`が実体はGit LFSのポインタファイル（132バイトのテキストのみ、実バイナリが未取得）で読み込み自体に失敗していた → 既にLFS解決済みで実体のある`Assets/Fonts/NotoSansJP-VF.ttf`を生成元に変更
  - `Tools > Yarn Dialogue > Generate Japanese Font Asset`を再実行し、784文字を焼き込み直して`NotoSansJP SDF.asset`を再生成（GUIDは維持、既存シーン参照は壊れない）。missing文字ゼロを確認
- [x] **コンタックの登場方向を左端に変更**：`peekEdgeXFar`/`peekEdgeXNear`/`contacRestingPosition`を負のXに変更し、`RunClimax()`のスライドイン元座標も右→左に変更
- [x] **チラ見せの見せ方を変更**：単純なON/OFF切り替えをやめ、`CityBackgroundScroller`に`FindNearestBuilding()`/`GetBuildingTopY()`を追加してチラ見せ位置を近くのビルの屋上ラインに合わせ、`ContacChaser`のSorting Orderをチラ見せ中だけビルより奥（-2）に、本登場時は元の手前（2）に切り替えることで、ビルの陰から頭だけ出ているような見た目にした
- [x] **冒頭の一言表示中はプレイヤー操作を無効化**：`StartBattle()`では`player.enabled = false`のままにし、`RunSequence()`で一言を消した後（またはstartLineが空ならその場で）初めて`enabled = true`にするよう変更

Play mode自動テストで、左側チラ見せ（Sorting Order=-2）→本登場（Sorting Order=2、定位置x=-4）まで一連の流れが正常動作することを確認済み（PASS）。

## BGM追加（2026-08-22、実装済み）

- 使用曲：`Assets/Audio/湖面のワルツ.mp3`
- 再生開始：冒頭の一言をスペースキーで進めた直後（開放フェーズ開始と同時）
- 再生停止：クライマックスでコンタックが本登場したタイミング（`AngerBattleController`の「敵登場と同時にBGMを止める」という既存の演出パターンに合わせた）
- 音量：他のミニゲーム（怒り戦・不安戦）と同じ音量（0.267）。既存の`BattleBGM`コンポーネント（`Assets/AngerBattle/BattleBGM.cs`）をそのまま流用し、`BedFlightRoot`直下に`BGMPlayer`（AudioSource+BattleBGM）を新規配置、`BedFlightController.bgm`に配線した
- ループ区間：曲全体（0〜155.9秒）をループ

**実装中に発覚した副次的な問題（修正済み）**：ユーザーから渡された`湖面のワルツ.mp3`（および`NotoSansJP-Regular.ttf`）は、実体はGit LFSのポインタファイル（テキストのみ数百バイト）で、実際の音声・フォントデータがワーキングツリーに展開されていなかった。幸い実データ自体はローカルの`.git/lfs/objects`キャッシュに存在していたため、該当オブジェクトを該当パスへ直接コピーして復元した（`git lfs pull`は追跡対象ファイルにしか効かないため、未追跡ファイルのポインタ復元には使えなかった）。

**文字抜けの追加対応（2026-08-22）**：「傷」（`FuanBattleController.cs`）も同様に表示されない報告あり。原因は同じで、フォント文字収集がBedFlightのC#しか見ていなかったため。`JapaneseFontAssetGenerator.cs`のスキャン対象を`Assets/AngerBattle`・`Assets/FuanBattle`・`Assets/BedFlight`全てのC#ファイルに拡張し、886文字を焼き込み直して解消（missing文字ゼロを確認）。今後、各ミニゲームのC#にハードコードされた新しいセリフを追加した場合も、フォント再生成（`Tools > Yarn Dialogue > Generate Japanese Font Asset`）を実行すれば自動でカバーされる。

Play mode自動テストで、BGM再生（0.267音量、正しいクリップ）→本登場タイミングでの自動停止まで確認済み（PASS）。

## チラ見せフェーズの削除（2026-08-22・実装済み）

- ユーザー指示：「コンタックのチラ見えいらない」。開放フェーズの後半で入れていたチラ見せ（`ContacChaser.Peek`が画面端に一瞬姿を見せる演出）は不要と判断し、丸ごと削除
- **削除内容**：
  - `BedFlightController.cs`：`RunSequence()`から`RunPeekPhase()`呼び出しを削除（開放フェーズ→クライマックスに直結）。`RunPeekPhase()`/`RandomPeekPosition()`メソッドと、チラ見せ専用フィールド（`chaseDurationSeconds`/`peekIntervalStart`/`peekIntervalEnd`/`peekShowDuration`/`peekEdgeXFar`/`peekEdgeXNear`/`peekYRange`/`peekBehindBuildingOverlap`）を削除
  - `ContacChaser.cs`：`Peek()`/`PeekRoutine()`/`StopPeekIfRunning()`と、チラ見せ専用フィールド（`peekAlpha`/`peekScale`/`peekSortingOrder`/`peekRoutine`）を削除。`AppearFully`（本登場）・`Hide`は維持
  - `CityBackgroundScroller.cs`：チラ見せ専用だった`SetTension()`/`tension`フィールド/`tenseSkyColor`/`FindNearestBuilding()`/`GetBuildingTopY()`を削除（他に呼び出し元がないことを確認済み）。`ApplySkyColor()`は開放度（openness）のみで空の色を決める形に簡略化
- バッチモード確認OK（コンパイルエラーなし、2026-08-22）
- 全体の流れは「開放フェーズ→クライマックス（コンタック本登場〜一撃）→終了演出」の2段階に整理

## 不具合修正：クライマックスでコンタックとベッドが重なる（2026-08-22・実装済み）

- **症状**：実機で、コンタック本登場時にベッド（プレイヤー）と位置が重なって見える
- **原因（コード確認済み）**：`RunClimax()`（[BedFlightController.cs](Assets/BedFlight/BedFlightController.cs)）はコンタックを固定位置`contacRestingPosition`（既定 -4, 0, 0）へスライドインさせる一方、プレイヤー（ベッド）は`player.enabled = false`で操作を止めるだけで位置は動かさない。自由飛行フェーズの終了時にたまたまベッドが`contacRestingPosition`付近にいると重なる
- **対応**：怒り戦・不安戦の`MovePlayerToCenter()`と同じ狙いで`MovePlayerToRestingPosition()`を新設。クライマックス開始時、ベッドを画面中央（`player.minBounds`/`maxBounds`の中央X、Yはコンタックの`contacRestingPosition.y`に合わせてクランプ）へ`moveToRestingDuration`（既定0.3秒）かけて自動移動させる
- **経緯**：一旦「保留、メモだけ」の指示だったが、その後「さっき話してた溜まってる実装やっちゃって」との指示で実装着手
- バッチモード確認OK（コンパイル・シーンインポートともにエラーなし、2026-08-22）
- **実機でのPlay確認は必須**

## 新規実装：開始演出「ベッドが家から飛び立つ」（2026-08-22・実装済み）

- ユーザー確認済み仕様：家は簡易版（単色矩形で実行時生成のシルエット）。飛び出した後、家はその場に残ってスクロールしていく
- **対応**：
  - `Assets/BedFlight/HouseIntro.cs`（新規）：三角屋根＋四角い胴体を実行時にピクセル単位で生成する単色シルエット（`CityBackgroundScroller`の単色スプライト生成方式を踏襲）。`GetLaunchStartPosition()`（ベッドが飛び出す起点＝家の中）、`StartScrolling()`（以後、ビルと同じ`scrollSpeed`で左へスクロール、画面外`despawnX`で非表示）を提供
  - `BedFlightController.cs`：`StartBattle()`で`houseIntro`が設定されていれば、ベッドの本来の開始位置（シーン配置の値）を`burstOutTargetPosition`として覚えておいてから、ベッドを一旦`houseIntro.GetLaunchStartPosition()`（家の中）へ移す。開始の一言をスペースで送った直後、新設の`RunHouseBurstOut()`で家の中から本来の開始位置まで`burstOutDuration`（既定0.4秒）かけて移動させ、到着と同時に`houseIntro.StartScrolling()`を呼んで家をスクロールさせ始める。`houseIntro`が未設定なら従来通り（演出なしでいきなり操作可能）
  - シーン：`SampleScene.unity`の`BedFlightRoot`配下に`HouseIntro`（`(-5, -4.4, 0)`、ビルの接地ラインと同じY）を新規追加、`BedFlightController.houseIntro`に配線済み
- バッチモード確認OK（コンパイル・シーンインポートともにエラーなし、2026-08-22）
- **実機でのPlay確認は必須**：家からベッドが飛び出す動き・その後の家のスクロールが意図通りか、家の見た目（色・大きさ）が気になるか

### 実機フィードバック（2026-08-22）と修正（実装済み）

CADツールで手描きの参考レイアウト画像を共有してもらいながら、以下2点の指摘があり対応：

- [x] **窓を追加**：家に窓が無かった。`HouseIntro.cs`に格子（十字の桟）付きの窓を追加（`windowColor`で色設定可、既定は空色）
- [x] **背景のビル・テキストボックスと被る**：家の初期配置`(-5, -4.4, 0)`が、`CityBackgroundScroller`のランダム配置ビル（`buildingBaseY: -4.4`と同じ接地ライン）や画面下部のセリフ欄と近すぎて重なって見えていた。参考画像（家が高台にあり、下の街並みとは離れている構図）を踏まえ、`(-7, 0.5, 0)`へ変更。ビルの最大高さ（top ≒ 0.1）より確実に高い位置に家を置くことで、ランダム配置のビルとは常に重ならないようにした
- [x] **「ベッドが家の上に乗っているように見える」問題の修正**：ベッドの飛び出し開始位置は元々家の胴体内（`GetLaunchStartPosition`）だったが、Sorting Orderがベッド側が家より手前（前面）だったため、実際には「家の中に隠れている」のではなく「家の上に浮いて乗っている」ように見えていた。`BedFlightController.StartBattle()`で、家の中にいる間はベッド（＋乗っている人の子オブジェクト含む全SpriteRenderer）のSorting Orderを家のシルエットより奥へ一時的に沈め、`RunHouseBurstOut()`で飛び出す瞬間に元の手前の値へ戻すようにした（「家の中から出てくる」ように見せる狙い）
- バッチモード確認OK（コンパイル・シーンインポートともにエラーなし、2026-08-22）
- **実機確認OK（2026-08-22）**：窓の見た目・家の新しい配置（ビル・テキストボックスと重ならない）ともにユーザー確認済み（「これでいいと思う」）

### 再検討：「空に家が浮いているのは変」（2026-08-22・実装済み）

ユーザーから、上記の対応（家をビルより高い位置に浮かせて回避）自体への違和感の指摘。Claudeからの提案（「家はビルと同じ接地ラインに戻し、代わりに家の周囲だけビルが生成されない除外ゾーンを作る」）に対し、「テキストボックスとも被らないようにできるかな」との追加要望があり、実装。

- **調査**：実機スクリーンショットを見返すと、家がビルと同じ接地ライン（`buildingBaseY: -4.4`）にあった時点でも、家の高さ（胴体2＋屋根1.4＝3.4）は画面下部のテキストボックスとは重なっていなかった（重なって見えていたのはあくまで隣接する背景ビルとの方）。そのためテキストボックス対策として特別な追加調整は不要と判断
- **対応**：
  - [x] 家の位置を`(-7, 0.5, 0)`（浮かせた版）→`(-7, -4.4, 0)`（ビルと同じ接地ライン）に戻した
  - [x] `CityBackgroundScroller.cs`に除外ゾーン機能を追加：`hasBuildingExcludeZone`/`buildingExcludeZoneMinX`/`buildingExcludeZoneMaxX`。初期配置（`SpawnLayer`）でビルのX座標がこの範囲に入ったら、範囲外になるまで引き直す（`AvoidExcludeZone()`）
  - **家とビルが「以後もずっと重ならない」ことの根拠**：家（`HouseIntro.scrollSpeed`）とビル（`CityBackgroundScroller.buildingSpeed`）はどちらも2.5で同じ速度でスクロールするため、初期配置時点で重ならなければ、相対位置は（リサイクルで画面右端に再配置されるビルも含めて）以後ずっと変わらない。詳しい根拠：ビルが左端に到達して右端へリサイクルされる際の絶対座標は`rightBound + halfWidth`固定だが、家も同じ速度で左へ動き続けているため、家から見た相対距離は時間とともに開いていく一方で縮まることはない
  - `SampleScene.unity`の`CityBackgroundScroller`に`hasBuildingExcludeZone: 1`、`buildingExcludeZoneMinX: -8.8`、`buildingExcludeZoneMaxX: -5.2`を設定（家の位置-7・胴体幅2.2・屋根幅2.53から算出した半幅1.265に余裕を持たせた範囲）
- バッチモード確認OK（コンパイル・シーンインポートともにエラーなし、2026-08-22）
- **実機でのPlay確認は必須**：家がビルと同じ地面の高さで自然に見えるか、周囲にビルが生成されず浮いて見えないか、テキストボックスと重ならないか

### 不具合修正：家が飛び去った後、隙間が埋まらず残る（2026-08-22・実装済み）

- **症状（ユーザー指摘）**：「家が建ったところの隙間が、それ以降もずっと開いたまま。家がない状態なのにおかしい」
- **原因**：除外ゾーン（`hasBuildingExcludeZone`）は`SpawnLayer`（初期配置）でのみビルの生成を避けているだけで、家が飛び去った後にその場所へビルを補充する仕組みが無かった。ビルは同じ速度（2.5）で家と一緒にスクロールするため、既存のビルとの相対位置は保たれたまま＝隙間もそのまま平行移動して残り続け、自然には埋まらない
- **対応**：`CityBackgroundScroller.cs`に`FillExcludeZone()`を追加。除外ゾーンの中央に、通常のビルと同じ見た目で1棟を即座に生成し、以後`hasBuildingExcludeZone`を無効化（通常のリサイクルに任せる）。`BedFlightController.RunHouseBurstOut()`で、`houseIntro.StartScrolling()`と同時にこれを呼ぶようにした（家が飛び立つ瞬間に、その場へビルが生えて隙間が埋まる）
- バッチモード確認OK（コンパイルエラーなし、2026-08-22）
- **実機でのPlay確認は必須**：家が飛び立った直後、その場所に自然にビルが現れるか

## 新規要望：開始演出「ベッドが家から飛び立つ」（2026-08-22・未実装・要相談）

- ユーザーから手描きスケッチ提供（家のイラスト→矢印→ベッド）
- **イメージ**：現状は開始の一言（「少しだけ、遠くに行きたい。」）表示→スペース→そのままプレイヤーが操作可能になり自由飛行フェーズが始まる、という流れだが、ここに「家からベッドが飛び出してくる」演出を挟みたい
  - スペースキーを押すと、ベッドが家の中から**勢いよく（「バーッと」）飛び立って**画面に出てくる、という動き
  - 現状のシーケンス：`RunSequence()`で`ShowLineAndWaitForSpace(startLine)` → `player.enabled = true` → 即座に自由飛行フェーズ開始（[BedFlightController.cs:112](Assets/BedFlight/BedFlightController.cs:112)）。この「スペース押下」と「操作可能になる瞬間」の間に、家からの飛び出しアニメーションを挟むイメージと理解
- **未確定・要相談な点**：
  - [ ] 家のビジュアル：スケッチはシルエット風（三角屋根＋四角い胴体＋小さい張り出し）。色・スタイル（既存の単色矩形生成方式を踏襲するか、新規スプライトを描くか）は未定
  - [ ] 家は画面のどこに配置するか（画面外からベッドだけ飛んでくる簡易版 or 実際に家のイラストを表示してそこから飛び出す本格版）
  - [ ] 飛び出した後、家はどうなるか（消える／画面奥へスクロールして消えていく／背景の一部として残る）
  - [ ] 飛び出す方向・軌道（まっすぐ画面手前へ／斜め上へ等）、演出の長さ
  - [ ] この演出の間はプレイヤー操作を止めておくか（現状の「一言表示中は操作不可」の設計を踏襲するイメージ）
- **ステータス**：仕様確認中、実装はまだ着手していない

## 今後の課題（未着手）

- [x] Yarnとの接続 → `Escape.yarn`（旧`Apathy.yarn`、2026-08-22に`Escape`へリネーム）の`Escape_Loop`ノード末尾から`<<start_minigame "BedFlight">>`を呼ぶ形で対応済み（2026-08-22）
- [ ] SEの追加
- [ ] 開放フェーズの長さ（`freedomDurationSeconds`）のパラメータ調整（実機で感触を見てから）
- [x] クライマックスでのベッド自動移動 → 実装済み（上記「不具合修正」参照）
- [x] 開始演出「ベッドが家から飛び立つ」 → 実装済み（上記「新規実装」参照）

## 逃避行専用背景・ベビーメリー（2026-09-01、実装）

- 正式素材：`Assets/Sprites/touhikou/`
  - `mati_mae.png`：街の前景
  - `mati_ushiro.png`：街の後景
  - `kumo.png`：雲
  - `marry.png`：メリー本体
  - `maryy_himo.png`：天井からの吊り紐
- 表示範囲は`BedFlightRoot`内だけとし、ほかの章には表示しない。
- 街の前景、街の後景、雲を別速度でスクロールする。素材参照が欠けた場合は従来の単色仮背景へフォールバックする。
- メリーはワールド上の`SpriteRenderer`として画面上部のカメラ位置へ追従する。吊り紐を上端支点で小さく揺らし、本体は少し遅れて揺らす。上下移動も視認できるかどうか程度に抑える。
- `BedFlightBackdropInstaller`を追加し、既存の`BedFlightRoot`を壊さず冪等に差し替えられるようにした。`BedFlightSceneBuilder`からも同じ処理を呼び、再構築で演出が消えないようにする。
- 雲画像の数ピクセルだけの自動切り出しは使用せず、旧背景・重複メリー・作業用一時物をシーンへ残さない。

### 画面全体移動・背景先読み・更新版メリーへの調整（2026-09-01、実装）

- ベッドの移動範囲を固定座標ではなく、実行時のカメラ表示範囲とベッド＋乗員の大きさから算出する。横長画面を含め、見切れない範囲で画面全体へ移動できる。
- 街の前景・後景・雲は、カメラの横幅に画面外余白を加えた範囲へ起動時にまとめて生成する。左端で再利用する際も画面外右側へ戻し、スクロール途中の突然表示を防ぐ。
- 更新版`marry.png`の透明部分に合わせてスプライト切り抜きを更新した。メリー全体を従来の約1.44倍にし、画面上部の中央より少し右（Viewport X=0.60）へ配置した。

### ベビーメリーの調整機能・描画順（2026-09-01、実装）

- `BabyMobileAmbient`のInspectorから、画面内位置（`viewportAnchor`）、大きさ（`visualScale`）、描画順（`sortingOrder`）、色（`visualTint`）、揺れ幅・速度を調整できるようにした。
- 画面上の位置をViewport X=`0.68`へ右寄せした。
- 描画順を`-6`にし、雲（`-5`）より後ろ、かつプレイヤーより後ろへ配置した。

### ベッド＋主人公の正式画像・旧家演出の撤去（2026-09-01、実装）

- `参考画像/Codex 画像 2026年9月1日 23_48_40.png`に仮置きされていた「ベッド＋寝ている主人公」の透過画像を採用した。
- 元の参考画像は保持し、透明余白だけ詰めたゲーム用コピーを`Assets/Sprites/touhikou/flying_bed_player.png`へ追加した。
- 実行時に単色ベッドへ上書きする処理をフォールバック時だけに限定し、旧`Rider`仮画像を撤去した。
- `BedFlightRoot/HouseIntro`とController参照を撤去し、旧・家から飛び出す演出を無効化した。街並みの家用除外ゾーンも無効化した。
- `BedFlightSceneBuilder`を将来再実行しても、正式画像と家なしの構成へ戻るよう更新した。ただし今回の反映・検証では同ビルダーを実行しない。
