# ベッド飛行（BedFlight）打ち合わせメモ

`怒り戦_打ち合わせメモ.md`・`不安戦_打ち合わせメモ.md`と同じ趣旨で、仕様・実装経緯・トラブル対応を記録する。

## 呼称・位置づけ

- ミニゲーム名：`BedFlight`（ベッド飛行）
- 本編の想定文脈（ユーザー説明より）：コンタックを飲んで怒り・不安を倒し、主人公が社会になじむ → だんだん疲れてしまう → 「どこか遠くに行きたい」と思ったらベッドが宙に浮かび、街の上を自由に飛び回れる → 終盤、コンタックが現れて主人公を一撃で撃ち落とし、現実に連れ戻す
- **本編（Yarn）との接続はまだ無い**。今回はF1デバッグメニューからの単体起動のみに対応

## ゲーム性（確定）

怒り戦・不安戦とは逆に、**プレイヤー＝主人公、追ってくる敵＝コンタック**という関係が入れ替わっている点が特徴。

1. **開始演出**：一言（`主人公: 少しだけ、遠くに行きたい。`）を表示、スペースキー待ち
2. **開放フェーズ**（`freedomDurationSeconds`、既定14秒）：プレイヤーは画面内を自由に移動できる。追跡演出は一切出さず、空の色が徐々に明るく開けていく（「疲れた现実逃避」→「気持ちよく飛べている」への変化）
3. **チラ見せフェーズ**（`chaseDurationSeconds`、既定9秒）：開放感を大きくは壊さないまま、コンタックが画面端に一瞬だけ姿を見せては引っ込む（Peek）。時間とともに頻度が増え、出現位置が少しずつ内側へ近づく
4. **クライマックス**：プレイヤー操作を止め、コンタックが本登場（画面右からスライドイン）。セリフ（`コンタック: 見つけた。現実に戻ろう。`）を表示してスペースキーで読み進めた後、一拍置いて必ず命中する一撃を放つ（避けさせる仕様ではなく、企画上「必ず見つかって連れ戻される」結末を作るため）
5. 命中後、画面を暗転させてから終了を通知する

### 検討過程の要点（ユーザーとの相談より）

- 「追跡ゲージ」のような抽象的な数値表現は、何に追われているか分からず伝わりにくいという指摘があり不採用
- 代わりに、**怒り戦・不安戦で既に見せているコンタックの見た目をそのまま流用**し、「見覚えのある姿」がチラ見えする頻度・近さで緊張を積み上げる方式に変更（Web調査：チェイス演出は「脅威の正体を事前に理解できているか」が伝わりやすさの鍵、という知見を踏まえた判断）
- 「開放感」がこのシーンの核なので、チラ見せは全体の後半（チラ見せフェーズ）に限定し、前半（開放フェーズ）には一切出さない設計にした
- 今回のスコープは「チラ見せ＋コンタック登場・一撃で落とされるところまで全て」で確定（ユーザー確認済み）。よけて生き残る要素は無い

## 実装内容

**アセット方針**：新規の画像・プレハブアセットは作らず、既存のAngerBattle用スプライトを色替えして再利用（既製アセット不使用の方針を維持しつつ、Unity側でのインポート待ちを避けるため）

- コンタック：`PlayerSprite.png`（怒り戦・不安戦のプレイヤー、青い丸）をそのまま流用 → 「見覚えのある姿」
- ベッド（プレイヤー）：`EnemySprite.png`（四角）を茶色に色替え
- 同乗者・コンタックの弾：`BulletSprite.png`（丸）を色替えして流用
- 空・雲・ビルは全て実行時にコードで生成した単色スプライト（`CityBackgroundScroller.cs`）

**新規スクリプト（`Assets/BedFlight/`）**
- `CityBackgroundScroller.cs`：空の色（開放感でLerp）、ビル・雲の2層パララックス
- `ContacChaser.cs`：チラ見せ（Peek）・本登場（AppearFully）
- `ContacBullet.cs`：クライマックスで必ず命中する一撃（プレイヤー操作停止中に発射するため、狙いは外れない）
- `BedFlightController.cs`：全体の進行管理

**シーン構築**：`Assets/Editor/BedFlightSceneBuilder.cs`（Unityエディタの`Tools > BedFlight > Build Scene`から実行するツール）を用意したが、作業環境にUnityが無く実行できなかったため、**最終的にはFuanBattleRootと同じ方式で`SampleScene.unity`に直接`BedFlightRoot`一式を書き込んで対応**した（Player/Rider/Contac/ContacBulletTemplate/CityBackground+Sky/BattleUI（背景パネル・本文・話者名・終了暗転パネル）/BedFlightController）。既存の`MinigameLauncher`に`bedFlightRoot`/`bedFlightController`参照とデバッグメニュー用の`"BedFlight"`エントリを配線済み。

**安全策として追加した修正**
- `MinigameLauncher.Awake()`で、シーン保存時の状態に関わらず`battleRoot`/`fuanBattleRoot`/`bedFlightRoot`を必ず`SetActive(false)`する処理を追加（誤ってアクティブなまま保存された場合の重さ対策）
- `BedFlightController.FireAndWaitForHit()`で、`Instantiate`した弾を明示的に`SetActive(true)`する処理を追加（`contacBulletPrefab`が正式なPrefabアセットではなく非アクティブなシーン上テンプレートを指しているため）

## 実機確認でのトラブルと対応（2026-08-22）

- 実機Playで重くなる・固まる不具合を報告→調査の結果、**原因はBedFlightのコードとは無関係**。Unity 6の「AI」機能（Muse/AIジェネレーター）が`https://generators.ai.unity.com`にサブスクリプション未加入のままアクセスし続けてConsoleにエラーを連投していたのが主因と判明。AIパネル/Assistantを閉じたところ改善を確認
- F1メニューに「BedFlight」が出ない問題→`Tools > BedFlight > Build Scene`未実行が原因と判明。前述の通り、Unity非搭載環境のため実行できず、シーンファイル直接編集に切り替えた
- **Git同期のトラブル**：ユーザーのローカルPCでは`main`ブランチを直接使っており、しかもそのローカル`main`が`origin`とのブランチ追跡設定がされていない状態だった（リモートURL自体は正しく設定されていた）。この状態はローカル環境を直接見られない私からは事前に把握できず、後手の対応になった。最終的にはユーザー側で新しいフォルダにクローンし直してもらう方向で調整中（本メモ作成時点では反映確認は未完了）

## 実機（Unityエディタ）での確認待ち

- [ ] 新しくクローンしたフォルダでUnityプロジェクトを開き、F1メニューに「BedFlight」が表示されるか
- [ ] 「BedFlight」単体起動→開放フェーズ（空が明るくなる）→チラ見せフェーズ（コンタックが画面端に見え隠れ）→本登場→セリフ→一撃→暗転、の流れが正しく動くか
- [ ] ベッド・同乗者・コンタックの見た目（色替えしたスプライト）が意図通りか
- [ ] 各フェーズの時間・チラ見せの頻度感（`freedomDurationSeconds`/`chaseDurationSeconds`/`peekIntervalStart`/`peekIntervalEnd`等）の微調整

## 今後の課題（未着手）

- [ ] Yarnとの接続（どのノードから`<<start_minigame "BedFlight">>`を呼ぶか）
- [ ] BGM・SEの追加（今回は音無し）
- [ ] 開放フェーズ・チラ見せフェーズのパラメータ調整（実機で感触を見てから）
